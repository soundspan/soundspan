{{- $haEnabled := eq (include "soundspan.haEnabled" .) "true" }}
{{- $backendWorkerEnabled := eq (include "soundspan.backendWorkerEnabled" .) "true" }}
{{- $backendWorkerReplicas := include "soundspan.backendWorkerReplicas" . | int }}
{{- $backendWorkerPdbEnabled := and (gt $backendWorkerReplicas 1) (ternary .Values.haMode.pdb.enabled .Values.backendWorker.pdb.enabled $haEnabled) }}
{{- $readinessRequireDependencies := ternary .Values.haMode.runtime.readinessRequireDependencies .Values.config.readinessRequireDependencies $haEnabled }}
{{- if and (eq .Values.deploymentMode "individual") $backendWorkerEnabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "soundspan.fullname" . }}-backend-worker
  labels:
    {{- include "soundspan.componentLabels" (dict "context" . "component" "backend-worker") | nindent 4 }}
spec:
  replicas: {{ $backendWorkerReplicas }}
  strategy:
    {{- toYaml .Values.backendWorker.strategy | nindent 4 }}
  selector:
    matchLabels:
      {{- include "soundspan.componentSelectorLabels" (dict "context" . "component" "backend-worker") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "soundspan.componentSelectorLabels" (dict "context" . "component" "backend-worker") | nindent 8 }}
        {{- with .Values.global.labels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.global.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      {{- with .Values.global.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "soundspan.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.global.podSecurityContext | nindent 8 }}
      containers:
        - name: backend-worker
          image: "{{ .Values.backendWorker.image.repository }}:{{ .Values.backendWorker.image.tag }}"
          imagePullPolicy: {{ .Values.backendWorker.image.pullPolicy }}
          ports:
            - name: health
              containerPort: {{ .Values.backendWorker.health.port }}
              protocol: TCP
          {{- with .Values.backendWorker.command }}
          command:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- with .Values.backendWorker.args }}
          args:
            {{- toYaml . | nindent 12 }}
          {{- end }}
          securityContext:
            {{- toYaml .Values.global.securityContext | nindent 12 }}
          {{- $backendWorkerEnv := .Values.backendWorker.env | default (dict) }}
          {{- $backendWorkerHasPostgresSecretEnv := or .Values.postgresql.enabled (not .Values.postgresql.external.url) }}
          {{- $backendWorkerExplicitEnvKeys := dict
            "NODE_ENV" true
            "PORT" true
            "TZ" true
            "BACKEND_PROCESS_ROLE" true
            "WORKER_HEALTH_PORT" true
            "SCHEDULER_CLAIM_SKIP_WARN_THRESHOLD" true
            "READINESS_REQUIRE_DEPENDENCIES" true
            "READINESS_DEPENDENCY_CHECK_INTERVAL_MS" true
            "READINESS_DEPENDENCY_CHECK_TIMEOUT_MS" true
            "MUSIC_PATH" true
            "TRANSCODE_CACHE_PATH" true
            "POSTGRES_USER" true
            "POSTGRES_PASSWORD" true
            "POSTGRES_DB" true
            "DATABASE_URL" true
            "REDIS_URL" true
            "REDIS_FLUSH_ON_STARTUP" true
            "SESSION_SECRET" true
            "SETTINGS_ENCRYPTION_KEY" true
            "INTERNAL_API_SECRET" true
            "TIDAL_SIDECAR_URL" true
            "YTMUSIC_STREAMER_URL" true
            "ALLOWED_ORIGINS" true
            "LOG_LEVEL" true
            "DOCS_PUBLIC" true
            "LIDARR_ENABLED" true
            "LIDARR_URL" true
            "LIDARR_API_KEY" true
            "AUDIOBOOKSHELF_URL" true
            "AUDIOBOOKSHELF_TOKEN" true
            "LASTFM_API_KEY" true
            "FANART_API_KEY" true
            "OPENAI_API_KEY" true
          }}
          env:
            {{- if hasKey $backendWorkerEnv "NODE_ENV" }}
            - name: NODE_ENV
              value: {{ index $backendWorkerEnv "NODE_ENV" | quote }}
            {{- else }}
            - name: NODE_ENV
              value: {{ .Values.config.nodeEnv | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "PORT" }}
            - name: PORT
              value: {{ index $backendWorkerEnv "PORT" | quote }}
            {{- else }}
            - name: PORT
              value: {{ .Values.backend.port | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "TZ" }}
            - name: TZ
              value: {{ index $backendWorkerEnv "TZ" | quote }}
            {{- else }}
            - name: TZ
              value: {{ .Values.config.timezone | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "BACKEND_PROCESS_ROLE" }}
            - name: BACKEND_PROCESS_ROLE
              value: {{ index $backendWorkerEnv "BACKEND_PROCESS_ROLE" | quote }}
            {{- else }}
            - name: BACKEND_PROCESS_ROLE
              value: {{ (default "worker" .Values.backendWorker.processRole) | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "WORKER_HEALTH_PORT" }}
            - name: WORKER_HEALTH_PORT
              value: {{ index $backendWorkerEnv "WORKER_HEALTH_PORT" | quote }}
            {{- else }}
            - name: WORKER_HEALTH_PORT
              value: {{ .Values.backendWorker.health.port | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "SCHEDULER_CLAIM_SKIP_WARN_THRESHOLD" }}
            - name: SCHEDULER_CLAIM_SKIP_WARN_THRESHOLD
              value: {{ index $backendWorkerEnv "SCHEDULER_CLAIM_SKIP_WARN_THRESHOLD" | quote }}
            {{- else }}
            - name: SCHEDULER_CLAIM_SKIP_WARN_THRESHOLD
              value: {{ .Values.config.schedulerClaimSkipWarnThreshold | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "READINESS_REQUIRE_DEPENDENCIES" }}
            - name: READINESS_REQUIRE_DEPENDENCIES
              value: {{ index $backendWorkerEnv "READINESS_REQUIRE_DEPENDENCIES" | quote }}
            {{- else }}
            - name: READINESS_REQUIRE_DEPENDENCIES
              value: {{ $readinessRequireDependencies | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "READINESS_DEPENDENCY_CHECK_INTERVAL_MS" }}
            - name: READINESS_DEPENDENCY_CHECK_INTERVAL_MS
              value: {{ index $backendWorkerEnv "READINESS_DEPENDENCY_CHECK_INTERVAL_MS" | quote }}
            {{- else }}
            - name: READINESS_DEPENDENCY_CHECK_INTERVAL_MS
              value: {{ .Values.config.readinessDependencyCheckIntervalMs | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "READINESS_DEPENDENCY_CHECK_TIMEOUT_MS" }}
            - name: READINESS_DEPENDENCY_CHECK_TIMEOUT_MS
              value: {{ index $backendWorkerEnv "READINESS_DEPENDENCY_CHECK_TIMEOUT_MS" | quote }}
            {{- else }}
            - name: READINESS_DEPENDENCY_CHECK_TIMEOUT_MS
              value: {{ .Values.config.readinessDependencyCheckTimeoutMs | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "MUSIC_PATH" }}
            - name: MUSIC_PATH
              value: {{ index $backendWorkerEnv "MUSIC_PATH" | quote }}
            {{- else }}
            - name: MUSIC_PATH
              value: /music
            {{- end }}
            {{- if hasKey $backendWorkerEnv "TRANSCODE_CACHE_PATH" }}
            - name: TRANSCODE_CACHE_PATH
              value: {{ index $backendWorkerEnv "TRANSCODE_CACHE_PATH" | quote }}
            {{- else }}
            - name: TRANSCODE_CACHE_PATH
              value: /music/.soundspan/transcodes
            {{- end }}
            # Secret-backed env vars
            {{- if hasKey $backendWorkerEnv "POSTGRES_USER" }}
            - name: POSTGRES_USER
              value: {{ index $backendWorkerEnv "POSTGRES_USER" | quote }}
            {{- else if $backendWorkerHasPostgresSecretEnv }}
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: {{ include "soundspan.secretName" . }}
                  key: POSTGRES_USER
            {{- end }}
            {{- if hasKey $backendWorkerEnv "POSTGRES_PASSWORD" }}
            - name: POSTGRES_PASSWORD
              value: {{ index $backendWorkerEnv "POSTGRES_PASSWORD" | quote }}
            {{- else if $backendWorkerHasPostgresSecretEnv }}
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "soundspan.secretName" . }}
                  key: POSTGRES_PASSWORD
            {{- end }}
            {{- if hasKey $backendWorkerEnv "POSTGRES_DB" }}
            - name: POSTGRES_DB
              value: {{ index $backendWorkerEnv "POSTGRES_DB" | quote }}
            {{- else if $backendWorkerHasPostgresSecretEnv }}
            - name: POSTGRES_DB
              valueFrom:
                secretKeyRef:
                  name: {{ include "soundspan.secretName" . }}
                  key: POSTGRES_DB
            {{- end }}
            {{- if hasKey $backendWorkerEnv "DATABASE_URL" }}
            - name: DATABASE_URL
              value: {{ index $backendWorkerEnv "DATABASE_URL" | quote }}
            {{- else }}
            - name: DATABASE_URL
              value: {{ include "soundspan.databaseUrl" . | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "REDIS_URL" }}
            - name: REDIS_URL
              value: {{ index $backendWorkerEnv "REDIS_URL" | quote }}
            {{- else }}
            - name: REDIS_URL
              value: {{ include "soundspan.redisUrl" . | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "REDIS_FLUSH_ON_STARTUP" }}
            - name: REDIS_FLUSH_ON_STARTUP
              value: {{ index $backendWorkerEnv "REDIS_FLUSH_ON_STARTUP" | quote }}
            {{- else }}
            - name: REDIS_FLUSH_ON_STARTUP
              value: {{ .Values.config.redisFlushOnStartup | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "SESSION_SECRET" }}
            - name: SESSION_SECRET
              value: {{ index $backendWorkerEnv "SESSION_SECRET" | quote }}
            {{- else }}
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "soundspan.secretName" . }}
                  key: SESSION_SECRET
            {{- end }}
            {{- if hasKey $backendWorkerEnv "SETTINGS_ENCRYPTION_KEY" }}
            - name: SETTINGS_ENCRYPTION_KEY
              value: {{ index $backendWorkerEnv "SETTINGS_ENCRYPTION_KEY" | quote }}
            {{- else }}
            - name: SETTINGS_ENCRYPTION_KEY
              valueFrom:
                secretKeyRef:
                  name: {{ include "soundspan.secretName" . }}
                  key: SETTINGS_ENCRYPTION_KEY
            {{- end }}
            {{- if hasKey $backendWorkerEnv "INTERNAL_API_SECRET" }}
            - name: INTERNAL_API_SECRET
              value: {{ index $backendWorkerEnv "INTERNAL_API_SECRET" | quote }}
            {{- else }}
            - name: INTERNAL_API_SECRET
              valueFrom:
                secretKeyRef:
                  name: {{ include "soundspan.secretName" . }}
                  key: INTERNAL_API_SECRET
            {{- end }}
            {{- if hasKey $backendWorkerEnv "TIDAL_SIDECAR_URL" }}
            - name: TIDAL_SIDECAR_URL
              value: {{ index $backendWorkerEnv "TIDAL_SIDECAR_URL" | quote }}
            {{- else }}
            - name: TIDAL_SIDECAR_URL
              value: {{ include "soundspan.tidalSidecarUrl" . | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "YTMUSIC_STREAMER_URL" }}
            - name: YTMUSIC_STREAMER_URL
              value: {{ index $backendWorkerEnv "YTMUSIC_STREAMER_URL" | quote }}
            {{- else }}
            - name: YTMUSIC_STREAMER_URL
              value: {{ include "soundspan.ytmusicStreamerUrl" . | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "ALLOWED_ORIGINS" }}
            - name: ALLOWED_ORIGINS
              value: {{ index $backendWorkerEnv "ALLOWED_ORIGINS" | quote }}
            {{- else if .Values.config.allowedOrigins }}
            - name: ALLOWED_ORIGINS
              value: {{ .Values.config.allowedOrigins | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "LOG_LEVEL" }}
            - name: LOG_LEVEL
              value: {{ index $backendWorkerEnv "LOG_LEVEL" | quote }}
            {{- else if .Values.config.logLevel }}
            - name: LOG_LEVEL
              value: {{ .Values.config.logLevel | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "DOCS_PUBLIC" }}
            - name: DOCS_PUBLIC
              value: {{ index $backendWorkerEnv "DOCS_PUBLIC" | quote }}
            {{- else if .Values.config.docsPublic }}
            - name: DOCS_PUBLIC
              value: "true"
            {{- end }}
            {{- if hasKey $backendWorkerEnv "LIDARR_ENABLED" }}
            - name: LIDARR_ENABLED
              value: {{ index $backendWorkerEnv "LIDARR_ENABLED" | quote }}
            {{- else if .Values.config.lidarrEnabled }}
            - name: LIDARR_ENABLED
              value: "true"
            {{- end }}
            {{- if hasKey $backendWorkerEnv "LIDARR_URL" }}
            - name: LIDARR_URL
              value: {{ index $backendWorkerEnv "LIDARR_URL" | quote }}
            {{- else if .Values.config.lidarrEnabled }}
            - name: LIDARR_URL
              value: {{ .Values.config.lidarrUrl | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "LIDARR_API_KEY" }}
            - name: LIDARR_API_KEY
              value: {{ index $backendWorkerEnv "LIDARR_API_KEY" | quote }}
            {{- else if .Values.config.lidarrEnabled }}
            - name: LIDARR_API_KEY
              value: {{ .Values.config.lidarrApiKey | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "AUDIOBOOKSHELF_URL" }}
            - name: AUDIOBOOKSHELF_URL
              value: {{ index $backendWorkerEnv "AUDIOBOOKSHELF_URL" | quote }}
            {{- else if .Values.config.audiobookshelfUrl }}
            - name: AUDIOBOOKSHELF_URL
              value: {{ .Values.config.audiobookshelfUrl | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "AUDIOBOOKSHELF_TOKEN" }}
            - name: AUDIOBOOKSHELF_TOKEN
              value: {{ index $backendWorkerEnv "AUDIOBOOKSHELF_TOKEN" | quote }}
            {{- else if .Values.config.audiobookshelfUrl }}
            - name: AUDIOBOOKSHELF_TOKEN
              value: {{ .Values.config.audiobookshelfToken | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "LASTFM_API_KEY" }}
            - name: LASTFM_API_KEY
              value: {{ index $backendWorkerEnv "LASTFM_API_KEY" | quote }}
            {{- else if .Values.config.lastfmApiKey }}
            - name: LASTFM_API_KEY
              value: {{ .Values.config.lastfmApiKey | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "FANART_API_KEY" }}
            - name: FANART_API_KEY
              value: {{ index $backendWorkerEnv "FANART_API_KEY" | quote }}
            {{- else if .Values.config.fanartApiKey }}
            - name: FANART_API_KEY
              value: {{ .Values.config.fanartApiKey | quote }}
            {{- end }}
            {{- if hasKey $backendWorkerEnv "OPENAI_API_KEY" }}
            - name: OPENAI_API_KEY
              value: {{ index $backendWorkerEnv "OPENAI_API_KEY" | quote }}
            {{- else if .Values.config.openaiApiKey }}
            - name: OPENAI_API_KEY
              value: {{ .Values.config.openaiApiKey | quote }}
            {{- end }}
            {{- range $key := keys $backendWorkerEnv | sortAlpha }}
            {{- if not (hasKey $backendWorkerExplicitEnvKeys $key) }}
            - name: {{ $key }}
              value: {{ index $backendWorkerEnv $key | quote }}
            {{- end }}
            {{- end }}
          {{- $globalEnv := .Values.global.env | default (dict) }}
          {{- $globalEnvFrom := .Values.global.envFrom | default (list) }}
          {{- $backendWorkerEnvFrom := .Values.backendWorker.envFrom | default (list) }}
          {{- if or (gt (len $globalEnv) 0) (gt (len $globalEnvFrom) 0) (gt (len $backendWorkerEnvFrom) 0) }}
          envFrom:
            {{- if gt (len $globalEnv) 0 }}
            - configMapRef:
                name: {{ include "soundspan.fullname" . }}-global-env
            {{- end }}
            {{- with $globalEnvFrom }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- with $backendWorkerEnvFrom }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          {{- end }}
          livenessProbe:
            httpGet:
              path: /health/live
              port: health
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health/ready
              port: health
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: music
              mountPath: /music
          resources:
            {{- toYaml .Values.backendWorker.resources | nindent 12 }}
      volumes:
        - name: music
          {{- if .Values.music.persistence.existingClaim }}
          persistentVolumeClaim:
            claimName: {{ .Values.music.persistence.existingClaim }}
          {{- else if .Values.music.persistence.enabled }}
          persistentVolumeClaim:
            claimName: {{ include "soundspan.fullname" . }}-music
          {{- else if .Values.music.hostPath }}
          hostPath:
            path: {{ .Values.music.hostPath }}
            type: Directory
          {{- else }}
          emptyDir: {}
          {{- end }}
      {{- with .Values.backendWorker.topologySpreadConstraints }}
      topologySpreadConstraints:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.backendWorker.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- else }}
      {{- with .Values.global.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- end }}
      {{- with .Values.backendWorker.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- else }}
      {{- with .Values.global.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- end }}
      {{- with .Values.backendWorker.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- else }}
      {{- with .Values.global.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- end }}
{{- if $backendWorkerPdbEnabled }}
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "soundspan.fullname" . }}-backend-worker
  labels:
    {{- include "soundspan.componentLabels" (dict "context" . "component" "backend-worker") | nindent 4 }}
spec:
  selector:
    matchLabels:
      {{- include "soundspan.componentSelectorLabels" (dict "context" . "component" "backend-worker") | nindent 6 }}
  {{- if hasKey .Values.backendWorker.pdb "maxUnavailable" }}
  maxUnavailable: {{ .Values.backendWorker.pdb.maxUnavailable }}
  {{- else }}
  minAvailable: {{ .Values.backendWorker.pdb.minAvailable | default 1 }}
  {{- end }}
{{- end }}
{{- end }}
