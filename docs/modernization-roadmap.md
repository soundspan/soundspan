# Modernization Roadmap

Canonical index of the modernization review findings (F1–F53, plus F54 added on audit) from the **2026-06-16** review of soundspan. The review mapped each subsystem, assessed it across readability / performance / idempotency / security / extensibility / dependencies, then adversarially re-read every finding against the real code (53 high-confidence findings, **no true false positive**), grouped them into the epics below, and was itself **independently audited** — the audit's corrections and analysis live inline in this document (see "Corrections from the independent audit" below and the closing "Architecture: strategy going forward" section) rather than in a separate file.

> **Read me first.**
>
> - **Status:** PR #21 = 5 complete + 3 partial (8 findings). The **Wave-1 continuation** (stacked PRs #22–#32) then shipped: **F54, F22, F29, F28, F36, F35, F33** complete; the **F3/F24/F6/F18** follow-ups (cast removal + TDD backfill); **F32** partial (rate-limit + no-full-scan shipped, hard-fail-closed auth deferred). The whole **#11 secrets program is done (4/4)**. **F31** and **F37** are **deferred-with-a-note** (see their entries) — F31 because the env had no Python toolchain to verify the security-critical sidecar code, F37 as a cross-cutting redesign. **Merged to `main` so far:** #3, #9, #21, #22 (F54), and #23 (the TDD backfill), each squash-merged; a post-review fix wave landed directly on `main` after them. The remaining Wave-1 continuation — stacked PRs #24–#26 and #28–#33 (nine PRs) — merged to `main` on 2026-07-08. Releases **1.6.0**, **1.7.0**, **1.8.0**, **2.0.0**, and **2.1.0** have since shipped; nothing from the Wave-1 continuation remains unmerged. ✅ complete · 🟡 partial · ⬜ open/deferred.
> - **Audit corrections are inline.** Where the audit corrected a count, severity, or premise, the finding carries an **Audit note** (✓ = re-verified against the tree here; "per audit" = relayed). Severity columns show `orig→recalibrated` where adjusted for this single-operator deployment.
> - **Line numbers** in finding bodies were captured against the pre-#3 branch tree and may be off by a few lines on `main`.
> - **Historical paths stay verbatim.** Finding bodies preserve the files and services that existed when each review was recorded, even after later releases remove them. Use the current architecture, testing, and upgrade guides for the live tree.
> - **Not exhaustive.** Areas the review did **not** assess: test quality/coverage, observability/structured logging, data-migration safety, and PWA-offline/accessibility. "53" is ~51 distinct issues (F2≈F44, F17≈F47 are duplicates kept for their differing angles).

## Corrections from the independent audit

The audit found **0 true false positives** but several packaging/measurement errors and one missed bug. The load-bearing ones (re-verified here):

| Item              | Correction                                                                                                                                                                         |
| ----------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| **New bug (F54)** | Unauthenticated TOCTOU in `/device-link/verify` mints full-privilege API keys — same class as F26, missed by the review. Added below.                                              |
| F45               | "five workflows" → **3** (was 4; the former AWM-health PR-gate workflow was removed in the ACM→AWM rename); Dockerfiles → **7**.                                                   |
| F46               | "~480 `@ts-ignore`" → **1** (480 is `eslint-disable`).                                                                                                                             |
| F35               | "helmet ships without HSTS/CSP" → helmet **does** ship both by default.                                                                                                            |
| F28               | "breaking / keys re-issued" → **no re-issue** (HMAC-in-place; keys keep working).                                                                                                  |
| F3                | both gratuitous casts now removed (line-433 in #21, line-1530 in `feat/f3-second-cast`); the `DownloadJobMetadata` typing / discoverWeekly / backend ESLint steps remain deferred. |
| F7 / F49          | undercounts: retry predicate in **8** files; a 2nd Express-5 break at `subsonic.ts:6667`.                                                                                          |
| F22               | omits a 4th regenerated key (`INTERNAL_API_SECRET`).                                                                                                                               |
| Status            | F3/F18/F24 are **partial**, not complete; F6 re-homed out of #14; severities recalibrated (F20/F23/F24/F35/F36/F47).                                                               |

## Epics

| Epic | Title                                                             | ✅ / 🟡 / Total |
| ---- | ----------------------------------------------------------------- | --------------- |
| #8   | JS toolchain: workspaces + pinned Node (pre-existing issue)       | 1 / 1 / 2       |
| #10  | CI security scanning & supply-chain guardrails (Wave 0)           | 1 / 1 / 3       |
| #11  | Secrets & credential storage hardening                            | 4 / 0 / 4       |
| #12  | Request-path auth & egress hardening                              | 6 / 1 / 8       |
| #13  | Background-job idempotency, retries & reconciler dedup            | 2 / 2 / 8       |
| #14  | Backend error-handling unification & route god-file decomposition | 0 / 2 / 6       |
| #15  | Type-safety ratchet (backend any + frontend strict + typed API)   | 0 / 1 / 2       |
| #16  | Frontend consolidation, decomposition & render performance        | 0 / 1 / 5       |
| #17  | Database & streaming performance                                  | 1 / 3 / 5       |
| #18  | Provider abstractions: acquisition, streaming & engine seams      | 0 / 0 / 4       |
| #19  | Framework & production-image modernization                        | 1 / 0 / 5       |
| —    | Standalone (no epic)                                              | 1 / 0 / 1       |

## By dimension

| Dimension          | Findings |
| ------------------ | -------- |
| dependencies-build | 9        |
| extensibility      | 6        |
| idempotency        | 9        |
| performance        | 7        |
| readability        | 11       |
| security           | 12       |

_(includes F54; dimension tallies double-count the F2/F44 and F17/F47 duplicate pairs.)_

## Index

| ID          | St  | Dimension          | Sev         | Eff | Risk   | Brk | Epic | Title                                                                                                |
| ----------- | --- | ------------------ | ----------- | --- | ------ | --- | ---- | ---------------------------------------------------------------------------------------------------- |
| [F1](#f1)   | 🟡  | readability        | high        | L   | medium |     | #14  | No asyncHandler wrapper: ~130+ hand-rolled try/catch handlers bypass the existing error…             |
| [F2](#f2)   | ✅  | readability        | high        | L   | medium |     | #14  | library.ts is an 8324-LOC route god-file: 36 handlers, 121 direct prisma calls, 66 `any…             |
| [F3](#f3)   | 🟡  | readability        | high        | L   | medium |     | #15  | 888+ `any` across strict:true backend silently defeat the type system, concentrated on …             |
| [F4](#f4)   | ⬜  | readability        | high        | L   | medium |     | #15  | frontend `type ApiData = any` erases types on ~90 return slots; strict:false + ES2017 h…             |
| [F5](#f5)   | ⬜  | readability        | high        | L   | medium |     | #16  | AudioPlaybackOrchestrator is a 7080-LOC headless god-component (97 refs, 24 effects) wh…             |
| [F6](#f6)   | ✅  | readability        | medium      | M   | low    |     | —    | Statistically-biased, non-transitive Array.sort() shuffle inlined 10x and a hand-copied…             |
| [F7](#f7)   | ⬜  | readability        | medium      | M   | low    |     | #14  | Prisma-retry-proxy + retryable-error infrastructure is copy-pasted near-verbatim across…             |
| [F8](#f8)   | ⬜  | readability        | medium      | M   | low    |     | #14  | Manual req.query parsing repeated ~21x with `as string`+parseInt fallbacks instead of t…             |
| [F9](#f9)   | ⬜  | readability        | medium      | M   | low    |     | #16  | Two parallel frontend systems each duplicated: dual toast renderers (sonner + custom) a…             |
| [F10](#f10) | ⬜  | readability        | medium      | M   | low    |     | #16  | Per-page god-components and duplicated cover-art widgets: VibePage (1247 LOC, 16 useSta…             |
| [F11](#f11) | 🟡  | readability        | medium      | M   | medium |     | #14  | Three near-identical auth resolvers duplicate the credential ladder and carry a permane…             |
| [F12](#f12) | 🟡  | performance        | high        | M   | medium |     | #16  | Player UI re-renders 4×/second during playback: useAudio() pipes the high-frequency cur…             |
| [F13](#f13) | 🟡  | performance        | high        | L   | medium |     | #17  | Per-track/per-candidate N+1 query loops dominate Spotify import and Discover Weekly hot…             |
| [F14](#f14) | ✅  | performance        | high        | M   | medium |     | #17  | pgvector ANN searches never set ivfflat.probes — every 'similar tracks' / vibe query sc…             |
| [F15](#f15) | 🟡  | performance        | medium      | M   | medium |     | #17  | ORDER BY RANDOM() full-table scans on Track / track_embeddings for radio, random tracks…             |
| [F16](#f16) | ⬜  | performance        | medium      | M   | medium |     | #17  | Genre filtering scans JSONB with jsonb_array_elements_text + LOWER(...) LIKE '%g%' inst…             |
| [F17](#f17) | ⬜  | performance        | medium      | M   | low    |     | #19  | API runtime transpiles TypeScript on every cold start via tsx and ships the full TS too…             |
| [F18](#f18) | 🟡  | performance        | medium      | M   | low    |     | #17  | Soulseek downloads buffer whole files into heap; cleanup uses sync fs and audio streami…             |
| [F19](#f19) | ✅  | idempotency        | high        | S   | low    |     | #13  | Discover processor swallows real failures (returns instead of throws), so Bull never re…             |
| [F20](#f20) | ⬜  | idempotency        | high→medium | M   | low    |     | #13  | Discover Weekly batch creation has no per-(user, week) idempotency guard — double-trigg…             |
| [F21](#f21) | ⬜  | idempotency        | high        | M   | medium |     | #13  | Audio-analysis / vibe re-queue enqueues BEFORE flipping status and has no per-track ded…             |
| [F22](#f22) | ✅  | idempotency        | high        | M   | low    |     | #11  | Helm Secret regenerates SESSION_SECRET / SETTINGS_ENCRYPTION_KEY / POSTGRES_PASSWORD on…             |
| [F23](#f23) | 🟡  | idempotency        | high→medium | M   | medium |     | #13  | Download dedup rides on a JSON-path findFirst plus a 5-strategy heuristic cascade inste…             |
| [F24](#f24) | 🟡  | idempotency        | high→medium | M   | low    |     | #13  | High-volume scan jobs enqueued from ~15 sites with no coalescing jobId, no attempts/bac…             |
| [F25](#f25) | ⬜  | idempotency        | medium      | M   | medium |     | #13  | Two overlapping download reconcilers (unlocked 30s queueCleaner interval vs claim-locke…             |
| [F26](#f26) | ✅  | idempotency        | medium      | S   | low    |     | #13  | Invite-code maxUses check is outside the transaction with an unconditional increment — …             |
| [F27](#f27) | ⬜  | idempotency        | medium      | M   | low    |     | #13  | TIDAL sidecar track/album downloads have no on-disk or in-flight idempotency — retries …             |
| [F28](#f28) | ✅  | security           | high        | M   | medium |     | #11  | API keys stored in plaintext and looked up by raw value — a read-only DB leak yields wo…             |
| [F29](#f29) | ✅  | security           | high        | M   | medium | ⚠️  | #11  | Settings encrypted with unauthenticated AES-256-CBC, a naive pad/truncate key, and a si…             |
| [F30](#f30) | ✅  | security           | high        | S   | low    |     | #12  | Internal CLAP-callback endpoints fail open and use non-constant-time secret comparison               |
| [F31](#f31) | ✅  | security           | high        | M   | medium | ⚠️  | #12  | FastAPI sidecars have zero inbound authentication and use unvalidated user_id as a file…             |
| [F32](#f32) | 🟡  | security           | high        | M   | low    |     | #12  | Lidarr webhook is unauthenticated and unthrottled by default and parses payload as unty…             |
| [F33](#f33) | ✅  | security           | high        | M   | medium |     | #12  | SSRF guard blocks only literal private hosts — DNS rebinding to an internal IP bypasses…             |
| [F34](#f34) | ✅  | security           | medium      | S   | low    |     | #12  | CORS allows every origin unconditionally despite an allowlist knob, while credentials:t…             |
| [F35](#f35) | ✅  | security           | medium→low  | S   | medium |     | #12  | Helmet ships without HSTS/CSP, session cookie 'secure' is opt-in via string env, and tr…             |
| [F36](#f36) | ✅  | security           | medium→low  | S   | low    |     | #11  | jwt.verify omits the algorithms pin and /refresh re-reads the secret inline as `as any`              |
| [F37](#f37) | ⬜  | security           | high        | M   | medium | ⚠️  | #12  | Long-lived full-privilege JWT embedded in stream/cover-art URLs (?token=) leaks via log…             |
| [F38](#f38) | ✅  | security           | medium      | M   | low    |     | #10  | ML model artifacts and Python deps are pulled at build time with no checksum/hash pinni…             |
| [F39](#f39) | ⬜  | extensibility      | high        | L   | medium |     | #18  | No shared MediaSource/DownloadProvider abstraction: each acquisition source is a bespok…             |
| [F40](#f40) | ⬜  | extensibility      | high        | L   | medium |     | #16  | Frontend api.ts is a 3693-LOC monolithic ApiClient (286 methods) hand-mirroring a backe…             |
| [F41](#f41) | ⬜  | extensibility      | medium      | M   | low    |     | #18  | AudioEngine interface declares ~13 optional methods, forcing runtime typeof-capability …             |
| [F42](#f42) | ⬜  | extensibility      | medium      | L   | medium |     | #18  | ~50-entry recommender registry is hand-maintained with one near-duplicate bespoke metho…             |
| [F43](#f43) | ⬜  | extensibility      | medium      | L   | medium |     | #18  | Per-external-source coupling: each media source is a parallel stack (route file + servi…             |
| [F44](#f44) | ✅  | extensibility      | medium      | L   | medium |     | #14  | library.ts (305KB) mixes HTTP, business logic, and 121 direct Prisma calls in one god-r… _(dup F2)_  |
| [F45](#f45) | 🟡  | dependencies-build | high        | M   | low    |     | #10  | CI has no security/vulnerability scanning and no dependency automation (Dependabot/Reno…             |
| [F46](#f46) | 🟡  | dependencies-build | medium      | S   | low    |     | #8   | No typecheck gate in CI; frontend stuck on strict:false / target ES2017 while backend i…             |
| [F47](#f47) | ⬜  | dependencies-build | high→medium | M   | low    |     | #19  | API runtime image ships uncompiled TypeScript and runs it via tsx in production with fu… _(dup F17)_ |
| [F48](#f48) | ⬜  | dependencies-build | medium      | L   | medium | ⚠️  | #19  | Bull 4 is maintenance-only with a version-mismatched @types/bull; migrate to BullMQ                  |
| [F49](#f49) | ✅  | dependencies-build | medium      | L   | medium | ⚠️  | #19  | Express 4 (Express 5 is GA) with half-migrated middleware majors                                     |
| [F50](#f50) | ✅  | dependencies-build | medium      | M   | low    |     | #10  | Python sidecar dependencies are floor-pinned with no lockfile or hashes — non-reproduci…             |
| [F51](#f51) | ✅  | dependencies-build | medium      | L   | medium |     | #19  | audio-analyzer pinned to EOL Ubuntu 20.04 / Python 3.8 / old TensorFlow                              |
| [F52](#f52) | ⬜  | dependencies-build | medium      | L   | medium |     | #19  | AIO image is a single-stage, root-supervisord monolith with build-toolchain bloat and r…             |
| [F53](#f53) | ✅  | dependencies-build | medium      | S   | low    |     | #8   | Node runtime version unpinned and drifts across CI (frontend on Node 24, everything els…             |
| [F54](#f54) | ✅  | security           | high        | S   | low    |     | #12  | POST /device-link/verify is an unauthenticated TOCTOU that mints full-privilege API keys             |

## Findings

### F1 — No asyncHandler wrapper: ~130+ hand-rolled try/catch handlers bypass the existing errorHandler/AppError and duplicate the same logger.error+sendInternalRouteError boilerplate

**🟡 partial** · dimension: readability · severity: high · effort: L · risk: medium · epic: #14

> **Audit note.** ✓ `ErrorCode` enum is 15-member (not 18); `errorHandler` is registered at index.ts:385 (not 402). Core thesis (asyncHandler 0×, three parallel error paths) holds.
>
> **Fix shipped (partial — infra + 31 library.ts tails)** (PR #123). Recommendation items (a)+(b) landed as infrastructure with zero behavior change for existing callers: `AppError` gained an optional trailing `httpStatus` param (explicit status wins over the category map — `errorHandler` can now express 401/404/409 etc.; all 14 existing 3/4-arg call sites untouched), `ErrorCode` gained a generic `INTERNAL`, `middleware/asyncHandler.ts` exists (`Promise.resolve(fn(req,res,next)).catch(next)`, generic over express-4 handler type params), and the dead third error path `utils/errors.safeError` (0 production callers) is deleted. Migration tranche 1: all 33 `sendInternalRouteError` tails in `library.ts` were two-pass classified — (a) catch-body purity, (b) try-block AppError-throw reachability — and the 31 PURE tails now delegate via `asyncHandler`; the 2 MIXED tails (`/tracks/:id/stream`, `/tracks/:id/audio-info`) are **deliberately left hand-rolled** because their try blocks reach `audioStreaming.getStreamFilePath`, whose `AppError`s (`TRANSCODE_FAILED`/`DB_QUERY_ERROR` = RECOVERABLE) would be re-mapped 500→400 by `errorHandler` and would expose raw (prod-unredacted) messages — do not migrate them without first deciding those AppErrors' route-level semantics. Log context: the migrated routes' per-site catch strings ("Get tracks error:" etc.) are gone, replaced in the same change by `errorHandler`'s unhandled-error log line carrying `req.method` + `req.path` — route identification in logs is preserved, not genericized. Prod 500 bodies on migrated routes changed from route-specific `{"error":"Failed to …"}` to the errorHandler shape (CHANGELOG'd). Still open under #14: `subsonic.ts` (its `sendSubsonicError` protocol layer needs its own design — Subsonic errors ship as HTTP 200), the remaining `routeErrorResponse.ts` importers (`downloads.ts` 11, `discover.ts` 9, `systemSettings.ts` 2 `sendInternalRouteError` tails), other route files' hand-rolled tails, and the eventual Express-5 upgrade (F49) that retires the wrapper. Tests: `asyncHandler.test.ts` + extended `errorHandler.test.ts` (new infra pins incl. explicit-status precedence and the method+path log line); 27 migration-proof status-500 pins + 6 new uncovered-tail behavior tests across `libraryRuntime` / `libraryCoverArtCompat` / `libraryRadioVibeReliabilityCompat` / `libraryBranchCoverage` / `libraryRemotePreference`.

**Files:** `backend/src/routes/library.ts`, `backend/src/routes/subsonic.ts`, `backend/src/middleware/errorHandler.ts`, `backend/src/routes/routeErrorResponse.ts`

**Problem.** A capable global errorHandler exists (middleware/errorHandler.ts maps AppError+ErrorCategory to HTTP, hides stack traces in prod) but routes never delegate to it: grep shows 0 asyncHandler imports anywhere in backend/src. Express 4 does not forward rejected promises to error middleware, so every async handler hand-rolls try/catch — 73 catch blocks in library.ts alone — and 34 of them end in the identical logger.error(...)+sendInternalRouteError(res, 'Failed to ...') pattern (sendInternalRouteError appears 34x in library.ts). routeErrorResponse.ts is a parallel, ad-hoc error path that reimplements status mapping the errorHandler already owns, so error policy (status codes, prod redaction, logging) is scattered and inconsistent across hundreds of call sites.

**Recommendation.** Confirmed core problem: asyncHandler appears 0x in backend/src; a capable global errorHandler exists and is registered at index.ts:402 after all routes; Express is ^4.18.2 (no async-rejection auto-forward); and routes never delegate — `new AppError` and `next(err)`/`next(error)` each appear 0x in any route file. sendInternalRouteError appears 34x in library.ts. There are actually THREE parallel ad-hoc error paths, not two: errorHandler/AppError, routeErrorResponse.ts (sendInternalRouteError/sendRouteError), AND utils/errors.ts safeError (which has 0 production callers) — reinforcing 'scattered/inconsistent policy.' Tighten the plan: (a) FIRST extend errorHandler's category->status map (or add an explicit statusCode/httpStatus field to AppError) to cover 401/403/404/409 before migrating any non-500 throws; (b) add a generic ErrorCode (e.g., INTERNAL/UNKNOWN) or relax AppError's constructor so route 500s have a natural code; (c) scope the migration to genuine route-handler error tails (~the 33 sendInternalRouteError sites in library.ts), explicitly excluding inner swallow-and-continue/fallback catches (catch(cacheError), boolean-returning helpers); (d) note and verify the prod response-body change (specific 'Failed to ...' -> generic 'Internal server error') against contract/integration tests; (e) keep the smallest-safe-change, file-by-file sequencing and the Express 4->5 follow-up to eventually drop the wrapper. Net: high-value, real readability/consistency win — adopt with the above guardrails rather than as a blanket throw-and-delete sweep.

**Safety / pitfalls.** Several real pitfalls the proposal understates. (1) Status-code expressiveness: the existing errorHandler (middleware/errorHandler.ts:14-27) only maps ErrorCategory to 400 (RECOVERABLE), 503 (TRANSIENT), 500 (FATAL). It cannot express 401/403/404/409. library.ts already returns 401 and 403 in catch/guard paths, so 'throw new AppError' only safely round-trips the 500 case unless errorHandler's mapping is extended first. (2) ErrorCode mismatch: ErrorCode (utils/errors.ts) is a fixed 18-member enum scoped to config/fs/transcode/metadata/db with NO generic route code; AppError's constructor requires one, so funneling arbitrary route 500s through 'new AppError(code,...)' needs a new generic code or a loosened constructor. (3) The '73 catch blocks in library.ts' are NOT all deletable handler tails: many are inner helper try/catch (e.g., catch (cacheError) at lines 872/902, and the resize helper at 919 that returns false) that intentionally swallow-and-continue or drive boolean fallback control flow. asyncHandler only covers top-level handlers; a blanket 'delete the try/catch' would break fallback logic. Deletable boilerplate is ~33 (the sendInternalRouteError sites), not 73. (4) Response-shape change: current 500 tails return a specific {error:'Failed to ...'} message; routing through errorHandler in production returns generic {error:'Internal server error'} — a client/test-visible behavior change to verify against any contract tests.

---

### F2 — library.ts is an 8324-LOC route god-file: 36 handlers, 121 direct prisma calls, 66 `any`, no service layer, 1100-line helper preamble

**✅ complete (#124)** · dimension: readability · severity: high · effort: L · risk: medium · epic: #14

> **Audit note.** ✓ Duplicate of F44 (same `library.ts` god-file). The `any` count is ~64–70 depending on the matcher — treat as approximate, not the exact 66/70 in the title/body. The 8324-LOC anchor is exact.
>
> **Fix shipped (#124).** Stage A moved the shared helper/cache preamble into typed `utils/library*` modules and `services/libraryMaintenance.ts`, including Prisma-typed sort-map values. Stage B exported all 36 handlers by name and split registration across per-resource modules under `routes/library/`, re-aggregated by `routes/library/index.ts` behind the unchanged `routes/library.ts` default-export facade. The `/api/library` mount, route order, middleware chains, async-handler vs hand-rolled tails, and 14 current `$queryRaw` occurrences are unchanged. A typed LibraryService/Prisma-access layer remains the explicitly separate follow-up described by the audit correction, not unfinished F2 scope.

**Files:** `backend/src/routes/library.ts`, `backend/src/routes/library/*.ts`, `backend/src/utils/library*.ts`, `backend/src/services/libraryMaintenance.ts`

**Problem.** library.ts mixes HTTP, data-access, caching and business logic in one 8324-line file: 121 direct prisma._ calls (no controller/service split), 66 `any` occurrences, untyped `let where: any = {}` query builders (e.g. the dual fast-path/fallback artist filter at 1811-1859), and 27 module-level helper functions + caches (audioInfoCache, nativeCoverHealInFlight, sort maps) packed into the first 1190 lines before the first route. Sort maps are typed `Record<string, any>` (lines 99-114), defeating Prisma orderBy type-safety. Routes group cleanly by prefix (/artists, /albums, /tracks, /genres, cover-art, scan) but live in one file, making the module impossible to navigate, review, or test without spinning up HTTP. By contrast subsonic.ts (same era) externalizes its helpers into utils/subsonic_ and defines named handleX handlers — the in-repo model library.ts should follow.

**Recommendation.** Confirmed: library.ts is 8324 LOC, 36 inline anonymous handlers, ~113-121 direct prisma calls, 70 `any` (43 `: any`, 20 `as any`), three `Record<string, any>` sort maps (lines 99-114), and `let where: any = {}` query builders at 1811, 3145, 3408. First route is at line 1190, confirming the ~1190-line helper preamble (~30 module-level helpers/caches). No LibraryService exists. Recommendation stands, with two corrections to the rationale: (a) The subsonic.ts comparison is the right in-repo model but for the wrong reason — its win is 54 named exported `handleX` handlers wired via `router.get(alias, handleX)` plus externalized `utils/subsonic{Ids,Indexes,Media,Response}.ts`; it is NOT a service-layer exemplar (it still has 91 of its own direct prisma calls and only 3 AudioStreamingService usages). So scope F2 first as: extract each handler into a named, individually-testable exported function and move shared helpers/caches into `utils/` modules — this alone kills the "can't test without HTTP" problem and is a smaller, lower-risk change than introducing a full LibraryService. A typed LibraryService for Prisma access is a worthwhile follow-up but is a larger refactor and not what the cited exemplar actually demonstrates. (b) The sort-map fix is valid and the suggested types are correct: `Prisma.ArtistOrderByWithRelationInput`, `Prisma.AlbumOrderByWithRelationInput`, `Prisma.TrackOrderByWithRelationInput` all exist in the generated client. Type as `Record<string, Prisma.ArtistOrderByWithRelationInput>` (the literal-key narrowing is cosmetic; the real fix is replacing `any` value with the Prisma input type so a typo'd orderBy key is caught — `as const` alone does not catch it). Execute incrementally per resource (one PR each), re-aggregating sub-routers via a `routes/library/index.ts` that preserves registration order, and keep CHANGELOG.md updated per contract.

**Safety / pitfalls.** A naive resource-prefix split can break behavior in two concrete ways. (1) Route-ordering: `/tracks/shuffle` (line 3956) MUST be registered before `/tracks/:id` (line 5776), or Express matches "shuffle" as an :id; the split must re-aggregate sub-routers into a single index router that preserves intra-resource registration order. (2) Prefix mis-grouping: `/artist-counts/*` (1980/2022) and `/image-backfill/*` (2068/2110) carry the "artist" prefix but are NOT part of the /artists resource — splitting by literal prefix would misfile them. Also: library.ts contains 6 `$queryRaw` calls (lines 752, 3994, 6508, 6719, 6754, 6802) which violate the AGENTS.md "no raw SQL" contract; these are pre-existing and should be moved verbatim (not rewritten) during a readability-only split to avoid behavior change, and flagged separately. Shared helpers like buildMultiTrackRadio (566) and audioInfoCache (147) are used across radio/tracks routes, so they must go to shared utils, not a single resource file. The router is a single default export mounted at /api/library (index.ts:234), so the public API surface must be reconstructed exactly.

---

### F3 — 888+ `any` across strict:true backend silently defeat the type system, concentrated on the idempotency-critical metadata in simpleDownloadManager

**🟡 partial (PR #21)** · dimension: readability · severity: high · effort: L · risk: medium · epic: #15

> **Audit note.** ✓ Originally only step 1 of 4 shipped (the line-433 cast).
>
> **Update** (`feat/f3-second-cast`). The SECOND identical cast at `simpleDownloadManager.ts:1530` (`(job as any).lidarrAlbumId`) is now removed too — `job` is a `DownloadJob` from `findMany` (no select) and `lidarrAlbumId` is a typed `Int?` column, so the cast was gratuitous (tsc clean; 88/88 simpleDownloadManager tests pass). Both gratuitous casts are gone. Still 🟡: the `DownloadJobMetadata` interface / `parseMetadata`, the discoverWeekly typing, and the backend ESLint gate are deferred — typing the heterogeneous `Json?` metadata and rewiring `resolveDownloadJobMetadata`'s 3 callers is a regression-prone change better shipped as its own typed-metadata PR under #15.

**Files:** `backend/src/services/simpleDownloadManager.ts`, `backend/src/services/discoverWeekly.ts`, `backend/src/routes/library.ts`

**Problem.** Despite tsconfig strict:true, there are 899 `any` occurrences in non-test backend src — escape hatches that turn strict mode off locally. The worst case is simpleDownloadManager.ts (50 `any`): job.metadata is repeatedly cast `(job.metadata as any)` and ad-hoc keys read (meta?.lidarrMbid, existingMetadata as any), and at line 433 a REAL typed column is cast away — `(j as any).lidarrAlbumId` — on the exact data that drives download dedup/matching. A typo in a metadata key is therefore undetectable at compile time on the most correctness-sensitive path. discoverWeekly.ts (53 `any`: similarCache: Map<string, any[]>, getArtistMatchScore(artist: any)) leaves the whole recommender pipeline untyped.

**Recommendation.** Confirmed against code. Counts verified: strict:true (backend/tsconfig.json:8), 905 `any` in non-test backend src, simpleDownloadManager.ts=50, discoverWeekly.ts=53, library.ts=67 (top offender). Every specific claim checks out: `(existingJob?.metadata as any)` (182,228), `meta?.lidarrMbid` (423), and crucially line 433 `(j as any).lidarrAlbumId` casts away a REAL typed Prisma column. The strongest evidence the team should lead with: the SAME column is used correctly typed at line 658 (`j.lidarrAlbumId === lidarrAlbumId`) and written as a real column at lines 193/1109 — so the line-433 cast is gratuitous and self-contradictory within one file. discoverWeekly claims verified verbatim: `similarCache: Map<string, any[]>` (2361,2907), `getArtistMatchScore(artist: any)` (2546,2951), plus `allSimilarArtists: any[]`, `tier: any[]`, `allTracks/libraryAnchors: any[]` leaving the recommender pipeline untyped end-to-end. Sequencing: (1) Drop the line-433 cast immediately — zero-risk, no helper needed. (2) Add a `DownloadJobMetadata` interface with ALL keys optional (lidarrMbid?, lidarrAlbumId?, lidarrAttempts?, artistName?, albumTitle?, downloadType?, statusText?, batchId?, spotifyImportJobId?, previousDownloadIds?, artistMbid?, tier?, similarity?) and a `parseMetadata(job): DownloadJobMetadata` that mirrors today's null-tolerant `(... as any) || {}` semantics — this is the right fix because Prisma types `metadata` as `Json?`/`JsonValue` which genuinely isn't indexable, so SOME narrowing is required regardless. (3) Thread SimilarArtist/PendingTrack types through discoverWeekly. (4) The ratchet is worthwhile but correct the framing: it's net-new backend ESLint config (none exists today), so scope it as "stand up backend eslint with no-explicit-any as warn + a per-file allowlist baselined at current counts" rather than warn->error flip. Treat the per-key typo risk as the headline business case: lidarrAlbumId is read via string key 23 times across the dedup/retry path with no compile-time protection.

**Safety / pitfalls.** The line-433 fix is provably zero-risk: `activeJobs` comes from `tx.downloadJob.findMany()`, so each `j` is a fully-typed `DownloadJob` with `lidarrAlbumId: number | null` (schema.prisma:658). The same field is already used UN-cast at line 658 (`j.lidarrAlbumId === lidarrAlbumId`), so dropping `(j as any)` at 433 keeps identical runtime behavior and only restores type-checking. The one real caution is the `parseMetadata` helper: `metadata` is Prisma `Json?` (typed `JsonValue`), populated by historical/unvalidated writes, so the typed interface MUST be all-optional (Partial-style) and the helper must tolerate null/missing/oddly-shaped keys exactly like today's `(... as any) || {}` + `?.` reads. If the interface marks any key required or the helper throws on malformed JSON, it would silently break dedup/retry matching on old rows — a behavior change, not just a typing change. No AGENTS.md contract is violated (no raw SQL, no API-boundary or config changes). Note: backend has NO eslint config at all (only frontend/eslint.config.mjs exists), so the proposed ratchet is net-new backend lint infrastructure, not a one-line warn->error flip — slightly larger scope than implied, but still safe and additive.

---

### F4 — frontend `type ApiData = any` erases types on ~90 return slots; strict:false + ES2017 hold the frontend to a lower bar than the strict backend

**⬜ open** · dimension: readability · severity: high · effort: L · risk: medium · epic: #15

> **Audit note.** ✓ No `request<any>` calls exist (residual is 3 `Promise<any>` + 3 `any[]`). Overlaps F40.

**Files:** `frontend/lib/api.ts`, `frontend/tsconfig.json`

**Problem.** frontend/tsconfig.json sets strict:false (strictNullChecks/noImplicitAny off) and target ES2017, so the two halves of the monorepo hold different safety bars. lib/api.ts then defines `type ApiData = any` (line 376) with a file-wide eslint-disable, used in 91 places (e.g. tracks: ApiData[], request<{artists: ApiData[]}>) plus 3 request<any> calls. Hand-maintained domain types (Track/Album/Artist in features/library/types.ts) exist but are completely disconnected from these returns, so every consumer of getAlbums/getRecommendations/etc. receives `any` and loses all compile-time safety. With strictNullChecks off, the optional-field/null bugs the data layer is full of pass through silently.

**Recommendation.** Core thesis is real and worth fixing: tsconfig.json has strict:false + target ES2017 (vs strict backend), and lib/api.ts line 376 defines `type ApiData = any` used in 90 return slots (getAlbums/getArtists/getRecommendations/getPlaylists/settings/systemSettings/...), erasing compile-time safety; features/library/types.ts (Track/Album/Artist) exists but is never imported here, so consumers receive any. Corrections to the finding before acting: (a) it's a single eslint-disable-next-line (line 375), not a file-wide suppression — and the inline comment shows it was a deliberate trade-off, so frame this as revisiting that decision, not removing an accident; (b) there are no `request<any>` calls — the residual any is 3 `Promise<any>` (getYtMusicAlbum/Artist/Song, 3171-3179) and 3 `any[]` fields (3425-3427), plus the small 13 explicit-any elsewhere in the frontend, so the problem is genuinely concentrated in api.ts as claimed; (c) the OpenAPI-codegen 'ideal' path is overstated: backend/src/config/swagger.ts defines only 6 component schemas, routes/library.ts uses `$ref` zero times, and response bodies are stub `type: object` blocks (e.g. progress/result as opaque objects) — openapi-typescript would emit mostly Record<string,unknown> for these endpoints. Revised plan: (1) do NOT lead with codegen; first import/extend the existing features/library/types for the well-understood library endpoints, and use `unknown` + narrowing guards for the rest — codegen only becomes worthwhile after the backend OpenAPI response schemas are enriched (a separate, larger effort). (2) Stage strict: flip strictNullChecks first and FIX the ~107 real errors per-directory (don't suppress); note 4 of the dry-run errors are TS5097 .mts import-extension config issues unrelated to null-safety, address separately. Then noImplicitAny, then full strict + no-explicit-any as error, guarded by a CI type-error budget. Bumping target ES2017→ES2020 to match backend is low-risk and fine. Net: high-value readability/safety work, but sequence it types-then-strict and treat codegen as a downstream goal contingent on spec enrichment, not a quick win.

**Safety / pitfalls.** The change is type-level only with no runtime behavior change, so it's low-risk by default. Two real concerns: (1) Flipping strictNullChecks is not free — a dry-run (tsc --noEmit --strictNullChecks) surfaced ~111 errors, of which ~107 are genuine null/undefined issues (TS18047/18048/18049 + TS2322/2345/2769); these are real latent bugs that must be FIXED, not suppressed, or the flip just relocates the problem to @ts-ignore. (2) The proposal's preferred path — replacing ApiData with domain types — risks asserting shapes that don't match reality. The hand-written frontend types (features/library/types.ts: Album has coverArt/year/nested artist) DIFFER from the backend OpenAPI schemas (swagger.ts Album has rgMbid/artistId/coverUrl/primaryType) and from actual route payloads. Casting any-returns to either without a runtime narrowing guard would lie to the compiler. unknown + guards (which the finding correctly endorses for dynamic shapes) is the safe form; broad `as DomainType` is not. No AGENTS.md contract violation: api.ts remains the API boundary and no raw-SQL/config rules are touched.

---

### F5 — AudioPlaybackOrchestrator is a 7080-LOC headless god-component (97 refs, 24 effects) whose core engine-binding effect has a ~40-entry dep array that re-binds all handlers on unrelated state changes

**⬜ open** · dimension: readability · severity: high · effort: L · risk: medium · epic: #16

**Files:** `frontend/components/player/AudioPlaybackOrchestrator.tsx`

**Problem.** The orchestrator returns null and exists only to wire engine events to context state, yet it concentrates the entire playback lifecycle into 97 useRefs, 24 useEffects and 43 useCallbacks in one 7080-line closure. No single concern (segmented startup recovery, handoff/circuit breakers, prewarm validation, foreground recovery, progress persistence) can be read or tested in isolation. The central effect that registers all 9 engine handlers (ends at line 4993) has a ~40-entry dependency array (lines 4994-5034) including isPlaying, queue, currentIndex, shuffleIndices and dozens of callbacks, so React tears down and re-attaches every engine listener whenever any of these change — a rebind-race readability/correctness hazard. The dep array is simultaneously huge AND partly redundant because most live state is already mirrored into \*Ref values.

**Recommendation.** Core claims verified: file is 7080 LOC (exact), 24 useEffects + 43 useCallbacks (exact), ~92 useRefs (finding's "97" is a slight overcount from grepping all lines containing 'useRef'). The central engine-binding effect spans lines 4091-4993 and registers all 9 handlers; its dep array (lines 4995-5033) has exactly 39 entries including isPlaying/queue/currentIndex/shuffleIndices/isShuffle/repeatMode — matching "~40". The rebind hazard is real AND already partially worked-around in-code: the effect captures effectLoadId=loadIdRef.current and rejects stale handler firings via effectLoadId !== loadIdRef.current, with an explicit comment (lines 4104-4108) describing the "React re-registers this handler before load() replaces the source" race. The team's pure \*Policy.ts modules (lib/audio-engine/recoveryPolicy.ts, foregroundRecoveryPolicy.ts, segmentedStartupPolicy.ts, nextTrackPreloadPolicy.ts, segmentedRepresentationPolicy.ts, segmentedPlaybackRegressionPolicy.ts + components/player/audioPlaybackOrchestratorPolicy.ts), each with unit tests, do prove the pure-logic extraction pattern. CORRECT the finding: do not claim the ref-mirror pattern is "~80% in place" — it covers only currentTrack and playbackType. The right sequencing is: (1) extract the proposed cohesive hooks (useEngineEventBridge, useSegmentedStartupRecovery, useSegmentedHandoff, useProgressPersistence, useForegroundRecovery, usePrewarmValidation, useTransientErrorRecovery) one at a time behind the existing component tests; (2) WITHIN the event-bridge hook, FIRST add the missing ref mirrors (queueRef, currentIndexRef, shuffleIndicesRef, isShuffleRef, repeatModeRef, currentAudiobookRef, currentPodcastRef) with dedicated sync effects mirroring the existing currentTrackRef pattern at line 3192; (3) only then migrate handlers to read those refs and shrink the dep array toward []. Treat the []-deps goal as the end state of an incremental TDD migration, not a single rewrite. The estimated ~150-line composition root is plausible but should be validated as an output, not promised up front.

**Safety / pitfalls.** The proposed "bind once with deps [], read live state exclusively through existing \*Ref mirrors" is NOT a low-risk reuse of existing infra as the finding implies. Only currentTrack (line 688) and playbackType (line 694) are actually mirrored to refs. The critical handleEnd handler (lines 4234-4450) reads queue, currentIndex, shuffleIndices, isShuffle, repeatMode, currentAudiobook, currentPodcast DIRECTLY from closure (e.g. resolveQueueAdvance/getNextTrackInfo at lines 4305-4337) — none of these have ref mirrors today. Collapsing the dep array to [] therefore REQUIRES first introducing ~7 new ref-mirror sync effects; if any mirror is missed or lags a render, queue advancement, shuffle order, and repeat-one would silently use stale values (skip-to-wrong-track, autoplay regressions) with no type error to catch it. This is high-blast-radius playback logic with heavy but behavior-pinned test coverage (audioPlaybackOrchestrator.component.test.ts is 1946 lines). The refactor must be done in TDD increments per-hook (contract says smallest-safe-change + TDD), not as a single 7080->150 line rewrite, and each extracted hook's ref mirroring must be asserted before deleting the corresponding dep. No AGENTS.md contract violation in the approach itself (stays in frontend, no new API/DB boundaries).

---

### F6 — Statistically-biased, non-transitive Array.sort() shuffle inlined 10x and a hand-copied LCG repeated 22x in programmaticPlaylists, ignoring the correct seededShuffle already in the file

**✅ complete (PR #21)** · dimension: readability · severity: medium · effort: M · risk: low · epic: —

> **Test backfill** (`feat/wave1-tdd-backfill`). `seededShuffle` is now exported and unit-tested in `programmaticPlaylistsBehavior.test.ts`: determinism (same seed → same order), permutation, **non-mutation** (the old in-place `tracks.sort()` would fail this), and a uniformity-spread check. Closes the audit §3.1 "no test added" gap.

**Files:** `backend/src/services/programmaticPlaylists.ts`

**Problem.** At least 10 mix generators shuffle via `tracks.sort(() => { random = (random*9301+49297)%233280; return random/233280 - 0.5; })` (e.g. line 1964, 2067, 2327, 3834). Passing a comparator that ignores its arguments and returns a stateful value is a non-transitive comparator — V8 sort is implementation-defined for such comparators, the result is a biased (non-uniform) permutation, AND .sort() mutates the cached `tracks` array in place. The `(x*9301+49297)%233280` LCG literal is hand-copied 22 times. Crucially, a correct Fisher-Yates seededShuffle(items, seedKey) ALREADY EXISTS at line 160 and is simply not used by these generators, alongside a separate getSeededRandom 32-bit hash (142) and createSeededRng LCG (152) — three RNG implementations coexist.

**Recommendation.** Confirmed core defect: exactly 10 inline shuffles `tracks.sort(() => { random = (random*9301+49297)%233280; return random/233280 - 0.5; })` exist at lines 1964, 2066, 2178, 2326, 2409, 2490, 2572, 2623, 2731, 3834. Passing a comparator that ignores its arguments and returns a stateful value is non-transitive; V8 TimSort is implementation-defined for such comparators and yields a biased, non-uniform permutation. This is genuinely present and worth fixing. The correct Fisher-Yates seededShuffle DOES already exist (line 160) and IS already used at lines 369/433/1038, so the fix is to route the 10 stragglers through it — a high-value consistency cleanup. CORRECTIONS to the finding: (1) "mutates the cached tracks array" is overstated — in the inspected generators `tracks` is a fresh local array built from a Prisma findMany (e.g. line 1912-1924), discarded after slicing; the in-place mutation is benign, the real bug is the biased permutation, not aliasing. (2) Do NOT remove randomSample / Math.random — it backs the intentional forceRandom refresh path covered by a determinism test; only the 10 seeded inline shuffles should be replaced. (3) Do NOT add the seedrandom package; reuse the existing createSeededRng/seededShuffle. (4) The "22 hand-copied" figure counts `233280` occurrences (2 per shuffle); the actual LCG step appears on 12 lines / ~11 distinct sites. Tightened scope: replace ONLY the 10 inline `*9301+49297` sort comparators with seededShuffle(items, `<mix>-${today}`), being careful at the line-3834 loop site to vary the seedKey per iteration (e.g. include the key in the seedKey) so loop iterations aren't correlated. Each non-loop call-site is individually shippable and covered by existing tests in programmaticPlaylistsPriorityMixes/CuratedMixes/Behavior tests. Leave getSeededRandom + createSeededRng + seededShuffle as the single seeded stack and leave randomSample for the explicit forceRandom paths.

**Safety / pitfalls.** The proposal's "Remove any Math.random-based randomSample so non-determinism can't leak back in" is dangerous and must NOT be done. randomSample (def line 132) is used at 26 call sites, and there is an intentional forceRandom mode (generateAllMixes, line 459; seedString line 463-465 uses Date.now()+Math.random()) powering the user "refresh mixes" feature where non-determinism is DESIRED. A behavior test (backend/src/services/**tests**/programmaticPlaylistsBehavior.test.ts:297 "generateAllMixes forceRandom mode is deterministic for a fixed seed input") asserts that contract by mocking Math.random (jest.spyOn(Math,"random")). Deleting randomSample would break this test and the refresh feature. Second concern: the key-journey site at line 3834 runs the inline shuffle INSIDE a loop sharing a single seedVal accumulator across iterations; naively swapping in seededShuffle(keyTracks, seedKey) would re-seed identically each loop iteration (correlated output) unless the seedKey is varied per iteration — so this one is NOT a mechanical 1:1 replacement. Third: adding the seedrandom npm package conflicts with smallest-safe-change; the repo already has a working seeded LCG (createSeededRng, line 152) and it is NOT installed — no new dependency is warranted.

---

### F7 — Prisma-retry-proxy + retryable-error infrastructure is copy-pasted near-verbatim across discoverWeekly and spotifyImport (and the worker Redis claim-lock is duplicated 4x)

**⬜ open** · dimension: readability · severity: medium · effort: M · risk: low · epic: #14

> **Audit note.** ✓ Undercount: the retry predicate is duplicated in 8 files (not 7) — `jobs/queueCleaner.ts` is the 8th. Strengthens the finding.

**Files:** `backend/src/services/discoverWeekly.ts`, `backend/src/services/spotifyImport.ts`, `backend/src/workers/index.ts`, `backend/src/workers/moodBucketWorker.ts`, `backend/src/workers/unifiedEnrichment.ts`

**Problem.** isRetryable\*PrismaError, withXPrismaRetry, and the two-level createPrismaRetryProxy are duplicated almost identically in discoverWeekly.ts (39-135) and spotifyImport.ts (36-169) — same P-code list, same Proxy structure, same `value.apply(target, args)` with `unknown[]` that strips all Prisma type-safety. The retryable-error string list will inevitably drift between copies. Separately, the SET-NX + Lua compare-and-delete claim lock and withRedisRetry/recreate-client helpers are reimplemented in 4 worker files (index.ts, moodBucketWorker.ts, unifiedEnrichment.ts, discoverProcessor.ts) with subtly different retry counts and TTLs, so a fix must be applied 4-5 times.

**Recommendation.** Confirmed and actually broader than stated. Corrections: (1) discoverProcessor is at backend/src/workers/processors/discoverProcessor.ts (finding's path workers/discoverProcessor.ts is wrong but the file/lock exist). (2) The full two-level createPrismaRetryProxy is verbatim-identical in only the 2 services (discoverWeekly.ts:91-135, spotifyImport.ts:88-131) — line refs accurate. (3) But the isRetryable*PrismaError predicate with the IDENTICAL P-code list ["P1001","P1002","P1017","P2024","P2037"] and the matching withXPrismaRetry loop (250ms*attempt backoff + $connect().catch) is duplicated across 6 files (discoverWeekly, spotifyImport, workers/dataIntegrity.ts, services/podcastDownload.ts, routes/podcasts.ts, workers/unifiedEnrichment.ts) plus a 7th class-method copy in services/moodBucketService.ts (isRetryablePrismaError/withPrismaRetry). (4) The SET-NX + identical Lua compare-and-delete lock + with*RedisRetry/recreate*RedisClient is in exactly 4 worker files (index.ts, moodBucketWorker.ts, unifiedEnrichment.ts, processors/discoverProcessor.ts), all built on createIORedisClient (utils/ioredis.ts). Recommend: (a) Extract one isRetryablePrismaError + withPrismaRetry(opts) into utils/prismaRetry.ts and replace all 7 copies — highest value, lowest risk, kills the drift surface. (b) Keep createPrismaRetryProxy as a typed wrapper there too; prefer the $extends approach ONLY if you additionally wrap $transaction/$connect, otherwise document the narrowed scope. (c) Extract a parameterized DistributedLock(key, {ttlMs, retryAttempts}) with acquire->token / Lua release / runWithClaim into utils, returning acquired/skipped so each of the 4 workers keeps its bespoke skip-logging and index.ts's observability counters. Severity is fair at readability/medium; treat as 3 independent small PRs (predicate+withRetry, proxy, lock) rather than one, and pair with the existing TDD expectation.

**Safety / pitfalls.** The "implement retry as a Prisma Client Extension ($extends query-level retry)" sub-proposal is NOT strictly behavior-preserving. The current two-level Proxy wraps BOTH top-level client methods ($transaction, $connect, $queryRaw) and model.operation methods. A $extends({ query }) extension only intercepts model + $queryRaw operations; it does NOT wrap $transaction callbacks or $connect. So the existing code retries a whole $transaction as one unit, while a pure $extends version would silently stop retrying transactions — a real behavior change. Any extraction must keep a thin client-level wrapper (or explicitly accept narrowed scope). For the lock: the 4 sites have genuinely divergent semantics that must be preserved — index.ts checks the SET-NX result as falsy AND maintains elaborate schedulerClaimCounters/observability + a multi-attempt (=3) redis retry loop, whereas discoverProcessor checks `result !== "OK"`, and moodBucket/enrichment retry only ONCE. TTLs differ on purpose (discover 45min, enrichment 15min, mood 2min) and are env-overridable. A shared DistributedLock is safe ONLY if it is parameterized over ttl/retry-attempts and returns the acquired/skipped outcome so each caller keeps its own skip-logging and counters; a fixed-policy helper would change behavior. Note: the Lua eval is raw Redis Lua, not SQL, so it does NOT violate the AGENTS.md "no raw SQL" contract (line 30) — consolidating it is contract-neutral.

---

### F8 — Manual req.query parsing repeated ~21x with `as string`+parseInt fallbacks instead of the project's adopted zod standard

**⬜ open** · dimension: readability · severity: medium · effort: M · risk: low · epic: #14

> **Audit note.** Per audit: `parseInt(x as string)||fallback` occurs ~7× (not ~21); the in-repo `req.query` zod precedent is `youtube.ts` (streaming.ts validates the body).

**Files:** `backend/src/routes/library.ts`

**Problem.** library.ts validates input by hand: it destructures req.query with string defaults and `parseInt(x as string, 10) || fallback` is repeated 21 times (e.g. /artists at 1785-1800), uses `as string` casts on query values, and silently coerces bad input (NaN -> fallback) instead of returning 400s. This contradicts the in-repo standard already adopted in 18/46 route files — auth.ts (loginSchema/registerSchema) and streaming.ts (createSessionSchema with z.coerce) show idiomatic zod with .refine/.regex/enums/bounded numbers. The hand-parsing is a major contributor to library.ts's 66 `any` and provides no validation guarantees.

**Recommendation.** Adopt the zod standard in library.ts — the problem is real (no zod import; parseInt(x as string,10)||fallback at lines 1795/1798/3138/3141/3393/3396/3960; bare parseInt at 1422/1644 yielding NaN; 29 `as string` casts; 68 `any`; sort maps typed Record<string,any> at lines 99/105/111 indexed via `sortBy as string`). Correct two inaccuracies in the finding before acting: (1) the "~21x parseInt fallbacks" count is INFLATED — the exact pattern occurs 7x (8 counting all parseInt||); ~22 req.query sites total are a mix of pagination/booleans/cursors/sizes, not 21 parseInt fallbacks. (2) The cited template is wrong: streaming.ts validates req.BODY not query, and z.coerce lives in playlists.ts (body), not streaming.ts. The ONLY existing req.query zod precedent is youtube.ts (infoQuerySchema.parse(req.query)) — cite that as the in-repo template instead. Implementation: define a shared paginationQuery schema (z.coerce.number().int().min(1).max(MAX_LIMIT) for limit, .min(0) for offset) and per-route z.enum for sortBy/filter keyed to the existing SORT_MAP keys, then safeParse(req.query) once per route. To stay within the smallest-safe-change / no-regression culture, preserve today's lenient coercion using .catch()/.default() (so bad limit still clamps rather than 400s) unless a deliberate decision is made to tighten to 400 — and gate that decision on a frontend/lib/api.ts audit. This removes `as string` casts and, paired with typed sort maps, eliminates a meaningful share of the file's `any`. No AGENTS.md conflict (zod is already the adopted standard; no raw SQL involved).

**Safety / pitfalls.** This is a behavior change, not a pure refactor. Current code silently coerces bad input to fallbacks: parseInt("abc",10)||50 -> 50, and lines 1422/1644 (parseInt with no ||) pass NaN downstream into `take: limitNum*3`. Negative offset (offset=-5) currently flows into Prisma `skip`. Unknown sortBy currently falls back via `?? {default}`. Switching to safeParse+400 and z.enum will START rejecting inputs the frontend may currently send (empty strings, floats, out-of-range limits, unrecognized sort keys). The frontend API boundary (frontend/lib/api.ts) MUST be audited first so a stricter schema does not 400 previously-tolerated calls. Mitigate by using z.coerce.number with .catch()/.default() and .clamp-equivalent (min/max) plus z.enum(...).catch(default) to preserve today's lenient semantics where the contract requires it, rather than hard-failing.

---

### F9 — Two parallel frontend systems each duplicated: dual toast renderers (sonner + custom) and dual data-fetching paradigms (TanStack Query vs hand-rolled useEffect loaders)

**⬜ open** · dimension: readability · severity: medium · effort: M · risk: low · epic: #16

> **Audit note.** ✓ ~26 files reference the custom toast-context (this figure stands; an audit claim of 15 counted call-sites, not file imports). sonner = 34. The real point — two toast renderers mounted at once — holds.

**Files:** `frontend/app/layout.tsx`, `frontend/lib/toast-context.tsx`, `frontend/app/vibe/page.tsx`, `frontend/hooks/useQueries.ts`

**Problem.** layout.tsx mounts BOTH sonner's <Toaster/> (line 96) AND the custom ToastProvider (line 85, a full hand-rolled renderer in lib/toast-context.tsx with its own timeout map/ToastItem). Usage is split 35 files importing sonner vs 26 importing the custom useToast — duplicated infra, double DOM containers, divergent styling/a11y, and a coin-flip per notification. Separately, a capable query layer (hooks/useQueries.ts, with a centralized queryKeys factory) is used by only ~6 pages while ~23 app pages still hand-roll useState(loading/error/data)+api.\* inside useEffect (e.g. vibe/page.tsx), and there are 67 inline `queryKey: [...]` magic-string arrays that bypass the factory, so the same logical key is hardcoded in multiple files and a typo silently breaks cache invalidation.

**Recommendation.** Confirmed on all material claims. Verified counts: layout.tsx mounts BOTH ToastProvider (line 85, full hand-rolled renderer with timeout Map + ToastItem in lib/toast-context.tsx) AND sonner's <Toaster/> (line 96); 35 files import sonner vs 26 import the custom useToast; 68 inline queryKey:[...] arrays exist (claim said 67) vs 61 factory uses; only 8 app pages (of 38) import useQueries hooks while 18 hand-roll useEffect+api.\* loaders (vibe/page.tsx is a clear example: isLoading/error/setError state at lines 588-590 driving useEffect blocks that call api.getVibeStatus()/api.getTracks()/api.getVibeSimilarTracks directly). TOASTS: standardize on sonner; reimplement useToast() as a 4-line adapter over sonner.toast (.success/.error/.warning/.info → sonner equivalents), map error type to assertive aria-live to preserve a11y, then delete ToastProvider from layout and the bespoke renderer. This also removes a z-index inconsistency (z-[100] vs 10020). FETCHING: migrate manual loaders to useQuery/useMutation incrementally, highest-traffic first; extend the queryKeys factory to cover every inlined key and replace the 68 inline arrays with factory calls. Add one guardrail the finding omitted: also route the ~handful of inline invalidateQueries/setQueryData calls (e.g. ["library","liked-playlist"] at my-liked:236, ["playlist",id] x4 in playlist/[id]/page.tsx, ["user-settings"], ["notifications"], ["enrichment-status"] in settings) through the factory in the SAME pass, since those are where a typo silently breaks cache invalidation — and add an ESLint rule banning literal-array queryKey/invalidateQueries to make the cache graph statically discoverable and prevent regression.

**Safety / pitfalls.** Toast migration is low-risk but not zero: the custom useToast surface (toast.success/error/warning/info(message)) is a strict subset of sonner's API, so a thin adapter delegating to sonner.toast.\* keeps all 26 call-sites working unchanged, and no call-site passes the custom `action` field (verified via grep), so no feature loss. Two real divergences must be preserved when deleting the bespoke renderer: (a) z-index — custom container uses z-[100] while the mounted sonner Toaster uses zIndex 10020, so consolidating onto sonner actually FIXES a latent layering bug rather than regressing, but the team must confirm 10020 clears all overlays/player chrome; (b) the custom renderer's `aria-live`/role-per-type a11y (role=alert+assertive for errors) should be matched via sonner's options so error announcements aren't downgraded. For the fetching migration, the genuine hazard is cache-key correctness: I found inline keys that drift from the factory, e.g. app/playlist/my-liked/page.tsx:236 invalidates ["library","liked-playlist"] (2 elements) while queryKeys.likedPlaylist() emits ["library","liked-playlist",limit] (3) — today these only line up by TanStack prefix-matching, so any centralization must preserve exact prefix semantics or invalidations silently stop matching. No AGENTS.md contract violation (both stay within frontend/lib/api.ts boundary; no new direct fetch, no backend/SQL touch). Must be done page-by-page with the documented per-key staleTime preserved, since vibe/page.tsx and similar manual loaders have no caching today and a wrong staleTime could change refetch behavior users rely on.

---

### F10 — Per-page god-components and duplicated cover-art widgets: VibePage (1247 LOC, 16 useState, 7 inline components) and two near-identical CoverImage/CoverThumb copies

**⬜ open** · dimension: readability · severity: medium · effort: M · risk: low · epic: #16

**Files:** `frontend/app/vibe/page.tsx`, `frontend/app/listen-together/page.tsx`

**Problem.** app/vibe/page.tsx is a 1247-LOC page mixing types (TrackFeatures/TrackData), a trackDataToTrack mapper, preset tables, 7 inline presentational components (CoverImage at 135, SimilarityBadge, FeatureComparison, MoodGrid, TagPills, ComparisonPanel, TrackRow) and a 600-line VibePageContent holding 16 useState values with an ad-hoc viewMode state machine and per-track api.getTrackAnalysis fan-out with no caching. It's untestable and representative of the per-page god-component pattern. Separately, vibe/page.tsx CoverImage (135) and listen-together/page.tsx CoverThumb (62) are near-identical cover-art-with-error-fallback components that diverge subtly (rounding, icon sizing) — the shared CachedImage doesn't cover the onError-fallback case, so pages keep rolling their own.

**Recommendation.** Accept the finding; it is accurate and aligns with the repo's own established patterns. Tighten as follows. (A) Cover dedup: create components/ui/CoverArt.tsx (NOT an extension of CachedImage, which has no onError state and is SW-cache-memoized). API: { coverArt: string|null; title: string; size?: number; className?: string; fallbackIcon?: ReactNode; fallbackClassName?: string; priority?: boolean }. It encapsulates getCoverArtUrl(coverArt, size\*2) sizing + internal hasError state + the bare-div fallback. Replace both app/vibe/page.tsx CoverImage and app/listen-together/page.tsx CoverThumb, passing the existing per-page styles via props so the #282828-vs-#1a1a1a bg and 0.3-vs-0.45 icon-scale differences are preserved exactly (verify with a visual/snapshot diff). These two files are the ONLY rollers of this onError pattern, so this fully closes the gap. (B) Vibe split: this is the larger, higher-risk change — sequence it separately from the cover extraction so each lands as a small-safe-change. Move TrackFeatures/TrackData/LibraryTrack/VibePreset types + trackDataToTrack + distanceToSimilarity + VIBE_PRESETS/FEATURE_CONFIG/MOOD_CONFIG into features/vibe/types.ts and features/vibe/constants.ts; extract the 7 presentational components into features/vibe/components/. Add a batched hook in hooks/useQueries.ts (e.g. useTrackAnalysesQuery(trackInfos)) built on the already-in-repo useQueries from @tanstack/react-query (precedent: features/album/hooks/useAlbumActions.ts, hooks/useCollectionLikeAll.ts) — caching + dedupe via queryKeys, with per-item error fallback to the 0.5-default features object preserving today's catch behavior, and add to the queryKeys factory at hooks/useQueries.ts:25. Collapse the isLoading/isSearching/viewMode/error/sourceTrack/similarTracks/selectedMatch cluster into a useReducer (keep vibeTab and inputValue as local state). Follow the existing features/<domain> convention (note features/vibe/ does not exist yet; components/vibe/VibeMap.tsx already does and should move under it). TDD: there is already tests/component/useQueriesBrowseFeatured.component.test.ts as a template for the new batched hook.

**Safety / pitfalls.** Behavior-preservation traps when consolidating the two cover components and refactoring vibe state — none are blockers but must be respected: (1) The two cover widgets diverge VISUALLY and these are load-bearing styles, not noise: CoverImage fallback uses bg #282828 + icon scale 0.3 + no default rounding + uses useMemo + accepts a `priority` prop; CoverThumb uses bg #1a1a1a + icon scale 0.45 + hardcoded `rounded flex-shrink-0` + no priority. A naive single <CoverArt> that picks one set silently changes pixels on whichever page loses. The shared component must parametrize fallbackBg, fallbackIconScale (or fallbackIcon node), default rounding/flex-shrink via className, and optional priority — otherwise it is a visual regression. Also CoverImage's non-fallback wrapper uses bg #1a1a1a while CoverThumb adds `rounded flex-shrink-0`; preserve via className passthrough. (2) Do NOT fold this into CachedImage — CachedImage is `memo`'d, returns null on empty src, and is wired to the Service-Worker cache-first /api/library/cover-art route; bolting hasError state onto it would defeat its memoization contract and touch ~9 consumers. A NEW components/ui/CoverArt is the safer move (as proposed). (3) The useQueries batching swap changes failure semantics: today fetchTrackWithFeatures swallows per-track getTrackAnalysis errors and substitutes 0.5-default features so a partial analysis failure still renders the row; a useQueries port must replicate this per-item fallback (don't let one rejected query blank the list). (4) Note getTrackAnalysis can return analysisStatus 'pending'/null fields — caching is fine but pick a sane staleTime so an in-progress analysis isn't cached as permanently-empty. (5) AGENTS contract: all three cover paths already correctly route through lib/api.ts getCoverArtUrl (no direct fetch) and no backend/raw-SQL surface is touched, so the contract is not at risk. (6) The vibe page has a hidden coupling: handleRefresh/handleUseCurrentTrack reconstruct a LibraryTrack from TrackData and TrackData from currentTrack — a useReducer/types extraction must keep these round-trip mappers intact or 'Now Playing'/'Refresh' break silently.

---

### F11 — Three near-identical auth resolvers duplicate the credential ladder and carry a permanently-dead express-session branch

**🟡 partial (PR #115)** · dimension: readability · severity: medium · effort: M · risk: medium · epic: #14

> **Fix shipped (partial — step 1 only).** The Safety/pitfalls paragraph
> below names 8 sites where `req.session.userId!` was the SOLE source of
> `userId` while the router mounts `requireAuth` (which populates
> `req.user`, never `req.session`): `routes/offline.ts:53,196,252,337,387`
> and `routes/listeningState.ts:59,127,181`. All 8 now read `req.user!.id`
> instead — a genuine correctness fix, not dead-code cleanup: every one of
> these handlers ran with `userId === undefined` for every real client
> before this change. Regression tests: `offlineRuntime.test.ts` and
> `listeningStateRuntime.test.ts` previously passed by injecting
> `req.session = { userId: "u1" }` directly into a hand-built request,
> pinning the bug rather than exercising it — both suites now build the
> request the way `requireAuth` actually does (`req.user` populated, empty
> `req.session`) and assert the resolved id reaches Prisma, including one
> new per-file case proving a _second_ authenticated user's id flows
> through rather than a hardcoded literal. Re-running the adjusted suites
> against the pre-fix handlers reproduced `userId: undefined` in the Prisma
> call args (8 of 22 cases failed across both suites), confirming the fix is
> load-bearing.
> **Still open, none of it touched by this change:** the dead session
> branch itself (auth.ts:92-102, 219-233), the `|| req.session?.userId`
> fallback idioms (`apiKeys.ts`, `mixes.ts`), the 3-resolver consolidation,
> and the express-session/connect-redis removal.

**Files:** `backend/src/middleware/auth.ts`, `backend/src/middleware/subsonicAuth.ts`

**Problem.** authenticateRequest() (auth.ts:87), requireAuthOrToken() (auth.ts:213) and the Subsonic path each re-implement the same credential-resolution sequence (session lookup, X-API-Key + lastUsed touch, query-token verify + tokenVersion check, bearer verify + tokenVersion check); requireAuthOrToken inlines ~100 lines of authenticateRequest with subtly different control flow — the classic copy-paste that drifts (one path could forget the tokenVersion check). Worse, every resolver checks `req.session?.userId` FIRST (auth.ts:92, doing a prisma.user.findUnique), but a repo-wide grep confirms req.session.userId is NEVER assigned anywhere — login/register only issue JWTs. So a permanently-dead branch sits at the top of the hot path and spreads confusing `req.user?.id || req.session?.userId` dual-auth idioms (6 sites).

**Recommendation.** Confirmed and worth doing, but reframe and re-sequence. (1) Consolidate auth.ts into an ordered strategy array [apiKeyStrategy, queryTokenStrategy, bearerStrategy] each returning user-or-null, with requireAuth passing {apiKey,bearer} and requireAuthOrToken passing {apiKey,queryToken,bearer}; this centralizes the duplicated tokenVersion check and the lastUsed touch and removes requireAuthOrToken's confusing empty-if/else inversion (auth.ts:275-280,303-308). (2) Delete the dead session branch (auth.ts:92-102, 219-233) and the `|| req.session?.userId` fallbacks at apiKeys.ts:76,137,203 and mixes.ts:18 — but FIRST fix the real bugs: replace every `req.session.userId!` in offline.ts and listeningState.ts with `req.user!.id` (these routes are currently broken). (3) subsonicAuth.ts is genuinely a different protocol (md5 token+salt, encrypted subsonicPassword, dummy-bcrypt timing equalization, multi-mechanism conflict checks) — it shares only the apiKey lookup; do NOT force it into the same resolver, at most extract a shared apiKeyLookup helper for the lastUsed touch. (4) Treat removal of express-session/connect-redis (index.ts:202-221, package.json deps, src/types/express-session.d.ts, the test mock) as a SEPARATE follow-up change after the route migrations land, since it currently provides the req.session type and the Redis-backed store. Per AGENTS.md smallest-safe-change/TDD culture, add tests asserting tokenVersion mismatch rejects on both query-token and bearer paths before refactoring.

**Safety / pitfalls.** The finding UNDERSTATES the risk: the dead session branch is not merely a no-op. routes/offline.ts (lines 53,196,252,337,387) and routes/listeningState.ts (lines 59,127,181) read `req.session.userId!` with a non-null assertion as the SOLE source of userId, while mounting `router.use(requireAuth)` — which populates req.user, NOT req.session.userId. Because session.userId is never written anywhere in the repo, those 8 handlers already resolve userId to `undefined` (latent bugs, not just dead code). Any refactor MUST migrate these reads to `req.user!.id`, otherwise the bugs persist; and deleting the SessionData augmentation in src/types/express-session.d.ts will turn `req.session.userId!` into TS compile errors across both files. Separately, removing the express-session/connect-redis wiring (index.ts:202-221, deps in package.json, mock in **tests**/apiEntrypointRuntime.test.ts:238-239, the .d.ts) is a larger blast radius than 'as part of this consolidation' implies and should be its own change — express-session also currently provides the `req.session` object those broken routes type against. The auth-resolver consolidation itself is behavior-preserving and safe as long as the apiKeyStrategy keeps the fire-and-forget lastUsed touch and every JWT/query-token path keeps the strict tokenVersion check (decoded.tokenVersion === undefined || !== user.tokenVersion => reject). Note requireAuthOrToken's query-token path is effectively requireAuth + a query-token strategy; centralizing must preserve that requireAuth (checkQueryToken=false) does NOT honor ?token=.

---

### F12 — Player UI re-renders 4×/second during playback: useAudio() pipes the high-frequency currentTime context into the heaviest player trees

**🟡 partial (PR #109)** · dimension: performance · severity: high · effort: M · risk: medium · epic: #16

> **Fix shipped (partial).** Tightened-plan items (A) + (B) landed, frontend-only.
> (A) `UniversalPlayer` — the animated render root that mounts
> OverlayPlayer/MiniPlayer/FullPlayer inside AnimatePresence/LayoutGroup — moved
> off `useAudio()` onto the granular `useAudioState()`, so it no longer re-renders
> that whole subtree on every clock tick. (B) `setCurrentTimeFromEngine` now
> publishes to React state only at display-second (`Math.floor`) boundaries (or on
> a forced discontinuity), a decision extracted to the pure, unit-tested
> `lib/audio-clock-policy.ts`; an authoritative full-precision `currentTimeRef` is
> written on every accepted tick and by every discontinuity setter, and the former
> state→ref sync effect was removed — so persistence now reads engine-fresh
> precision in BOTH paths: server progress (`savePlaybackProgressToServer`) and
> the throttled localStorage resume snapshot (`<brand>_current_time`) that
> `AudioPlaybackOrchestrator` reads to compute the engine's resume `startTime`
> after a reload/crash. howler-engine's 250ms tick is unchanged. **Known minor
> trade-off (accepted):** relative-seek actions that compute from the _published_
> clock — TVLayout ±10s, keyboard-shortcut ±10s, media-session seek handlers
> (`seekOffset` default 10s), controls `skipForward`/`skipBackward` (default 30s)
> — now read a value up to ~1s stale instead of ~250ms; these are inherently
> coarse actions, and precision work on those consumers belongs to item (C)'s
> leaf-extraction follow-up. **Render evidence**
> (real provider stack under happy-dom, 8 ticks / 2 simulated seconds): the
> `UniversalPlayer` subtree re-renders **8 → 0**, and playback-state publishes
> **8 → 2** (quantized to the 1.0s/2.0s boundaries; both asserted as exact
> counts). Tests: `audioClockPolicy.test.ts` (unit),
> `universalPlayerSubscription.component.test.ts`,
> `universalPlayerRenderCount.component.test.ts`, and
> `audioPlaybackStoragePrecision.component.test.ts` (component). **Still open:**
> leaf extraction in OverlayPlayer/MiniPlayer/TVLayout (item C) and any F5
> orchestrator work.

**Files:** `frontend/lib/audio-hooks.tsx:16`, `frontend/lib/audio-playback-context.tsx:303`, `frontend/lib/howler-engine.ts:736`, `frontend/lib/howler-engine.ts:739`, `frontend/components/player/OverlayPlayer.tsx:157`, `frontend/components/player/MiniPlayer.tsx:43`, `frontend/components/player/UniversalPlayer.tsx`, `frontend/components/layout/TVLayout.tsx`

**Problem.** howler-engine.ts emits `timeupdate` on a 250ms interval (4×/sec, line 739), and setCurrentTimeFromEngine calls `setCurrentTime(time)` raw with full float precision (audio-playback-context.tsx:303) — no quantization/throttle. The compatibility hook useAudio() (audio-hooks.tsx:16) subscribes to ALL THREE contexts and spreads currentTime into one object, and 6 components consume it — including the two largest player UIs: OverlayPlayer (2367 LOC, a framer-motion/AnimatePresence tree, confirmed `useAudio()` at line 157) and MiniPlayer, plus UniversalPlayer and TVLayout. So the entire animated overlay subtree re-renders 4×/sec for the whole duration of every track, even though only a thin seek bar needs the ticking value. FullPlayer already proves the better pattern by using the granular useAudioState()/useAudioPlayback() hooks; the coarse consumers contradict the hook's own documented guidance.

**Recommendation.** Core problem is REAL and high-value: howler-engine.ts:739 ticks every 250ms; setCurrentTimeFromEngine (line 303) calls setCurrentTime raw; the playback context value useMemo (lines 589-632) lists currentTime in its deps, so a new context object is produced 4x/sec and every useAudioPlayback()/useAudio() consumer re-renders. No React.memo guards any player component and there is no useSyncExternalStore/zustand/jotai anywhere, so nothing mitigates this today.\n\nBut two specifics in the finding are WRONG and must be corrected before acting:\n1) FullPlayer does NOT prove a 'better pattern' for the ticking value. Its line-50 comment refers only to avoiding re-renders on STATE/track/queue changes; it still destructures currentTime from useAudioPlayback() (line 70) and re-renders 4x/sec. There is NO <SeekProgress/> leaf anywhere (grep empty). So the proposal to 'replicate the FullPlayer pattern' is replicating a component that has the same problem.\n2) UniversalPlayer does NOT consume currentTime (it destructures only playerMode/currentTrack/currentAudiobook/currentPodcast from useAudio at line 22). The finding mislabels it as a currentTime consumer. However UniversalPlayer is actually the RENDER ROOT that mounts OverlayPlayer, MiniPlayer, and FullPlayer inside AnimatePresence/motion.div — and because it subscribes to useAudio() it re-renders 4x/sec and re-renders that whole animated subtree. This makes it the single highest-leverage fix, and the finding undersells it.\n\nTightened plan (smallest-safe-change, frontend-only):\n- (A) Highest ROI, lowest risk: migrate UniversalPlayer from useAudio() to useAudioState() (it needs no playback/control fields). This alone stops the animated parent from ticking and cascading into OverlayPlayer/MiniPlayer/FullPlayer on every clock update.\n- (B) Quantize the clock SAFELY: in setCurrentTimeFromEngine, set currentTimeRef.current = time at full precision unconditionally, then only call setCurrentTime when the rounded/displayed second (or a >=~250-500ms delta) changes. This preserves seek-lock proximity and server-progress precision while cutting setState churn. Verify the seek-lock path (lines 286-302) and persistence (line 528) still see full-precision values.\n- (C) For OverlayPlayer/MiniPlayer/TVLayout that genuinely render the ticking second: extract the live value into a small leaf (e.g. wrap the existing <SeekSlider>/time labels in a child that is the only subscriber to currentTime via useAudioPlayback), and have the heavy parent consume useAudioState()/useAudioControls(). Note OverlayPlayer feeds displayTime (line 239) into <SeekSlider> at lines 1269 and 2077 plus formatTime labels (line 1279/1282) — those are the only places that need the tick.\n- Defer the useSyncExternalStore/zustand/jotai field-selector rewrite as a separate larger initiative; do not bundle it into this fix.

**Safety / pitfalls.** Quantizing the published clock is risky if done naively. setCurrentTimeFromEngine's `time` is also consumed by seek-lock proximity logic (shouldAcceptEngineTimeUpdate at audio-playback-context.tsx:286-291, plus howler-engine.ts:721 `Math.abs(seek - seekTargetTime) < 2` and the 500ms unlock) and by server-progress persistence via currentTimeRef.current (read at line 528 in savePlaybackProgressToServer). Today currentTimeRef is synced from the `currentTime` STATE (effect at line 311), so if you quantize the value passed to setCurrentTime you also quantize the ref unless you set currentTimeRef.current = time at full precision FIRST, independently of the gated setCurrentTime call. Otherwise saved progress and seek-target matching drift. Second concern: the finding's suggested useSyncExternalStore/zustand/jotai migration is a context-architecture rewrite, not a smallest-safe-change, and touches every audio consumer + the frontend hook boundary — it should be explicitly out of scope. No AGENTS.md contract is violated by the conservative version (no new fetch, no raw SQL — frontend-only).

---

### F13 — Per-track/per-candidate N+1 query loops dominate Spotify import and Discover Weekly hot paths

**🟡 partial (PR #108)** · dimension: performance · severity: high · effort: L · risk: medium · epic: #17

> **Fix shipped.** Audited smallest-safe scope, split by path per this finding's own Recommendation. SPOTIFY IMPORT: `buildPreviewFromTracklist`'s matchTrack loop (spotifyImport.ts:1009) is bounded via the already-imported PQueue (concurrency 4, matching the worker's Prisma pool size), order-stable (`matchedTracks` and the `unmatchedByAlbum` grouping stay in exact input order regardless of DB-resolution order). matchTrack's internals and per-track query sequences are untouched, so `spotifyImportRuntime.test.ts`'s pinned findFirst/findMany call-count assertions stay green unmodified; the deeper matchTrack query-merge remains deferred pending that suite's migration to behavior-based assertions, as this finding's Recommendation specifies. Benchmarked ~2.5x faster matchTrack-loop wall-clock on a 50-track playlist (medians 130.31ms → 52.14ms across 3 interleaved runs each, 15,230-track/299-artist/1,241-album dev corpus). DISCOVER WEEKLY: `selectFromTier` and the "fill remaining slots" loop (discoverWeekly.ts ~:3161 / ~:3241 at this branch's HEAD) now consult one batch-prefetched `artist.findMany` membership map instead of up to 2 DB round trips per candidate artist across the tier pool; `isArtistInLibrary`'s two-probe decision (temp- MBID exclusion, MBID-hit-with-zero-albums falling through to an independent name probe, case-insensitive name match) is reproduced exactly and pinned by a parity test — with one deliberate edge resolution, also pinned there: schema-legal duplicate case-insensitively-equal artist names (`Artist.name` carries no unique constraint; the live DB has zero such pairs) were previously decided by an unordered `findFirst` — nondeterministic true/false depending on which duplicate row Postgres happened to return — and now resolve as a deterministic OR across the duplicate rows (any duplicate owning an album ⇒ in-library, i.e. skip the ambiguous same-named artist — the safe direction for a discovery feature). `selectFromTier` itself stays fully serial by design (early break + ordered `seenArtists`/`seenAlbums` mutation unchanged) — this is a query-count win only, NOT a wall-clock win, since the dominant Discover Weekly cost is the rate-limited lastFm/MusicBrainz funnel, which batching cannot remove (per this finding's Safety/pitfalls). Tests: `spotifyImportMatchConcurrency.test.ts`, `discoverWeeklyPrefilterParity.test.ts` (new); `spotifyImportRuntime.test.ts`, `spotifyImportResilienceContract.test.ts`, `discoverWeeklyRuntime.test.ts`, `discoverWeeklyPrismaRetryContract.test.ts` (existing, count assertions unchanged).

**Files:** `backend/src/services/spotifyImport.ts:1009`, `backend/src/services/spotifyImport.ts:399`, `backend/src/services/spotifyImport.ts:514`, `backend/src/services/discoverWeekly.ts:3011`, `backend/src/services/discoverWeekly.ts:2016`, `backend/src/services/discoverWeekly.ts:3034`

**Problem.** buildPreviewFromTracklist awaits matchTrack(track) inside a plain `for (const track of tracks)` loop (spotifyImport.ts:1009). Each matchTrack runs up to ~5 SEQUENTIAL queries: exact match, full-artist-name match, normalized-album match, and a `album.findMany({ include: { tracks: true, artist: true } })` that pulls EVERY album-with-all-its-tracks for the candidate artist (spotifyImport.ts:514). For a 50-track playlist that is hundreds of serial round-trips. Discover Weekly's selectFromTier loops every candidate artist calling isArtistInLibrary (2 findFirst each, discoverWeekly.ts:2016) then findValidAlbumForArtist serially across 4 tiers + fallbacks (discoverWeekly.ts:3011-3052). All of this is awaited one-at-a-time with no batching or concurrency, despite PQueue already being imported in spotifyImport.ts for the Deezer path.

**Recommendation.** Split the finding by path; they are not equivalent. SPOTIFY IMPORT (strong, real win): the serial `for (const track of tracks) await this.matchTrack(track)` at spotifyImport.ts:1009 is purely DB-bound (no per-track external API), so it genuinely benefits from concurrency/batching. The expensive `album.findMany({ include: { tracks: true, artist: true } })` at :514 pulls every album-with-all-tracks for the candidate artist, but note it is CONDITIONAL (only when strategies 1/1b/2-first miss) — correct the finding's "every matchTrack runs ~5 queries" to "up to". Smallest-safe-change here: wrap the matchTrack loop in the already-imported PQueue with a bounded concurrency (e.g. 4-8) — this overlaps the serial DB round-trips with near-zero behavior risk (results are collected independently; unmatchedByAlbum grouping is order-independent). Defer the deeper matchTrack query-merge / bulk pre-load until the call-count assertions in spotifyImportRuntime.test.ts are migrated to assert behavior (matchType/confidence) rather than exact findFirst counts. DISCOVER WEEKLY (downgrade): the only justified change is batching isArtistInLibrary into one `artist.findMany` keyed by mbid+name with album-existence, used as a Map prefilter BEFORE the loop — but this saves only small indexed DB lookups, NOT the dominant cost. Do NOT parallelize selectFromTier (early-break + ordered seenArtists/seenAlbums mutation + rate-limited external calls make it both behavior-changing and ineffective). State explicitly that the real Discover Weekly latency is the rate-limited lastFm/MusicBrainz funnel, which batching cannot remove.

**Safety / pitfalls.** Multiple real risks. (1) BRITTLE TESTS: spotifyImportRuntime.test.ts asserts exact query call counts on matchTrack, e.g. `expect(prisma.track.findFirst).toHaveBeenCalledTimes(1)` (line 404) and `...times(2)` (line 432), and directly spies matchTrack/buildPreviewFromTracklist. Any restructure of matchTrack into a single findMany or a bulk pre-load WILL break these and is therefore NOT a zero-risk drop-in; tests must be rewritten. (2) BEHAVIOR CHANGE in selectFromTier (discoverWeekly.ts:3011): the loop has an early break (`if (selected.length >= count) break`) and mutates shared `seenArtists`/`seenAlbums` Sets in iteration order; introducing concurrency changes WHICH albums get picked and makes selection non-deterministic — a recommendation-content change, not a pure perf win. (3) SEMANTIC MISMATCH in the proposed artist-membership batch: isArtistInLibrary (discoverWeekly.ts:2016) matches by MBID OR `name insensitive equals` AND requires `albums.length > 0`; the proposed `normalizedName: { in }` keys off a DIFFERENT column (normalizedName != name-insensitive) and must reproduce the >0-albums gate via `_count`. Get this wrong and artists are wrongly classified as new/owned. (4) The proposed PQueue-overlap for Discover Weekly's external work would NOT help: lastFm/MusicBrainz already route through a global rate-limiter (rateLimiter.ts) with PQueue concurrency 2 and 1 respectively at ~1-3 req/s, so external latency stays serialized regardless. No AGENTS.md contract violation — proposal stays within Prisma (no raw SQL) and respects the config/logger boundaries.

---

### F14 — pgvector ANN searches never set ivfflat.probes — every 'similar tracks' / vibe query scans 1 of 224 lists (default recall)

**✅ complete (PR #107)** · dimension: performance · severity: high · effort: M · risk: medium · epic: #17

> **Fix shipped.** All five pgvector ANN sites — `hybridSimilarity.ts` hybrid + CLAP-only modes, and `vibe.ts` `findNearestToEmbedding` (both variants) + the text-vibe search — now route through one shared `backend/src/utils/annQuery.ts` helper that applies `ivfflat.probes` via a transaction-scoped `set_config('ivfflat.probes', …, true)` on the SAME pooled connection as the query (a bare `SET LOCAL` no-ops under Prisma pooling; utility statements reject bind params, so `set_config` with `is_local=true` is the parameterizable `SET LOCAL` equivalent). New `IVFFLAT_PROBES` config value, default **32**, chosen by `backend/scripts/benchmark-ivfflat-probes.ts` on the real local corpus (15,230 embeddings, lists=224): recall@10 rises from ≈0.26 at probes=1 (the silent pg default) to ≈0.96 at probes=32, p95 ≈ 6 ms; latency only cliffs (~44 ms) at probes≥112. lists=224 kept for the 15,230-row corpus; re-evaluation deferred — a lists change is an index rebuild (migration), out of smallest-safe scope. Tests: `annQuery.test.ts` (helper set_config/transaction contract), `hybridSimilarity.test.ts` + `vibeSearchCompat.test.ts` (route/service behaviour through the helper).

**Files:** `backend/src/services/hybridSimilarity.ts:136`, `backend/src/services/hybridSimilarity.ts:149`, `backend/prisma/migrations/20260127000000_add_pgvector/migration.sql:21`, `backend/prisma/migrations/20260128100000_reduce_embedding_dimension/migration.sql:25`

**Problem.** The embedding index is `ivfflat (embedding vector_cosine_ops) WITH (lists = 224)` but a repo-wide grep confirms NO `SET LOCAL ivfflat.probes` / no `ef_search` anywhere in src or prisma. ivfflat defaults to probes=1, so each `embedding <=> source` ANN search (hybridSimilarity.ts:149 candidate CTE, and the vibe-map / vibe.ts queries) scans only 1 of 224 inverted lists. Recall is silently very low: the CLAP candidate set — and therefore every similar-tracks result and the hybrid re-rank that sits on top of it — misses most relevant neighbors. This is a correctness-shaped performance bug: the system pays for ANN infrastructure but gets near-random candidates.

**Recommendation.** Real and high-value bug: ivfflat index (lists=224) is queried with the pgvector default probes=1, so every ANN ORDER BY ... <=> scans 1/224 lists and recall is silently low. Confirmed sites: hybridSimilarity.ts (clap_candidates CTE ORDER BY at line 152, and CLAP-only mode at lines 204/220/227) and vibe.ts (findNearestToEmbedding lines 213/227 and text-vibe search line 792). The cited hybridSimilarity.ts:149 is actually the similarity-score expression; the index-driving ORDER BY is line 152 — cite that instead. CORRECTION: umapProjection.ts:269 uses ORDER BY RANDOM() (full sample, no <=>), so it does NOT use the ivfflat index — drop it from the 'vibe-map query' claim; the finding overstates scope there. Also correct the corpus figure: MAX_EMBEDDINGS=15000 is only the UMAP sampling cap, not the embedding count; the real embedding-table row count is unknown from code and should be measured before re-tuning lists. Smallest-safe-change: introduce a config value (e.g. config.ts IVFFLAT_PROBES, default ~10-15) and run each ANN query inside prisma.$transaction([prisma.$executeRaw`SET LOCAL ivfflat.probes = ${probes}`, prisma.$queryRaw`...`]) so the GUC scopes to the same connection — a bare SET LOCAL outside a transaction will not work under Prisma pooling. Benchmark recall@k and p95 latency before/after on the real corpus and tune probes (and re-evaluate lists vs actual row count) accordingly. Treat HNSW migration as a separate, larger follow-up requiring CREATE INDEX CONCURRENTLY, not part of this change.

**Safety / pitfalls.** SET LOCAL only scopes to a transaction, so it MUST be wrapped in prisma.$transaction([...]) (interactive) — issuing SET LOCAL and the $queryRaw as two separate prisma calls will run on different pooled connections and the GUC will silently not apply. Prisma's connection pooling (db.ts sets connection_limit) makes a plain non-transactional SET LOCAL a no-op/footgun. Second concern: higher probes increases latency per query (probes=15 ~= up to 15x list scan cost) — must benchmark, not blindly set. The migrate-to-HNSW option (option b) is a schema-level migration that rebuilds the index (CREATE INDEX ... USING hnsw is slow/locking on a large table; needs CONCURRENTLY) and is NOT a smallest-safe-change — defer it. Also note the AGENTS.md 'no raw SQL' contract is already violated by these $queryRaw ANN sites (unavoidable for pgvector); adding SET LOCAL stays within that existing exception but should be acknowledged.

---

### F15 — ORDER BY RANDOM() full-table scans on Track / track_embeddings for radio, random tracks, and the vibe map

**🟡 partial (PR #110)** · dimension: performance · severity: medium · effort: M · risk: medium · epic: #17

> **Fix shipped.** `/tracks/shuffle`'s large-library branch — the audited priority (genuinely frequent, uncapped at `MAX_LIMIT`, no cache) — now samples via a new indexed `Track.random double precision` column (btree index, DB-generated `random()` default) instead of a full-table `ORDER BY RANDOM()` scan+sort: pick a uniform pivot in `[0,1)`, `WHERE random >= pivot ORDER BY random ASC LIMIT n`, with a wrap-around top-up query (`random < pivot`) for the exact `LIMIT` when the pivot lands near 1.0. This deletes the endpoint's raw-SQL site. Benchmarked on the local 15,230-track corpus (branch `perf/f15-shuffle-random-column`): `LIMIT 100` p50 ~2.0ms → ~0.7ms, p95 ~4.1ms → ~1.5ms; `LIMIT 1000` p50 ~2.7ms → ~2.0ms, p95 ~3.3ms → ~2.4ms. Sampling honesty (cross-review corrected an earlier "no gross skew" claim): pivot-successor sampling selects each track with probability proportional to its local gap between adjacent persisted `random` values, not 1/N — measured on the live corpus, collision probability is 2.03× true-uniform (theory says exactly 2×: Σgap² → 2/N), the 2,000-draw diagnostic (1,766 distinct ids, max repeat 5) actually sits ~2.7σ below the uniform null (expected ≈1,874 distinct; P(max-repeat≥5) < 0.5%), and the 20-bucket check mathematically cannot detect this (≈760 tracks/bucket averages the gap variance away). At real usage scale (limit=100, rolling 100-gap window over the actual corpus) per-track inclusion probability has CV ≈ 11% (range 68%–141% of fair) and adjacent-request page-overlap chance is ≈ 1.3% per pair; the persisted values never re-roll, so this bias is permanent per-library. Judgment: acceptable for a shuffle feature — approximate per-track fairness, not literal uniformity. Per this finding's own audit corrections, radio multi-seed Fallback B (library.ts:759) and the vibe-map's `umapProjection` sampling are intentionally left as-is: Fallback B because its `NOT IN` + JSONB predicates blunt the win ("consider leaving it unless profiling shows it matters"), and `umapProjection` because it's already gated behind a 24h cache and runs at most once/day, not per request. The discoverWeekly.ts citation in this finding's Problem statement was confirmed FALSE (no `ORDER BY RANDOM` exists there). Tests: `backend/src/routes/__tests__/libraryRuntime.test.ts` (shuffle empty/small/large-full-page/large-top-up-shortfall/error cases, rewritten off the deleted raw-SQL mock to assert exact-`limit` sampling behavior instead of pinning the removed `$queryRaw` call).

**Files:** `backend/src/routes/library.ts:3994`, `backend/src/routes/library.ts:759`, `backend/src/services/umapProjection.ts:269`, `backend/src/services/discoverWeekly.ts`

**Problem.** Several hot paths use `ORDER BY RANDOM() LIMIT n` over large tables. /tracks?random does `SELECT id FROM "Track" ORDER BY RANDOM() LIMIT ${limit}` (library.ts:3994) — a full sequential scan + sort of the entire Track table on every request. The radio multi-seed genre fallback does the same with extra JSONB predicates (library.ts:759). umapProjection.doCompute selects up to MAX_EMBEDDINGS rows with `ORDER BY RANDOM()` over track_embeddings joined to Track/Album/Artist (umapProjection.ts:269). Postgres cannot use an index for ORDER BY RANDOM(); cost grows linearly with table size, so these get slower as the library grows and each one ties up a pooled connection for the duration of the scan.

**Recommendation.** Three of the four citations are real and accurate: library.ts:3994 (/tracks/shuffle, only in the large-library branch where totalTracks > limit; no caching; reached via the proper frontend/lib/api.ts boundary) and library.ts:759 (radio multi-seed Fallback B, ORDER BY RANDOM with JSONB EXISTS predicates) both do full seq-scan+sort ORDER BY RANDOM. umapProjection.ts:269 is real but the finding's 'hot path' framing is WRONG: computeMapProjection() gates doCompute() behind a 24h Redis cache (CACHE_TTL_SECONDS=86400) plus an in-flight computePromise dedupe, so it runs at most once per cache miss/day, not per request. The discoverWeekly.ts citation is FALSE — that file has no ORDER BY RANDOM or raw randomization; it only uses in-memory shuffleArray/Math.random over already-fetched arrays. Prioritize /tracks/shuffle (genuinely frequent, uncapped at MAX_LIMIT=10000, no cache) as the smallest-safe fix, as the finding suggests. Correct the sampling strategy to a `random double precision` column with btree index populated on insert (uniform, exact-LIMIT-capable) rather than id-based keyset/offset, since id is a non-monotonic cuid. For the vibe map, the cheaper/safer fix is not changing the sampling primitive but the fact it's already cached — only worth touching if the once-daily 15k-row 4-table scan measurably blocks a pooled connection. For radio Fallback B, expect a smaller win because of the NOT IN + JSONB filters; consider leaving it unless profiling shows it matters. Centralizing behind a typed repository helper is a good idea and does address the no-raw-SQL contract, but the random_col path requires a schema migration, so it exceeds 'smallest safe change' — stage it after the shuffle endpoint.

**Safety / pitfalls.** The proposed keyset/random-offset-on-id strategy does NOT fit this schema: Track.id is a cuid() string with no monotonic integer column (only DateTime fileModified/createdAt). cuids are time-prefixed + random, so a `WHERE id >= random pivot` keyset or `OFFSET floor(random()*count)` on id would produce biased, non-uniform sampling. Only the `random double precision` btree column or TABLESAMPLE options are viable here. TABLESAMPLE SYSTEM returns a variable, page-level row count and cannot guarantee an exact LIMIT n, so both /tracks/shuffle and the radio Fallback B (which need exact counts) would require a top-up fallback. The radio Fallback B query also carries a `NOT IN (...)` exclusion plus per-row JSONB genre `EXISTS` predicates, so a random_col ordering won't fully avoid filtering work — the optimization win there is much smaller than for the unfiltered shuffle. Adding a random_col needs a Prisma migration + populate-on-insert wiring, which is larger than a smallest-safe change.

---

### F16 — Genre filtering scans JSONB with jsonb_array_elements_text + LOWER(...) LIKE '%g%' instead of the existing normalized Genre/TrackGenre tables

**⬜ open** · dimension: performance · severity: medium · effort: M · risk: medium · epic: #17

**Files:** `backend/src/routes/library.ts:748`, `backend/src/routes/library.ts:6513`, `backend/src/routes/library.ts:6809`, `backend/prisma/schema.prisma:240`

**Problem.** A normalized Genre + TrackGenre model exists, yet the hot genre paths drive off the denormalized Artist.genres / userGenres JSON columns via raw SQL that expands the array per row and substring-matches: `EXISTS (SELECT 1 FROM jsonb_array_elements_text("Artist"."genres") AS g WHERE LOWER(g) LIKE %g%)` in the radio fallback (library.ts:748) and `CROSS JOIN LATERAL jsonb_array_elements_text(ar.genres::jsonb)` in the genres-listing endpoint (library.ts:6513). LIKE over an unnested JSON array cannot use any index, so each call is a sequential scan that re-parses JSON for every Artist row joined to Albums/Tracks — and it runs on the request path (radio start, genre browse).

**Recommendation.** Confirmed the perf problem: all three cited locations (library.ts:748 radio multi-seed Fallback B, library.ts:6513 GET /genres listing, library.ts:6809 GET /radio case "genre") use jsonb_array_elements_text(...) + LOWER(g) LIKE '%g%' on the request path, and there is NO index on Artist.genres/Album.genres/userGenres (verified schema + migrations) — so each is a seq scan that re-parses JSON per Artist row. Do NOT adopt the 'preferred' TrackGenre rewrite as-is: that table has no production population path, so it must FIRST be wired into the scanner/enrichment pipeline (derive TrackGenre from the genres the enrichment worker already computes) before any read path can rely on it; doing so would also resurrect the already-dead trackGenres fallbacks elsewhere. The smallest-safe-change here is the finding's FALLBACK option: add a GIN index on the genres jsonb column(s) and switch the predicate. Caveats on that too: @> is exact-token containment and changes the current substring semantics (decide intentionally whether 'rock' should still match 'indie rock'; if substring is required, a GIN index won't help LIKE and you'd instead want to normalize/lowercase tokens into a separate indexable structure). And @> does NOT apply to library.ts:6513 at all — that endpoint needs per-genre track COUNTS via GROUP BY, not a filter, so it can't be served by a containment index; the real fix for 6513 is to maintain a materialized/normalized genre-count source. Net: keep Artist/Album.genres JSON as the live cache, add the GIN index for the two filter paths (748, 6809), and treat the TrackGenre migration as a larger, separately-validated change that requires building the missing population path first.

**Safety / pitfalls.** The finding's PREFERRED fix (drive genre filtering off track.trackGenres.some({ genre: { name: { in: [...] } } })) would BREAK behavior in production. The normalized TrackGenre/Genre relation has NO production writer: the only code in the entire repo that inserts TrackGenre rows is backend/scripts/measure-playlist-diversity-baseline.ts (a synthetic benchmark/seed script). The production enrichment worker (backend/src/workers/artistEnrichment.ts:348) writes genres ONLY to the Artist.genres JSON column (from Last.fm tags). So against a real library the TrackGenre table is effectively empty, and migrating the hot paths to it would return empty/wrong results. (The existing trackGenres-based fallbacks at library.ts:7891, recommendations.ts:444/494, subsonic.ts, programmaticPlaylists.ts appear to already be silently dead in prod for this reason.) Two further semantic mismatches: (1) the current raw-SQL paths match at the ARTIST level (Artist->Album->Track), whereas TrackGenre is per-track, so candidate sets differ; (2) the current LIKE '%g%' is SUBSTRING matching ("rock" matches "indie rock"/"classic rock"), while the proposed @> containment / { in: [...] } is exact-token, narrowing results. Also: contract says NO raw SQL but these $queryRaw calls already exist and use Prisma.sql parameterization, so they are not new violations, and the @> fallback would still be raw SQL.

---

### F17 — API runtime transpiles TypeScript on every cold start via tsx and ships the full TS toolchain in the production image

**⬜ open** · dimension: performance · severity: medium · effort: M · risk: low · epic: #19

**Files:** `backend/Dockerfile:84`, `backend/Dockerfile:111`, `backend/Dockerfile:73`

**Problem.** The api-runtime stage copies `src ./src` (not the compiled dist) and runs `CMD ["npx", "tsx", "src/index.ts"]`, keeping full node_modules (typescript + tsx) and transpiling TS in-process at boot — while the worker-runtime stage correctly ships compiled `dist/worker.js` with pruned prod deps. The build stage already runs `npm run build` (tsc), so compiled output exists and is being discarded for the API. This costs a larger image, slower/heavier cold starts (relevant under k8s/Flux rolling updates with maxUnavailable:0), higher memory, and ships a compiler into production.

**Recommendation.** Core claim is accurate and worth fixing: backend/Dockerfile api-runtime (lines 73-111) is the inconsistent outlier — it COPYs `src ./src` (line 89) plus the full unpruned `deps` node*modules including typescript+tsx (line 85), and boots via CMD npx tsx src/index.ts (line 111), transpiling TS on every cold start and shipping a compiler to prod. The worker-runtime stage (lines 44-70: dist/worker.js + worker-deps pruned node_modules) and the root AIO Dockerfile (lines 176-183, 270: npm run build, npm install -g prisma@<ver>, npm prune --omit=dev, then node dist/index.js) BOTH already do it right — the AIO is exactly the precedent the proposal cites and proves the pattern works. So adopt the AIO approach for the standalone api-runtime, but with the gaps closed: (a) build dist from the build stage and prune dev deps; (b) keep the Prisma CLI for `npx prisma migrate deploy` (docker-entrypoint.sh:46) — AIO does this via `npm install -g prisma@$(node -p ...version)` before prune, mirror that; (c) MANDATORY: either still COPY `src` into the image so swagger-jsdoc's ./src/routes/*.ts glob keeps working, OR retarget swagger.ts apis to ./dist/routes/\_.js (since comments are preserved by default) and validate /api-docs renders — pick one and verify; (d) MANDATORY: explicitly COPY backend/src/config/vibe-vocabulary.json into the runtime image (it is not emitted by tsc); (e) add a CHANGELOG.md entry per AGENTS.md. Before merging, build the image and smoke-test: GET /api-docs returns a populated spec, and vibe/vocabulary-dependent endpoints load the vocabulary. Without (c) and (d) this is not a behavior-preserving change.

**Safety / pitfalls.** The proposal's "Dockerfile-only, no application-code impact" framing is WRONG and the naive version breaks runtime behavior. (1) Swagger: backend/src/config/swagger.ts:105 sets apis: ["./src/routes/*.ts", "./src/config/swaggerSchemas.ts"]. swagger-jsdoc reads these source files at runtime (relative to CWD /app) to build the OpenAPI spec from @openapi JSDoc across 45 route files. If api-runtime stops copying `src ./src` (Dockerfile:89) and runs node dist/index.js, the glob matches nothing and the spec served at /api-docs (src/index.ts:389-398) becomes empty. tsc preserves comments by default (no removeComments in tsconfig), so the JSDoc survives into dist/_.js, but the hardcoded glob still points at ./src/_.ts, not ./dist/_.js. Fixing this requires either keeping `src` in the image OR editing swagger.ts to point at ./dist/routes/_.js (an application-code change) OR pre-generating the spec at build time. (2) Non-TS asset: src/config/vibe-vocabulary.json is loaded at runtime via readFileSync (src/services/vibeVocabulary.ts:37); tsc does NOT copy .json into dist, and the loader's only fallback path (line 38) is ../../src/config/vibe-vocabulary.json — which also disappears if `src` is dropped, silently degrading vibe search. Any dist-only switch must explicitly COPY this JSON. (3) Note ./src/config/swaggerSchemas.ts referenced by the glob does not exist in the repo, so that entry is already a no-op, but the 45 route files are real. (4) Contract: CHANGELOG.md exists at repo root and AGENTS.md requires keeping it updated — must add an entry.

---

### F18 — Soulseek downloads buffer whole files into heap; cleanup uses sync fs and audio streaming uses raw pipe() instead of pipeline()

**🟡 partial (PR #21)** · dimension: performance · severity: medium · effort: M · risk: low · epic: #17

> **Test backfill** (`feat/wave1-tdd-backfill`). Fixed the tautological "file not written" test the audit §3.1 flagged: it set `existsSync=false` but the code uses `await stat()`, so it passed only because `mockStat` returned `undefined` and reading `.size` threw a swallowed `TypeError`. `mockStat` now **rejects** (ENOENT), exercising the real not-found branch, and the test asserts `stat` was actually called. (Still 🟡: the whole-file-into-heap headline is deferred — the vendored slsk-client buffers regardless.)

**Files:** `backend/src/services/soulseek.ts:831`, `backend/src/services/soulseek.ts:849`, `backend/src/services/audioStreaming.ts:494`, `backend/src/services/audioStreaming.ts:510`

**Problem.** slsk-client's download() delivers the complete file as a single Buffer, so large FLAC/lossless downloads are held entirely in heap before being written, and concurrent downloads multiply this with no byte-budget cap (only a count cap via p-queue). Timed-out partials are cleaned with `fs.existsSync` + `fs.unlinkSync` (soulseek.ts:849) — synchronous fs blocking the event loop on every timeout. Separately, audioStreaming.streamFileWithRangeSupport pipes with raw `stream.pipe(res)` (line 510) and hand-rolls error/close handlers; pipe() does not propagate teardown/backpressure/errors from res back to the source on client abort the way stream.pipeline does — the manual res.on('close') destroy is the only thing covering it.

**Recommendation.** Confirmed in code: (a) audioStreaming.ts:510 uses raw `stream.pipe(res)` with a manual `res.on("close")` destroy and a now-dead `!res.headersSent` 500 branch; (b) soulseek.ts:849-855 uses sync fs.existsSync+fs.unlinkSync in the timeout handler (and success path at 902-903 also uses existsSync/statSync sync); (c) the whole-file-into-heap claim is real but at the LIBRARY level — slsk-client's download-peer-file.js accumulates the entire file via Buffer.concat and only fs.writeFile's it on socket close; no byte-budget cap exists, only p-queue concurrency:4 (soulseek.ts:1118). RECOMMEND: Items (a) and (b) are the correct smallest-safe wins — apply them. For (a): `await pipeline(stream, res)` from node:stream/promises, delete the manual res.on("close")/stream.on("error") handlers, and in the catch ignore ERR_STREAM_PREMATURE_CLOSE (client seek/abort) and only send 500 when !res.headersSent (which now means a pre-stream failure). For (b): `await fs.promises.rm(destPath, { force: true })` (drops the existsSync check entirely) in the timeout handler, and convert the success-path existsSync/statSync to await fs.promises.stat with a try/catch. REJECT item (c) as written: do NOT switch to slsk-client@1.1.0's downloadStream — verified it still buffers the full file, so it yields no memory benefit. The only real memory mitigations are (i) cap concurrent downloads by total expected bytes (sum match.size against a budget) instead of/in addition to count, or (ii) vendor-patch the library (patch-package) to fs.createWriteStream incrementally and drop the Buffer.concat. Recommend (i) as the in-repo fix; treat (ii)/library replacement as a separate larger task, not a mechanical change.

**Safety / pitfalls.** Two real concerns. (1) The finding's proposed fix (c) — "switch to slsk-client's streaming download API to relieve heap" — does NOT work for the installed version (slsk-client@1.1.0, ^1.1.0 in backend/package.json). I read node_modules/slsk-client/lib/peer/download-peer-file.js: even in stream mode it pushes chunks to the stream (lines 51-53) but STILL runs `buf = Buffer.concat([buf, data])` unconditionally on every chunk (line 55), then on socket close sets `down.buffer = buf` and `fs.writeFile(filePath, buf)` (lines 79-82). So downloadStream (slsk-client.js:179-184, which calls `this.download(obj, null, s)`) buffers the full file in heap regardless. Adopting the library's stream API gives zero heap savings; the only genuine memory fixes are forking/patching the vendored library or the byte-budget cap fallback. Recommending the stream API as-is would be a no-op that looks like a fix. (2) For the audioStreaming pipeline() swap: headers are always flushed before piping (res.set/res.status at lines 485-491), so on a mid-stream source error pipeline() will reject after headers are sent — the catch's `if (!res.headersSent) res.status(500).end()` branch is already effectively dead and pipeline() will destroy res itself; do NOT try to set a status after pipeline rejects (would throw ERR_HTTP_HEADERS_SENT). Keep the outer try/catch only for the pre-stream stat/parse failures, and swallow client-abort errors (ERR_STREAM_PREMATURE_CLOSE) from pipeline so they don't log as errors on normal seek/skip. No AGENTS.md/contract violations (no raw SQL, no new env, uses existing logger).

---

### F19 — Discover processor swallows real failures (returns instead of throws), so Bull never retries, dead-letters, or alerts background discovery

**✅ complete (PR #21)** · dimension: idempotency · severity: high · effort: S · risk: low · epic: #13

**Files:** `backend/src/workers/processors/discoverProcessor.ts:177`, `backend/src/workers/processors/discoverProcessor.ts:184`, `backend/src/workers/discoverCron.ts:51`, `backend/src/routes/discover.ts:181`

**Problem.** processDiscoverWeekly wraps the whole generation in try/catch and on any genuine error returns { success: false, error } (discoverProcessor.ts:184) instead of re-throwing. Bull only marks a job failed (and thus eligible for attempts/backoff, the failed set, and the 'failed' event) when the handler THROWS; a returned value resolves the job as COMPLETED. So a failed weekly generation (Last.fm down, no seeds, DB error) is silently recorded as a success, never retries, and never surfaces in @bull-board or the 'failed' listener. The lock-skip branch correctly returns success:true (skipped), but real errors are indistinguishable from that to Bull. Compounding this, the enqueue sites set no attempts/backoff at all (discoverCron.ts:51 and discover.ts:181 pass only { jobId }), so even if errors were thrown there would be no retry policy.

**Recommendation.** Accept. In processDiscoverWeekly's catch (discoverProcessor.ts:177-189): after logging the structured error, re-throw (throw error) instead of returning {success:false}. Keep the lock-skip branch (133-144) returning a resolved value; optionally relabel it {skipped:true} (drop the misleading success:true). Add retry policy at all three enqueue sites: discoverCron.ts:51, discover.ts:181, and discover.ts:217 — add { attempts:3, backoff:{ type:'exponential', delay:60000 } } alongside the existing jobId. Cleaner alternative: set defaultJobOptions:{ attempts:3, backoff:{ type:'exponential', delay:60000 } } once on the discoverQueue in queues.ts:72-75 so all current and future enqueues inherit it (avoids drift across the three call sites). Also update: discoverProcessor.test.ts:174-194 to assert rejects.toThrow; repoint the index.ts:1011-1013 completed-handler else branch to the skipped case (DiscoverJobResult.error becomes unused on the failure path and can be removed from the interface). No raw SQL or config/api-boundary contract is touched.

**Safety / pitfalls.** Low, with two implementer caveats. (1) Re-throwing must distinguish the lock-skip case (line 133-144, returns {success:true,skipped:true}) from genuine errors — only the catch block (177-189) should throw; keep the skip return as a resolved value. The finding states this correctly. (2) Blanket re-throwing on a caught error is provably safe because BOTH services only ever return success:true or throw — verified discoverWeekly.ts:526-536 (catch re-throws, never returns success:false) and discoveryRecommendations.ts:575-688 (throws on !enabled and propagates errors). So there is no legitimate non-throwing success:false business outcome (e.g. 'no seeds' actually throws), meaning retries won't be triggered on a benign 'nothing to recommend' result. The only required collateral edits: existing unit test discoverProcessor.test.ts:174-194 asserts the current swallow behavior and must be rewritten to expect a throw; the completed-listener else branch at index.ts:1011-1013 (logs result.error) becomes dead for real errors and should be repointed to the skipped case; and the status endpoint discover.ts:253-269 will return state:'failed' with returnvalue:undefined on failure (failure reason moves to job.failedReason) — a beneficial change but a frontend-facing nuance to confirm. attempts:3 + exponential backoff is fine but note retries re-run the full generation under the existing Redis lock (45min TTL, released in finally), so no double-run risk; with maxStalledCount:1 already set, total attempts interplay is acceptable.

---

### F20 — Discover Weekly batch creation has no per-(user, week) idempotency guard — double-trigger duplicates the whole batch and its Lidarr downloads

**⬜ open** · dimension: idempotency · severity: high→medium · effort: M · risk: low · epic: #13

> **Audit note.** ✓ Severity high→medium: the missing guard is confined to the deprecated, non-default `DISCOVERY_MODE=legacy` path. The legacy route is frozen and will be removed with the mode, so this duplicate-retry hazard remains intentionally unfixed. NOTE: F19's shipped queue-wide `attempts:3` retry makes the hazard load-bearing — a legacy retry can duplicate the batch and re-trigger Lidarr downloads.

**Files:** `backend/src/services/discoverWeekly.ts:329`, `backend/src/services/discoverWeekly.ts:331`, `backend/src/routes/discover/legacy/generation.ts`, `backend/prisma/schema.prisma`

**Problem.** generatePlaylist computes weekStart (discoverWeekly.ts:205) but the service NEVER checks for an existing DiscoveryBatch for (userId, weekStart) before tx.discoveryBatch.create (line 331) — a confirmed grep over lines 196-330 found no findFirst/findUnique/upsert on discoveryBatch. The only existence guard is a 409 in the frozen legacy HTTP handler (`backend/src/routes/discover/legacy/generation.ts`), which does NOT protect the cron path, its legacy `discoverQueue.add({ userId })` call (no jobId, so no Bull dedup either), a Bull retry, or a double-fired job. Two batches in the same week double the album set sent to Lidarr, doubling bandwidth and download quota. DiscoveryBatch has only @@index([userId, weekStart]) — no @@unique — so even concurrent creates aren't blocked at the DB.

**Recommendation.** Do not implement the earlier fix proposal. The maintainer lifecycle decision freezes legacy discovery without further fixes and removes this hazard with the mode. The default `recommendation` path is already idempotent. Historical analysis: the cron path is deduped via a deterministic week-scoped Bull jobId `discover:cron:${weekKey}:${userId}`; a per-user Redis SET NX lock serializes concurrent jobs; and per-album downloadJob.findFirst dedup prevents re-downloading albums with an in-flight job. The residual gap is a COMPLETED legacy batch re-triggered in the same week by the unnamed manual job or a Bull retry.

**Safety / pitfalls.** Adding @@unique([userId, weekStart]) to DiscoveryBatch is NOT safe as a plain migration: legacy mode's frozen HTTP handler and a fully-completed-then-re-triggered batch can legitimately produce a second row in the same week today, so the existing prod table may already contain duplicate (userId, weekStart) rows — the migration would fail mid-deploy unless preceded by a dedup/backfill step. A status-scoped partial unique index (active batches only) is safer but requires raw SQL via a Prisma migration, which sits at the edge of the AGENTS.md \"no raw SQL\" contract (migrations are the accepted exception, but call it out). Switching the bare create to an upsert on (userId, weekStart) would also silently overwrite/merge a prior week's batch logs/counters if the unique key collides with an older completed batch, changing observable history. Note also the default code path (DISCOVERY_MODE=recommendation) does not use DiscoveryBatch at all and is already week-idempotent, so this change only affects the deprecated legacy path.

---

### F21 — Audio-analysis / vibe re-queue enqueues BEFORE flipping status and has no per-track dedup — crash window and duplicate sidecar work

**⬜ open** · dimension: idempotency · severity: high · effort: M · risk: medium · epic: #13

**Files:** `backend/src/workers/unifiedEnrichment.ts:1336`, `backend/src/workers/unifiedEnrichment.ts:1350`, `backend/src/workers/unifiedEnrichment.ts:1402`, `backend/src/workers/unifiedEnrichment.ts:1414`

**Problem.** queueAudioAnalysis RPUSHes the track onto the plain Redis list 'audio:analysis:queue' (lines 1336-1347) and ONLY THEN sets Track.analysisStatus='processing' (line 1350). A crash/restart between the RPUSH and the Prisma update leaves the track enqueued but still 'pending', so the next enrichment cycle re-selects and re-RPUSHes it — duplicate analysis work in the Python sidecar. The Redis list has no per-track dedup (unlike a Bull jobId), so a track can also be RPUSHed twice within back-to-back cycles. queueVibeEmbeddings has the identical enqueue-then-update ordering for 'audio:clap:queue' (lines 1402-1421). The cycle-level claim lock serializes cycles but does not close this enqueue/status gap.

**Recommendation.** The enqueue-then-update ordering and absence of per-track Redis dedup are genuinely present at unifiedEnrichment.ts:1336-1356 (audio) and 1402-1421 (vibe); the cycle claim lock (lines 348-398) serializes cycles but does not close the per-track crash gap. However, the finding overstates real-world impact for the AUDIO path: the Python consumer already guards with `UPDATE ... WHERE analysisStatus IN ('pending','processing') RETURNING id` and drops stale duplicates (analyzer.py:1738-1763), and it runs its OWN claim-then-enqueue reconciliation (analyzer.py:1521-1577) that is exactly the pattern proposed here. So a crash-window duplicate on audio:analysis:queue mostly gets filtered at the consumer (residual risk: two still-'pending' duplicates drained in the same batch are analyzed twice; results upsert so no corruption). The VIBE/CLAP path is the stronger case: the CLAP consumer (clap analyzer.py:416-458) has NO status guard and computes an embedding for whatever it pops, so a duplicate RPUSH to audio:clap:queue genuinely wastes CLAP compute — though `_store_embedding` upserts via `ON CONFLICT (track_id) DO UPDATE` (clap analyzer.py:533-536) and the producer's LEFT JOIN excludes already-embedded tracks, so again no data corruption. Net: low-to-moderate idempotency/efficiency bug, NOT high severity; corruption is impossible and the audio path is largely self-healing. Recommended smallest-safe-change: reorder both producers to claim-then-enqueue using a conditional updateMany (audio: `where: { id: { in }, analysisStatus: 'pending' }`; vibe: `where: { id: { in }, vibeAnalysisStatus: { in: ['pending'] } } OR null` — get the nullability right per field), re-select the actually-transitioned ids, RPUSH only those, and preserve the `queued` count semantics used by enrichmentState. Do NOT bother with RPUSH-failure rollback (stale recovery covers it). The longer-term Bull-jobId=trackId dedup is the right direction but is a larger change and out of scope for smallest-safe-change. Note the producer also uses $queryRaw (lines 1379-1391) which violates the AGENTS.md no-raw-SQL contract — flag separately, not part of this fix.

**Safety / pitfalls.** The proposed updateMany WHERE clause is partly wrong against the actual schema and would silently fail to claim rows. backend/prisma/schema.prisma:345 declares `analysisStatus String @default("pending")` — it is NON-nullable, so `analysisStatus: { in: ['pending', null] }` is incorrect for the audio path (null never occurs; the existing producer correctly filters only `analysisStatus: "pending"`). Only `vibeAnalysisStatus` is nullable (schema.prisma:358, `String?`), and there the existing producer already matches `IS NULL OR = 'pending'`, so the claim for the vibe path must be `{ in: ['pending'] }` OR `null`, not a single shared clause. A blind copy of the proposed WHERE risks claiming zero rows. Second concern: the producer's current per-track try/catch counts a track as queued only after BOTH redis+prisma succeed; switching to a batch updateMany+re-select+batch RPUSH changes the failure granularity (one redis hiccup now affects the whole claimed batch) and the `queued` return value semantics that drive enrichmentState progress reporting (lines 1364, 1755-1769) — must be preserved. The 'RPUSH failure -> status rollback' idea opens its own crash window (claim ok, rpush fails, rollback fails => row stuck 'processing'), but that is net-neutral because the 15-min stale recovery already re-pends it.

---

### F22 — Helm Secret regenerates SESSION_SECRET / SETTINGS_ENCRYPTION_KEY / POSTGRES_PASSWORD on every upgrade — breaks sessions, makes stored credentials undecryptable, desyncs the Postgres password

**✅ complete** (`feat/f22-helm-stable-secrets`) · dimension: idempotency · severity: high · effort: M · risk: low · epic: #11

> **Audit note.** ✓ Omits a 4th regenerated key: the Helm template also defaults `INTERNAL_API_SECRET` via `randAlphaNum` (`secret.yaml:12`), so it rotates on every upgrade too. Fold it into the same lookup-stability fix.
>
> **Fix shipped** (#11 Phase 0). `secret.yaml` now reuses the live in-cluster Secret via `lookup` for all **four** keys (SESSION_SECRET, SETTINGS_ENCRYPTION_KEY, INTERNAL_API_SECRET, POSTGRES_PASSWORD); per-key precedence is explicit `secrets.*` → existing Secret → generated. Non-destructive and additive; installs with `existingSecret`/pinned values are unchanged. `helm-chart-render-check.sh` guards the client-renderable paths (4 keys present, explicit honored, existingSecret suppresses); the server-side lookup-reuse path is verified out-of-band per `docs/UPGRADING.md` (operators ship that note).

**Files:** `charts/soundspan/templates/secret.yaml:10`, `charts/soundspan/templates/secret.yaml:11`, `charts/soundspan/templates/secret.yaml:14`

**Problem.** When secrets.existingSecret is empty, the Secret template fills blank fields with randAlphaNum at render time and has NO lookup guard. Helm has no first-install memory, so every helm upgrade re-renders fresh random values: SESSION_SECRET rotates (invalidating all JWT/refresh sessions), SETTINGS_ENCRYPTION_KEY rotates (all AES-encrypted Lidarr/2FA/OAuth/Subsonic secrets become permanently undecryptable — and encryption.ts fails open, so they may silently round-trip as garbage), and POSTGRES_PASSWORD rotates against an already-initialized PG data dir (auth failures). This is a reliability/idempotency bug in the deploy path that can brick a running install on a routine upgrade.

**Recommendation.** Confirmed and high-severity for the individual deployment mode. secret.yaml lines 10/11/14 use `{{ .Values.x | default (randAlphaNum N) }}` with no lookup guard; Helm re-renders fresh randoms on every `helm upgrade`. All three keys are wired into pods via secretKeyRef (backend.yaml ~280-351, aio/deployment.yaml ~85-89, postgresql.yaml ~50-71) and Postgres uses a PVC, so the desync is real. SESSION_SECRET is the JWT signing/verify key (middleware/auth.ts line 7: JWT_SECRET || SESSION_SECRET) — rotating it invalidates all issued JWTs (AIO + individual). Two corrections to the teammate's writeup: (a) The "encryption.ts fails open / silently round-trips as garbage" claim is WRONG. decrypt() at utils/encryption.ts lines 80-88 specifically re-throws ERR_OSSL_BAD_DECRYPT (the exact error a wrong-key AES-256-CBC decrypt produces via PKCS7 padding failure), so rotating SETTINGS_ENCRYPTION_KEY makes stored Lidarr/2FA/OAuth/Subsonic secrets throw loudly, not silently corrupt — arguably worse for availability but not "silent garbage." (b) POSTGRES_PASSWORD desync applies only to individual/external-PG mode; AIO hardcodes soundspan:soundspan. Apply the lookup guard to all three generated keys, but use a proper emptiness check (`{{- $existing := (lookup "v1" "Secret" .Release.Namespace (include "soundspan.fullname" .)) }}` then `{{- if and $existing (index $existing.data "SESSION_SECRET") }}{{ index $existing.data "SESSION_SECRET" | b64dec }}{{- else }}{{ .Values.secrets.sessionSecret | default (randAlphaNum 64) }}{{- end }}`) to avoid zeroing secrets when the key is absent or during client-side/dry-run renders. Also fix the misleading values.yaml comments at lines 597 and 606 that currently claim auto-generation happens "on first install" — without the guard that is false. Given the operator uses Flux/GitOps (lookup may render empty server-side-less), the primary recommendation remains: strongly steer production to secrets.existingSecret (SOPS/sealed-secrets), with the lookup guard as defense-in-depth for the auto-generate path. Quick repro to confirm: `helm template` the chart twice and diff the rendered Secret — the three values change every render.

**Safety / pitfalls.** The proposed lookup-based fix is the standard Helm idiom and targets the correct Secret name (the chart-managed Secret is named include "soundspan.fullname", exactly what `lookup "v1" "Secret" .Release.Namespace (include "soundspan.fullname" .)` resolves to via soundspan.secretName in \_helpers.tpl). Two real pitfalls to call out: (1) `lookup` returns an empty dict during `helm template`, `helm install --dry-run` (without --dry-run=server), and any client-side rendering — so the guard must use a nil/emptiness check (e.g. `if and $existing (index $existing.data "SESSION_SECRET")`) and only then `b64dec`; calling `index .data ... | b64dec` on a missing key yields an empty string and would silently zero out the secret, and indexing a nil $existing errors. (2) GitOps/Flux deploys (this user runs Flux per their memory) often render server-side or via `helm template` piped to the cluster — if the tool renders without live cluster access, lookup still returns empty and the value regenerates anyway, so the lookup guard is necessary but NOT sufficient. The genuinely robust path is steering operators to secrets.existingSecret (SOPS/sealed-secrets), which the chart already supports. No AGENTS.md contract violation (Helm templates, not app code). Note: AIO mode hardcodes postgres creds to soundspan:soundspan in \_helpers.tpl line 81 and aio deployment, so the POSTGRES_PASSWORD-desync sub-claim only applies to individual mode with chart-managed/external PG, not AIO.

---

### F23 — Download dedup rides on a JSON-path findFirst plus a 5-strategy heuristic cascade instead of DB constraints — best-effort idempotency under load

**🟡 partial (PR #116)** · dimension: idempotency · severity: high→medium · effort: M · risk: medium · epic: #13

> **Audit note.** ✓ Severity high→medium: the 'no partial-unique' headline is refuted by migration `20260118000000` (the body says so). Real residual = a missing P2002 catch (a 500, not data corruption) + a seq scan.
>
> **Fix shipped** (recommendation (a) only). `onDownloadGrabbed` (`simpleDownloadManager.ts`) now catches P2002 from both the matched-job update and the tracking-job create — the two writes that race the partial-unique index `DownloadJob_targetMbid_active_unique`. The catch is wired via `.catch()` on the _outside_ of the `withTransaction(...)` call, not a try/catch inside its callback: Prisma's interactive `$transaction` rolls back and rethrows on a callback rejection, so the `tx` client is already dead by the time recovery runs — re-querying with it would fail with "current transaction is aborted, commands ignored until end of transaction block". The recovery re-find runs against the plain `prisma` singleton (`lidarrRef` OR `metadata.downloadId` match, mirroring Step 1's existing idempotency check) and returns the winner's jobId; if no winner is discoverable (e.g. a different downloadId racing on the same targetMbid — out of this slice's scope) it falls back to the pre-existing non-throwing `{matched:false}` shape rather than fabricating a match. **Still 🟡**: recommendation (b) — backfilling `metadata.downloadId` into the indexed `lidarrRef` column and replacing the Step-1 JSON-path idempotency lookup with an indexed one — is deferred to a later slice. No new `@unique`/`@@unique` or migration was added, per the safety note below (a global unique on `lidarrRef` would 500 normal operation, since download-client ids legitimately repeat). Tests: `simpleDownloadManager.test.ts` (create-path P2002 race, matched-update-path P2002 race, no-winner-found non-throwing fallback, non-P2002 errors still reject) plus a new `webhooksGrabConcurrency.test.ts` proving the HTTP layer now returns an idempotent 2xx instead of 500 for the second (racing) Grab webhook.

**Files:** `backend/src/services/simpleDownloadManager.ts:374`, `backend/src/services/simpleDownloadManager.ts:415`, `backend/src/services/simpleDownloadManager.ts:471`, `backend/prisma/schema.prisma`

**Problem.** onDownloadGrabbed's idempotency check is tx.downloadJob.findFirst({ where:{ metadata:{ path:['downloadId'], equals: downloadId } } }) (simpleDownloadManager.ts:374) — a JSON-path filter that cannot use a btree index — followed by an in-memory five-strategy match cascade (targetMbid, metadata.lidarrMbid, lidarrAlbumId, normalized artist+album, subject substring; lines 415-466). The load-bearing dedup key (lidarr downloadId) is written into metadata.downloadId AND into the real lidarrRef column on match (line 480), but DownloadJob has @@index([lidarrRef]) without @@unique, and there is no partial-unique on active (userId, targetMbid). So two concurrent Grab webhooks for the same album, or a webhook racing the queue-cleaner's reconcile, can each pass the findFirst and both match/create; the Serializable transaction narrows but does not close the race. The downloads.ts route already solved exactly this for user-initiated jobs with FOR UPDATE SKIP LOCKED (downloads.ts:268-285) — the pattern exists but isn't applied to the webhook path.

**Recommendation.** REFRAME: the finding's headline premise ("no partial-unique on active targetMbid") is factually WRONG — `DownloadJob_targetMbid_active_unique` (UNIQUE on targetMbid WHERE status IN pending/processing) already exists (migration backend/prisma/migrations/20260118000000_add_partial_unique_index_active_downloads/migration.sql). So the duplicate-active-job race the finding fears is ALREADY closed at the DB layer for any grab with a non-empty albumMbid. The genuine residual problems are narrower: (1) onDownloadGrabbed's create (simpleDownloadManager.ts:567) does NOT catch the P2002 from that existing index, so a concurrent duplicate grab surfaces as an uncaught 500 at webhooks.ts:152 instead of a graceful idempotent response — this is the real, in-code defect worth fixing; (2) the idempotency-replay check at line 374 is a genuinely unindexed JSON-path seq scan (confirmed: no GIN/expression index on DownloadJob.metadata in any migration) over potentially the whole table on every webhook — a performance/readability smell; and (3) the downloadId-level dedup key lives in metadata.downloadId, not in an indexed column, despite lidarrRef being written with the same value. RECOMMEND: (a) add a small P2002 catch around the create (and the matched update) that re-runs the lidarrRef/metadata lookup and returns the winning jobId — this closes the only true uncaught-race gap and is the smallest-safe-change; (b) backfill metadata.downloadId into the indexed lidarrRef column where missing, then replace the line-374 findFirst with an indexed `where: { lidarrRef: downloadId }` lookup (keep the metadata path only as a transitional fallback) — converts the seq scan into a btree hit using the EXISTING @@index([lidarrRef]); (c) do NOT add a global @unique on lidarrRef and do NOT add a redundant (userId,targetMbid) partial-unique — first verify lidarrRef-sharing semantics; if it is provably per-job-unique-when-set, a PARTIAL unique on lidarrRef WHERE lidarrRef IS NOT NULL is the correct scoping. The FOR UPDATE SKIP LOCKED port from downloads.ts is unnecessary given the existing partial-unique + a P2002 catch.

**Safety / pitfalls.** The proposed `@unique` on `lidarrRef` (the Lidarr downloadId) is UNSAFE as stated. In `onDownloadComplete` (simpleDownloadManager.ts:711-740, "Find and Mark Duplicates Complete") and in the bulk/retry logic, multiple DownloadJob rows legitimately carry related download references, and the grab path can REUSE/overwrite lidarrRef across retries (releaseIndex/triedReleases retry model, schema lines 666-667). Critically, `handleGrab` passes `downloadId` that can repeat, and SABnzbd/Lidarr emit grabs where two albums share a download client item — a hard global @unique on lidarrRef would throw P2002 and 500 the webhook in normal operation, not just on races. It must at most be a PARTIAL unique (lidarrRef WHERE lidarrRef IS NOT NULL AND status active), and even then needs verification that lidarrRef is never intentionally shared. Second concern: the finding's proposed partial-unique on (userId, targetMbid) WHERE status active is REDUNDANT/conflicting with the ALREADY-EXISTING `DownloadJob_targetMbid_active_unique` (targetMbid-only, no userId) from migration 20260118000000 — adding a (userId,targetMbid) variant would NOT subsume the stricter existing global-targetMbid one and could mask the fact that the real gap is just the missing P2002 catch. Third: switching the grabbed-tracking create to rely on catch(P2002) changes the current behavior where the loser returns {matched:false} silently — callers (webhooks.ts:185 queueCleaner.start, and onDownloadComplete batch logic) must still get a coherent jobId, so the catch must re-find and return the winner's id, not just swallow. Contract note: raw migration for partial-unique is explicitly blessed by the repo's own precedent (the existing 20260118 migration documents the Prisma limitation), so that part does not violate AGENTS.md.

---

### F24 — High-volume scan jobs enqueued from ~15 sites with no coalescing jobId, no attempts/backoff, and no removeOnComplete — stacked full scans + unbounded Redis growth

**🟡 partial (PR #21)** · dimension: idempotency · severity: high→medium · effort: M · risk: low · epic: #13

> **Audit note.** ✓ PARTIAL: only the 'safe half' (removeOnComplete/Fail) shipped; coalescing `jobId` + attempts/backoff deferred to BullMQ (#19). Severity high→medium (bounded Redis leak).
>
> **Test backfill** (`feat/wave1-tdd-backfill`). `queues.test.ts` now asserts the `library-scan` queue's `defaultJobOptions` (`removeOnComplete:100` / `removeOnFail:200`) — the missing regression guard the audit §3.1 flagged. (Still 🟡: the coalescing/backoff half is unbuilt.)

**Files:** `backend/src/routes/webhooks.ts:233`, `backend/src/routes/webhooks.ts:246`, `backend/src/routes/library.ts:1274`, `backend/src/jobs/queueCleaner.ts:360`, `backend/src/workers/queues.ts:67`

**Problem.** scanQueue is created with only stalled-job settings and NO defaultJobOptions (queues.ts:67). Across ~15 callsites (webhooks Grab/Download/external, library trigger, youtube, downloads, discover, spotifyImport, queueCleaner-recovery, notifications) scanQueue.add('scan', ...) is called with NO jobId (no dedup), NO attempts/backoff, and NO removeOnComplete/removeOnFail. The removeOn options found in the codebase (workers/index.ts:665-826) are all on the repeatable SCHEDULER jobs, not on scan. processScan runs a full MusicScannerService.scanLibrary with default concurrency 1 and no in-flight guard, so a Lidarr webhook burst enqueues many full scans that run back-to-back, and every completed scan job is retained in Redis forever, growing the keyspace unbounded.

**Recommendation.** The core defects are real and present now: queues.ts:67 has no defaultJobOptions; scanQueue.process('scan', processScan) at workers/index.ts:840 has no concurrency arg (Bull default 1); MusicScannerService.scanLibrary (musicScanner.ts:110) has no in-flight guard; and the removeOnComplete/removeOnFail options in index.ts:660-826 are on scheduler jobs only. So most scan callers retain completed jobs in Redis forever. BUT the finding overstates: it is NOT true that all ~15 callsites pass no removeOn options — subsonic.ts:4159 (removeOnComplete:true, removeOnFail:50 + a real debounce guard), notifications.ts:682, and playlists.ts:1634 all pass removeOnComplete:true. ADOPT the safe high-value half: add defaultJobOptions:{ removeOnComplete:100, removeOnFail:200 } on scanQueue so the ~12 option-less callers stop growing Redis unbounded (per-call opts still override). DROP the blanket attempts/backoff — full-library rescans on retry are costly and side-effect-bearing; if retries are wanted, scope them per-call to idempotent callers only. DO NOT apply a single static coalescing jobId globally — it would drop discoveryBatchId/spotifyImportJobId/downloadId payloads and break completion tracking. If coalescing is desired, limit it to the fungible type:'full', source-only callers (queue-cleaner-recovery webhooks.ts:246, download-queue downloadQueue.ts:516) and prefer the existing subsonic-style app-level hasPendingScan guard over jobId semantics. Note: a per-album 'scan:album:${mbid}' jobId gives no work savings because processScan rescans the whole library regardless of payload. Also update CHANGELOG.md per AGENTS.md.

**Safety / pitfalls.** Three concerns. (1) Blanket attempts:3 + exponential backoff is unsafe for this queue: processScan ALWAYS runs a full MusicScannerService.scanLibrary (it ignores type/paths/fullScan), so an auto-retry re-scans the entire library 3x — amplifying the exact load the finding wants to reduce. Several callers have completion-keyed side effects (download-job status updates in scanProcessor.ts:225+, artist enrichment, discovery-batch playlist build via discoveryBatchId, spotifyImport status). (2) A single static coalescing jobId (e.g. 'scan:full') across all callers would collapse jobs carrying DISTINCT payloads (discoveryBatchId, spotifyImportJobId, downloadId) into one, and Bull dedup drops the later add's payload — breaking discover-weekly batch completion (discoverWeekly.ts:854) and spotify-import completion tracking (spotifyImport.ts:1780). That is a correctness regression, not just perf. (3) subsonic.ts:4159 already has an app-level debounce (hasPendingScan via getActive/getWaiting/getDelayed + canQueueSubsonicScan) and passes removeOnFail:50; a queue default merging with its per-call opts is fine but must be verified not to weaken that guard.

---

### F25 — Two overlapping download reconcilers (unlocked 30s queueCleaner interval vs claim-locked 2-min scheduler job) race on the same DownloadJob rows

**⬜ open** · dimension: idempotency · severity: medium · effort: M · risk: medium · epic: #13

**Files:** `backend/src/jobs/queueCleaner.ts:132`, `backend/src/jobs/queueCleaner.ts:411`, `backend/src/workers/index.ts:322`

**Problem.** processReconciliationJob runs on the scheduler queue under runWithSchedulerClaim (a cross-process Redis claim lock) and calls markStaleJobsAsFailed -> reconcileWithLidarr -> reconcileWithLocalLibrary -> syncWithLidarrQueue (workers/index.ts:329-377). QueueCleanerService.runCleanup independently re-implements the SAME sequence (PART 0/0.25/0.26/0.3 at queueCleaner.ts:145-180) on its own self-scheduling setTimeout loop (queueCleaner.ts:411) with NO cross-process claim lock — it is started ad hoc from the webhook handler (webhooks.ts:187 queueCleaner.start()). Two reconcilers with different cadences and only one of them locked can concurrently mark/update the same DownloadJob rows, double-trigger scans (queueCleaner.ts:360), and double-fire batch-completion checks.

**Recommendation.** Consolidate to the claim-locked scheduler job as the single reconciler. Concretely: (1) move the cleaner's genuinely-unique behaviors (PART 1 cleanStuckDownloads+retry bump, PART 2 orphan recovery + scanQueue.add trigger, PART 0.5 checkStuckBatches) into processReconciliationJob (or stateless primitives it calls) so nothing is lost; (2) delete the PART 0/0.25/0.26/0.3 duplicate sequence and the setTimeout loop in queueCleaner.ts; (3) replace queueCleaner.start() at webhooks.ts:187 with a one-shot enqueue/trigger of the scheduler reconciliation job (or rely on the existing 2-min repeat) so the post-grab path still kicks reconciliation but under the existing cross-process claim. Keep queueCleaner.reconcileWithLocalLibrary() as the already-shared stateless primitive. Pass the snapshot consistently so behavior is uniform. If full consolidation is out of scope, the smallest-safe interim is to gate runCleanup behind runWithSchedulerClaim using a DISTINCT claim key (not the reconciliation-cycle key) so it cannot run concurrently with itself across replicas, but this still leaves two overlapping cadences and should be treated as temporary.

**Safety / pitfalls.** Outright deletion of queueCleaner.runCleanup is NOT a drop-in: the loop does work the scheduler job does NOT — PART 1 cleanStuckDownloads + retry-count bumping, PART 2 getRecentCompletedDownloads orphan recovery + the scanQueue.add("scan") trigger, PART 0.5 checkStuckBatches, and the auto-stop-after-3-empty-checks lifecycle started on the post-grab hot path (webhooks.ts:187). Deleting it without folding those unique behaviors into processReconciliationJob would regress missed-webhook recovery and post-download library-scan triggering. The "minimum" alternative (wrap runCleanup in runWithSchedulerClaim) is the riskier path: if it reused the reconciliation-cycle claim key it would starve or be starved by the 2-min cycle, and a 30s self-scheduling loop under that claim model is awkward (the in-process isRunning flag also does nothing cross-process). Note also the two paths are not equivalent: the cleaner calls the primitives with NO snapshot, which per simpleDownloadManager.ts:1348-1350 makes them skip Lidarr checks, so it is a weaker partial duplicate.

---

### F26 — Invite-code maxUses check is outside the transaction with an unconditional increment — concurrent registrations overshoot the cap

**✅ complete (PR #21)** · dimension: idempotency · severity: medium · effort: S · risk: low · epic: #13

**Files:** `backend/src/routes/auth/adminUserInvites.ts:883`, `backend/src/routes/auth/adminUserInvites.ts:846`, `backend/src/routes/auth/adminUserInvites.ts:852`

**Problem.** register() reads the invite and checks invite.useCount >= invite.maxUses (auth.ts:1104) BEFORE the prisma.$transaction (auth.ts:1130), and inside the transaction increments unconditionally with inviteCode.update({ data:{ useCount:{ increment:1 } } }) (auth.ts:1159). Two concurrent registrations against a maxUses=1 invite both read useCount=0, both pass the check, both create users, and both increment to 2 — the cap is exceeded. The downloads route already solved this exact TOCTOU class with row-level locking (downloads.ts:268), so the pattern is known in-repo but not applied here.

**Recommendation.** Confirmed exactly as described against backend/src/routes/auth/adminUserInvites.ts:833-908 and prisma/schema.prisma:85-99 (fields useCount/maxUses/revoked/expiresAt all exist; this is the ONLY invite-consumption path — line 765 is admin-revoke, line 690 is read-only listing). Default Postgres Read Committed isolation applies (only simpleDownloadManager.ts opts into Serializable), so the TOCTOU is real: concurrent maxUses=1 registrations both read useCount=0 and both increment to 2. Fix: inside prisma.$transaction, run `const consumed = await tx.inviteCode.updateMany({ where: { id: invite.id, revoked: false, useCount: { lt: invite.maxUses } }, data: { useCount: { increment: 1 } } });` as the first statement; if `consumed.count === 0` throw a sentinel error to roll back; otherwise proceed to create user/settings/inviteCodeUsage. In the catch, map the sentinel to res.status(400).json({ error: "This invite code has been fully used" }) to keep the existing contract. Optionally keep the pre-transaction revoked/expired/exists checks for precise 400 messages (the updateMany then only needs `useCount: { lt: maxUses }` plus revoked guard). No existing tests cover this path (the only "register" test hits match queue/worker registration, not user registration), so add a concurrency/TDD test asserting the cap holds. Note inviteCodeUsage.usedBy is @unique but a fresh user is created each call, so ordering introduces no extra constraint.

**Safety / pitfalls.** Low overall, but watch three things. (1) The guard must run as the FIRST write inside the transaction and a count===0 result must throw to roll back the already-created user/settings/usage rows — if you put the updateMany after tx.user.create without throwing on count===0, you'd still create the user. Cleanest is to do the conditional updateMany first, throw a typed error on count===0, then create user/settings/usage. (2) Preserve the existing 400 response with message "This invite code has been fully used" so the API contract/clients don't change; map the thrown sentinel to that 400 (the existing catch block only handles ZodError and otherwise returns 500, so add explicit handling). (3) Folding revoked/expiresAt into the updateMany WHERE is a genuine hardening but changes which 400 message a revoked/expired code returns (they'd collapse into the generic guard failure) — if distinct messages matter, keep the pre-transaction revoked/expired checks for UX and use the updateMany purely for the atomic cap. No AGENTS.md violation: the fix is pure Prisma (updateMany), and is actually MORE contract-compliant than the cited downloads.ts:268 precedent, which uses $queryRaw FOR UPDATE SKIP LOCKED in breach of the "no raw SQL" rule.

---

### F27 — TIDAL sidecar track/album downloads have no on-disk or in-flight idempotency — retries re-download bytes and re-hit the rate-limited TIDAL API

**⬜ open** · dimension: idempotency · severity: medium · effort: M · risk: low · epic: #13

**Files:** `services/tidal-downloader/app.py:277`, `services/tidal-downloader/app.py:1025`, `services/tidal-downloader/app.py:1056`

**Problem.** \_download_track_sync unconditionally calls api.get_track_stream + download_bytes and rewrites the file every invocation (app.py:306-319); download_track/download_album (app.py:1025, 1056) have no dedup or resume. Unlike the yt-dlp path (which has find_existing_download and find_active_download_job), a retried request — network blip, backend retry, or re-run of a partially-failed album — re-downloads everything and re-hits the TIDAL API, wasting bandwidth/quota and risking the very rate-limit bans the code otherwise sleeps (TIDAL_TRACK_DELAY) to avoid. The temp-file-then-move is atomic per write but does nothing to prevent redundant work.

**Recommendation.** Confirmed and reproducible. \_download*track_sync (services/tidal-downloader/app.py:277-359) has zero on-disk or in-flight idempotency: every call re-runs get_track_stream (line 306, the rate-limited TIDAL API hit), download_bytes (line 311), and rewrites the file. download_album (line 1056-1121) loops all tracks restarting from index 0 on any re-run, re-hitting the API and re-sleeping TIDAL_TRACK_DELAY (line 1088-1092) per track. The contrast with the yt-dlp path is accurate: ytmusic-streamer/app.py uses find_active_download_job (line 2551) and find_existing_download (line 2560), defined in yt_download.py:309. A concrete retry trigger exists upstream: backend/src/services/tidal.ts downloadTrack/downloadAlbum recursively retry the entire download on 401-after-refresh (lines 402, 435), and downloads.ts re-runs album downloadJobs.\n\nTighten the fix: (a) In \_download_track_sync, after building the sanitized stem (lines 293-303) but BEFORE get_track_stream, glob dest_base/{stem}.* restricted to a known audio-extension set (mirror find*existing_download's glob.escape + extension filter, excluding *.tmp) and, if a non-empty match exists, short-circuit returning the same result dict the success path returns (read file_size via stat) so callers stay unchanged. This skips both the API stream call and the byte download. (b) This automatically makes download_album resumable — partially-completed albums skip already-present tracks. (c) Add an in-flight guard only if concurrent identical requests are actually possible; key it on (user_id, track_id), register it with no await between check and registration (per the yt-dlp comment at app.py:2549), and clear it in a finally. (d) Keep the temp-file-then-move write but ensure the skip check ignores .tmp leftovers. Add a pytest mirroring services/ytmusic-streamer/tests/test_yt.py's find_existing_download cases (glob-char dir, wrong-extension, missing dir) since the codebase already has that test pattern and TDD is expected. This is medium severity (quota/bandwidth waste + ban risk), not a correctness bug — existing temp-then-move already prevents corruption.

**Safety / pitfalls.** Two real pitfalls the fix must handle. (1) The output extension is NOT known from the template alone: file_extension comes from get_track_stream/parse_track_stream (app.py:306-307), and format_template is called with with_asterisk_ext=False (app.py:298) so the template yields only a deterministic STEM, not the full path. A naive 'resolve the exact path and stat it' as the finding describes won't work pre-download without first calling get_track_stream — so to skip the rate-limited API call you must glob the stem against known audio extensions (the yt-dlp find_existing_download approach), not reconstruct one exact path. (2) The temp-then-move + FLAC ffmpeg extraction (app.py:317-331) means a crash can leave a .tmp file or a partially-written final file; an existence-only check could treat a truncated file as complete. The non-empty/size sanity-check the finding mentions mitigates this but should be kept, and the .tmp file must be excluded from the skip glob (yt-dlp's check restricts to AUDIO_EXTENSIONS for exactly this reason). The in-flight set keyed on (user_id, track_id) is sound but must mirror yt-dlp's atomicity note — register before any await; download_track/download_album run via asyncio.to_thread so a module-level set needs care if true parallel album requests can overlap. No AGENTS.md/contract violation (Python sidecar, no Prisma/raw-SQL surface); change is localized to \_download_track_sync/download_album.

---

### F28 — API keys stored in plaintext and looked up by raw value — a read-only DB leak yields working device credentials for every user

**✅ complete (code; backfill not yet run)** (`feat/f28-hash-api-keys`) · dimension: security · severity: high · effort: M · risk: medium · epic: #11

> **Audit note.** ✓ Label corrected — NOT 'forced re-issue.' The #11 migration plan hashes keys in place (HMAC) and validation is an exact-value lookup, so existing device keys keep working. The `⚠️ breaking` flag is dropped.
>
> **Fix shipped** (#11 Phase 2). Keys are stored as `hmac:<HMAC-SHA256>` (`utils/apiKeyHash.ts`). The three validate sites (auth.ts ×2, subsonicAuth.ts) use `findApiKeyRecord` — **dual-validate**: hash lookup first, raw-key fallback for not-yet-migrated rows, so existing keys keep working with **no re-issue**. The two create sites (apiKeys.ts, deviceLink.ts) store the hash and return the raw key once. The `hmac:` prefix is a self-describing migration marker (raw keys and HMACs are both 64 hex). **Backfill written but not run**: `scripts/hash-existing-api-keys.ts` (dry-run default, idempotent) + `apiKeys` section in `GET /api/admin/secrets-status`. Pepper = `API_KEY_PEPPER` → `SETTINGS_ENCRYPTION_KEY` → `SESSION_SECRET`. **Deferred:** the dedicated `API_KEY_PEPPER` is not yet added to the chart's stable-secret generation (code falls back to the F22-stabilized `SETTINGS_ENCRYPTION_KEY`); and the plaintext-lookup fallback should be removed once the backfill is verified (`apiKeys.plaintext === 0`).

**Files:** `backend/src/routes/apiKeys.ts:82`, `backend/src/routes/apiKeys.ts:88`, `backend/src/middleware/auth.ts:108`, `backend/src/middleware/auth.ts:239`, `backend/src/middleware/subsonicAuth.ts`, `backend/prisma/schema.prisma:1039`

**Problem.** ApiKey.key is the bearer secret stored verbatim: apiKeys.ts:82 generates crypto.randomBytes(32).toString('hex') and persists it unhashed (schema.prisma:1042 `key String @unique`, indexed at :1048), then auth resolves it with `prisma.apiKey.findUnique({ where: { key: apiKey } })` (auth.ts:108, :239). Every other secret in this codebase is encrypted at rest (2FA, Subsonic password, OAuth JSON via utils/encryption.ts) — API keys are the lone plaintext exception. Any read-only exposure (pg_dump, backup, log, a future SQLi/raw-query slip, replica access) immediately leaks live full-privilege credentials for every linked device, with no per-key expiry or scope to limit blast radius.

**Recommendation.** Accept the finding and the hash-at-rest direction, with these corrections. SCOPE: patch all THREE producers — routes/apiKeys.ts:82-88, routes/deviceLink.ts:20-22/145-149 — and all THREE verifiers — middleware/auth.ts:108, auth.ts:239, middleware/subsonicAuth.ts:114. MODEL: add `keyPrefix String` (indexed, non-secret, e.g. first 8 hex of secret or a cuid), `keyHash String` (HMAC-SHA-256 of the secret under a server pepper), drop `key @unique`/`@@index([key])`; the token returned to the client is `prefix.secret`. AUTH: split incoming token on first '.', findUnique by keyPrefix, then crypto.timingSafeEqual the recomputed HMAC — this also removes the current correctness nit that auth re-queries are O(unique-index) on the secret. PEPPER: source it from config.ts (extend the zod envSchema so it fails fast like SESSION_SECRET) — do not read process.env in middleware, per the backend-env-only-via-config.ts contract. Note the existing utils/encryption.ts is reversible AES-CBC and is the WRONG primitive here (we want a one-way KDF/HMAC, not decryptable storage), so don't reuse encrypt()/decrypt(); add a dedicated hashApiKey helper. MIGRATION: forced re-issue (not lazy re-hash — old raw values are unrecoverable once the lookup column changes); document in CHANGELOG.md and surface in the device-link/API-key UI that existing devices must re-link. Add nullable expiresAt + a scope enum now and enforce expiry in the verifiers. Add tests (TDD per contract) covering: create returns prefix.secret once, auth succeeds with correct token, fails on tampered secret/expired key, and timing-safe path. Minor extra cleanup: schema has both `key @unique` and a redundant `@@index([key])` — both go away in this change.

**Safety / pitfalls.** Three concrete pitfalls the proposal must handle or it will break working clients. (1) INCOMPLETE FILE LIST: the finding cites apiKeys.ts and the two auth.ts sites but MISSES backend/src/routes/deviceLink.ts:20-22,145-149, which is a SECOND minter of plaintext keys (generateApiKey() -> prisma.apiKey.create({ key })) and a primary path for mobile/Subsonic devices. Hashing must cover deviceLink.ts too, or those keys keep landing in plaintext and/or stop validating. (2) BACKFILL = HARD LOGOUT RISK: existing keys are stored as raw 64-hex with no recoverable secret. 'Lazily re-hash on next successful use' is impossible because after migrating to prefix+hash you can no longer match an old raw-value key (the lookup column changes), so the only safe migration is forced re-issue (every linked mobile/Subsonic device must re-link). This is a real operator-facing breaking change, not a transparent one — the changelog/UI must say so. (3) FORMAT/CLIENT COMPAT: Subsonic clients pass the key verbatim as the `apiKey` query param (subsonicAuth.ts:50,114) and mobile clients send X-API-Key; switching to an `id.secret` shape is fine ONLY if the full `id.secret` string is what gets handed to and stored by the client. Don't change the on-the-wire token length/charset assumptions without checking the Tauri/mobile clients. Also: AGENTS.md bans raw SQL but the Prisma prefix-lookup keeps this compliant; just ensure the new pepper uses config.ts (per contract, backend env only via config.ts) and NOT a fresh process.env read in middleware.

---

### F29 — Settings encrypted with unauthenticated AES-256-CBC, a naive pad/truncate key, and a silent plaintext fallback

**✅ complete (code; backfill not yet run)** (`feat/f29-gcm-envelope`) · dimension: security · severity: high · effort: M · risk: medium · ⚠️ breaking · epic: #11

> **Fix shipped** (#11 Phase 1). `encrypt()` now writes a versioned authenticated envelope `v2:<salt>:<iv>:<tag>:<ct>` (AES-256-GCM, per-value scrypt over the **full** key). `decrypt()` dispatches on prefix: `v2` is authenticated and **fails closed** (tamper/wrong-key/malformed all throw — no plaintext passthrough); anything else uses the **unchanged** legacy CBC read path (pad/truncate derivation preserved) so all historical data still decrypts. A small salt-keyed scrypt cache avoids a per-request KDF regression on the Subsonic hot path. Tests: `encryption.test.ts` (v2 round-trip, fail-closed on tamper/tag/wrong-key/malformed, full-entropy no-truncation, legacy v1 still decrypts). **Backfill written but not run** (per operator decision): `scripts/migrate-settings-to-gcm.ts` (v1→v2, dry-run default, leaves undecryptable values untouched — `settingsCipherMigration.ts` decision logic unit-tested) + `GET /api/admin/secrets-status`. Operators run the backfill per `docs/UPGRADING.md`; the legacy read path stays until `legacy === 0`.

**Problem.** encryption.ts protects API/OAuth tokens, 2FA secrets (User.twoFactorSecret), Subsonic passwords and Lidarr webhook secret with aes-256-cbc and NO integrity tag, so ciphertext is malleable and tamper-undetectable. The key is derived by padEnd(32,'0') for short keys or slice(0,32) for long ones (lines 33-38) — no KDF, so a low-entropy key stays low-entropy and a >32-char base64 key silently loses its tail. decrypt() fails open: any value without a ':' is returned as cleartext (line 66-68) and any non-ERR_OSSL error returns the input unchanged (line 86-87), so corrupt/tampered/forged data round-trips as 'unencrypted' instead of being rejected.

**Recommendation.** Accept the finding and the direction, with sequencing. All six sub-claims verified against backend/src/utils/encryption.ts (aes-256-cbc line 4, pad/truncate lines 33-38, no auth tag lines 48-55, `length<2` plaintext fallback lines 66-68, non-ERR_OSSL passthrough lines 80-88) and confirmed to protect 2FA secrets + recovery codes (routes/auth/shared.ts:35-69, routes/auth/accountSecurity.ts:320-329), Subsonic passwords (middleware/subsonicAuth.ts:194,234), Lidarr API key + webhook secret, soulseek/spotify/tidal/ytmusic credentials (routes/systemSettings.ts:227-253, routes/onboarding.ts, services/tidal.ts:208-209). Note: the documented key generator `openssl rand -base64 32` yields a 44-char string, so the happy path silently truncates 12 chars (real entropy loss, not hypothetical). Implementation plan: (1) Add AES-256-GCM with versioned envelope `v2:iv:tag:ciphertext`; derive the key via scrypt or HKDF (with a fixed app salt or env salt) from SETTINGS_ENCRYPTION_KEY. (2) Keep a `decryptV1` path that reproduces the legacy CBC + padEnd/slice derivation byte-for-byte so existing ciphertext stays readable; route decrypt by envelope prefix. (3) decrypt() should fail closed (throw) on GCM auth-tag mismatch for v2; for v1 keep current behavior only during the migration window. (4) Ship a one-shot Prisma-based backfill (no raw SQL) that reads every encrypted field, re-encrypts as v2, and writes back; only after it runs should the `parts.length<2` plaintext fallback and the non-ERR_OSSL passthrough be removed. (5) Update the three behavior-locking tests in src/utils/**tests**/encryption.test.ts (lines 90, 113, 140) plus add new tests for tamper-detection (flip a tag/ciphertext byte -> throws) and v1->v2 round-trip. (6) Per AGENTS.md, move the key presence/insecure-default/length validation into backend/src/config.ts and have encryption.ts consume it from config, eliminating the direct process.env read at encryption.ts:15 while preserving fail-fast on missing/default key. (7) Audit callers that swallow decrypt errors (tidal.ts:170-175, subsonicAuth.ts:194) so that, post-migration, a thrown auth-tag error surfaces as a real auth failure rather than silently falling back to treating ciphertext as plaintext.

**Safety / pitfalls.** Live code depends on the fail-open paths the proposal wants to remove, so the change is breaking unless sequenced carefully. (1) tidal.ts:170/175 use `decrypt(...) || settings.tidalAccessToken` and a catch-to-plaintext, and subsonicAuth.ts:194 wraps decrypt in try/catch — dropping the `parts.length<2` plaintext fallback before a one-shot backfill will break reads of legacy/unencrypted rows and can lock users out of Subsonic/Tidal. (2) Three existing tests in encryption.test.ts explicitly lock in the legacy behaviors being changed: line 113 (plaintext passthrough on no ':'), line 90 (long-key truncation cross-decrypt), line 140 (non-ERR_OSSL errors return input unchanged); a fail-closed v2 envelope contradicts them, so they must be rewritten, not just left. (3) The ENCRYPTION_KEY fallback env (line 15, test line 68) and short-key padding (test line 79) are also test-locked — a scrypt/HKDF migration changes derived keys, so v1 decrypt MUST retain the exact legacy pad/truncate derivation or all existing ciphertext becomes unreadable. (4) Subsonic re-encrypts on every successful password login (subsonicAuth.ts:234), so for that field lazy re-encryption already happens organically, but other fields (2FA secret, lidarr secret) only re-write when the user re-saves — a backfill job is needed before removing the v1 read path. No AGENTS.md 'no raw SQL' or 'Prisma-only' conflict; moving key validation into config.ts actually fixes a real contract violation (encryption.ts:15 reads process.env directly, bypassing the single env source).

---

### F30 — Internal CLAP-callback endpoints fail open and use non-constant-time secret comparison

**✅ complete (PR #21)** · dimension: security · severity: high · effort: S · risk: low · epic: #12

**Files:** `backend/src/routes/analysisInternal.ts:51`, `backend/src/routes/analysisInternal.ts:116`, `backend/src/index.ts:294`

**Problem.** analysisInternal.ts gates /vibe/failure and /vibe/success with `if (req.headers['x-internal-secret'] !== process.env.INTERNAL_API_SECRET)`. When INTERNAL_API_SECRET is unset, process.env value is undefined and a request omitting the header is also undefined, so `undefined !== undefined` is false and the check PASSES — fail-open auth bypass that writes EnrichmentFailure rows / mutates analysis state from any caller. The comparison is also a raw `!==` (variable-time, leaks length/prefix via timing) instead of the timingSafeCompare used everywhere else (webhooks.ts:99, subsonicAuth.ts:200). These routes are deliberately mounted even when audio-analysis is disabled (index.ts:294-299), so the surface is always reachable.

**Recommendation.** Confirmed exactly as described. Root cause: analysisInternal.ts:52 and :117 use `internalSecret !== process.env.INTERNAL_API_SECRET`; when the env var is unset (undefined) and the header is absent (undefined), `undefined !== undefined` is false, so the 403 branch is skipped and the request is authorized — a fail-open M2M auth bypass that lets any caller write EnrichmentFailure rows (recordFailure) and resolve/mutate vibe analysis failure state (resolveByEntity). The comparison is also a variable-time `!==`, inconsistent with the constant-time timingSafeCompare already used at webhooks.ts:99, subsonicAuth.ts:200, and auth.ts:177 (util at backend/src/utils/timingSafe.ts). The router is mounted with only apiLimiter (rate limit, not auth) at index.ts:294-299 (feature-disabled) and is ALSO reachable via the feature-enabled path through analysis.ts:885 `router.use(analysisInternalRoutes)` — and requireAuth in analysis.ts is per-route, not router-global, so the internal callbacks have no session gate in either configuration. The secret check is genuinely the only gate. Fix: (1) Add an extractable middleware (e.g. backend/src/middleware/requireInternalSecret.ts) that reads the env secret once, rejects with 403 when the header is missing OR the env secret is unset/placeholder, and compares via timingSafeCompare(provided, expected). Apply it router-level with `router.use(requireInternalSecret)` at the top of analysisInternal.ts so a single change covers both mount points and removes the two duplicated per-handler checks. (2) For the fail-fast on unset secret, prefer fail-CLOSED at request time (return 403/503 if the secret is unset) over a config.ts process.exit, to avoid breaking AIO deployments where the analyzer may not be present — or, if adding to config.ts, only enforce it when audioAnalysis is enabled and/or when the CLAP analyzer is configured, rather than unconditionally. (3) Keep analyzer.py:503 in sync (it sends os.getenv("INTERNAL_API_SECRET",""), which the new middleware will correctly reject when blank). Note the AGENTS.md contract: the env should ideally be surfaced through config.ts (currently only read via raw process.env), consistent with "backend env only via backend/src/config.ts".

**Safety / pitfalls.** The hard fail-fast in config.ts has one real footgun: INTERNAL_API_SECRET is currently OPTIONAL (no row in the Zod envSchema, config.ts:16-25). Making it required/non-placeholder will process.exit(1) any deployment that hasn't set it — including AIO/single-container deployments that the index.ts:291-294 comment specifically calls out as the reason these routes stay mounted. If audioAnalysis is off and the CLAP sidecar isn't deployed, a hard startup failure would be a regression in availability. Safer to gate the fail-fast: validate the secret only when the analyzer path can actually be exercised, or fail-closed at request time (reject 503/403 when unset) rather than crashing the whole backend at boot. Second concern: the proposed middleware MUST run AFTER express.json (it is — express.json is global at index.ts:187, before the route mounts), and must be added to BOTH mount points implicitly — it already is, because the router is shared (analysisInternal.ts is mounted standalone at index.ts:294 AND via analysis.ts:885 router.use), so router-level middleware on analysisInternal covers both with no duplication. Third: the test at analysisRuntime.test.ts:178 sets INTERNAL_API_SECRET="test-secret" but other tests/callers that hit these routes without the env set would start getting 403/503 — intended, but update the analyzer deployment docs/Helm so the sidecar and backend always share the value (analyzer.py:503 already reads os.getenv with "" default, which would now be correctly rejected).

---

### F31 — FastAPI sidecars have zero inbound authentication and use unvalidated user_id as a filesystem path (path traversal)

**✅ complete (PR #118)** · dimension: security · severity: high · effort: M · risk: medium · ⚠️ breaking · epic: #12

> **Fix shipped.** Both FastAPI sidecars now mount an app-wide fail-closed auth dependency `require_internal_secret` (shared helper in `services/common/sidecar_runtime_utils.py`, mirroring the TS `internalAuth.ts`/F30): unset/empty `INTERNAL_API_SECRET` → **403**, missing/mismatched `x-internal-secret` header → 403 (constant-time `hmac.compare_digest`), and `/health` is exempt so k8s probes + the backend health checks keep working. The FastAPI schema/docs routes (`/docs`, `/redoc`, `/openapi.json`) are disabled on both sidecars (`docs_url=None` etc.) — they're `add_route()`-registered, outside the auth dependency's reach, and would otherwise disclose the API schema unauthenticated; pytest pins them at 404. ytmusic-streamer's path builders (`_oauth_file` and a new `_client_creds_file`) call `validate_user_id` (`re.fullmatch(r"[A-Za-z0-9_-]{1,64}")` → **400**) so `../`-style ids are rejected before any file op; tidal's body `user_id` is left alone (in-memory dict key, not a path — audit-corrected). Backend sends the header on all four axios clients (`youtubeMusic.ts`, `youtubeDownload.ts`, `tidal.ts`, `tidalStreaming.ts`) sourced via `config.internalApiSecret` (new optional `config.ts` field; boot never fails when unset — unset simply sends no header and the sidecar rejects fail-closed) plus the gate-caught fifth site (see item 5). Deploy layers threaded: `docker-compose.yml` (both sidecars), Helm `tidal.yaml`/`ytmusic.yaml` (`secretKeyRef` into the chart's managed Secret — the same one that already carries `INTERNAL_API_SECRET` for the backend/CLAP — with the render-check script now exercising both sidecar templates), `docs/ENVIRONMENT_VARIABLES.md`, `docs/UPGRADING.md` (⚠️ breaking, operator checklist). The AIO image does not bundle the HTTP sidecars, so no AIO change was needed (its backend + CLAP already receive the secret). Tests: pytest both sidecars (auth 403/400 + `/health`-exempt + valid-passes; ytmusic traversal), backend jest (each client sets the header when configured; the fifth-site fix).
>
> **Executable spec (as implemented):**
>
> 1. **Shared helper** — add to `services/common/sidecar_runtime_utils.py`:
>     - `require_internal_secret(request)`: if `request.url.path == "/health"` → return (exempt for k8s probes + the backend's own `/health` checks at `tidal.ts:111`, `tidalStreaming.ts:184`); read `INTERNAL_API_SECRET` and **fail closed** (`HTTPException(403)`) if unset; compare the `x-internal-secret` header with `hmac.compare_digest` (both `str`); 403 on mismatch. Mirrors the verified TS `requireInternalSecret` (F30).
>     - `validate_user_id(user_id)`: `re.fullmatch(r"[A-Za-z0-9_-]{1,64}", user_id)` or `HTTPException(400)`. Prisma cuids are lowercase-alnum so legit traffic passes.
> 2. **Apply auth app-wide** in both `services/ytmusic-streamer/app.py` and `services/tidal-downloader/app.py`: `app = FastAPI(..., dependencies=[Depends(require_internal_secret)])` (add `Depends` to the `fastapi` import). The `/health` exemption lives in the dependency.
> 3. **Path-traversal** — in ytmusic-streamer, call `validate_user_id(user_id)` inside the file-path builders (`_oauth_file` / `client_creds_{user_id}.json`, ~`app.py:291`) so `auth_status`/`auth_restore`/`auth_clear` reject `../`-style ids. Do **not** apply to tidal's body `user_id` (the audit corrected that citation — tidal uses it only as an in-memory dict key, not a path).
> 4. **Backend (same PR — required or all calls break)** — add the `x-internal-secret` header (sourced via `config.internalApiSecret`, per the backend-env-only-via-config.ts rule — **not** a raw `process.env` read) to the **default headers** of each of the four axios clients (one edit each): `youtubeMusic.ts`, `youtubeDownload.ts`, `tidal.ts`, `tidalStreaming.ts`.
> 5. **Gate-caught correction:** the original claim "there are **no** direct sidecar calls bypassing these clients" was **FALSE** — `backend/src/routes/tidalStreaming.ts:119` (`ensureUserOAuth`) probed the tidal sidecar's `/user/auth/status` via a **bare `axios.get`** with a raw env URL, bypassing all four clients. Left alone it would 403 forever after F31 and be silently misread as "sidecar has no session." Fixed by routing it through a new `tidalStreamingService.checkSidecarAuthStatus()` (authenticated client, header attached), covered by a route test. A `grep -rn "TIDAL_SIDECAR_URL\|YTMUSIC_STREAMER" backend/src` sweep confirmed this was the only extra site.
> 6. **Tests** — pytest (both sidecars' existing `tests/` + `conftest.py`): traversal `user_id=../../etc/x` → 400; missing/wrong `x-internal-secret` → 403; unset secret → 403 (fail-closed); `/health` reachable without the secret; valid secret passes. Backend jest: assert each client sets the header + the fifth-site fix.

**Files:** `services/ytmusic-streamer/app.py:291`, `services/ytmusic-streamer/app.py:1383`, `services/ytmusic-streamer/app.py:1420`, `services/tidal-downloader/app.py:1025`

**Problem.** Neither ytmusic-streamer nor tidal-downloader defines any auth dependency/middleware (grep for Depends/X-Internal/api*key in their app.py returns nothing) — only the CLAP analyzer even sends INTERNAL_API_SECRET, and the streamers never check it. Every endpoint is callable by anyone who reaches the pod, including /auth/restore and /auth/clear which write/delete credential files. Worse, \_oauth_file(user_id) builds `DATA_PATH / f"oauth*{user*id}.json"` (app.py:291) and client_creds*{user_id}.json from a raw `user_id: str = Query(...)` with no validation, so user_id='../../etc/cron.d/x' lets /auth/restore write attacker-controlled bytes outside DATA_PATH and /auth/clear unlink arbitrary matching files — directly exploitable given the no-auth gap.

**Recommendation.** Accept the security thrust but correct the scope. (1) No-auth is real in BOTH sidecars — add the require*internal_secret dependency to ytmusic-streamer/app.py and tidal-downloader/app.py, fail-closed when INTERNAL_API_SECRET is empty, exempt /health, and in the SAME PR add the x-internal-secret header to the backend axios clients (backend/src/services/youtubeMusic.ts, tidalStreaming.ts, youtubeDownload.ts) or all calls break. Consider a shared helper in services/common/ (sidecar_runtime_utils.py already exists) to avoid duplicating the dependency in each app. (2) Path traversal is real ONLY in ytmusic-streamer (app.py:293/474/1402/1408/1427-1430/1503 build oauth*{user*id}.json and client_creds*{user*id}.json from raw Query user_id). The finding's tidal citation (tidal-downloader/app.py:1025) is INCORRECT: that is /download/track, its path derives from req.output_template not user_id, and tidal never uses user_id as a filesystem path (only as in-memory dict keys). So apply validate_user_id (re.fullmatch(r'[A-Za-z0-9*-]{1,64}')) to ytmusic for genuine traversal defense; in tidal it is merely hardening of cache keys (low value) — optional. Real-world exploitability today is gated by the no-auth gap since the backend only ever sends server-generated cuids; an attacker needs pod-network access. If the tidal output_template path is itself attacker-influenced, that is a separate traversal worth a follow-up (out of this finding's stated scope). Add pytest cases asserting traversal user_ids (../../etc/x) are 400-rejected and that missing/incorrect secret is 401/403.

**Safety / pitfalls.** App-wide FastAPI(dependencies=[Depends(require_internal_secret)]) is a breaking change unless the backend client is updated in the SAME change: backend/src/services/youtubeMusic.ts and tidalStreaming.ts currently send NO x-internal-secret header, so every sidecar call would start 401ing. Must (a) inject the header in both backend axios clients (config.ts exposes the URLs; INTERNAL*API_SECRET is already an env var per analysisInternal.ts) AND (b) exempt /health (used by the backend health probe at youtubeMusic.ts and likely k8s liveness/readiness) — otherwise probes fail and pods get killed. Also note hmac.compare_digest needs both args as bytes/str of consistent type and a non-empty configured secret; if INTERNAL_API_SECRET is unset the dependency must fail closed, not allow-all. The validate_user_id regex [A-Za-z0-9*-]{1,64} is safe for Prisma cuids (lowercase alnum) so it won't reject legitimate traffic, but tidal also receives a separate req.user_id (the TIDAL numeric user id) in the body — only the query-param user_id needs the dependency; don't over-apply it to the body field. No AGENTS.md conflict (this is Python sidecar code, not the Prisma/raw-SQL or frontend-api-boundary rules).

---

### F32 — Lidarr webhook is unauthenticated and unthrottled by default and parses payload as untyped any

**🟡 partial** (`feat/f32-webhook-auth`) · dimension: security · severity: high · effort: M · risk: low · epic: #12

> **Shipped (the non-breaking, high-value half).** Added a dedicated `webhookLimiter` to the `POST /lidarr` route (it was completely unthrottled), and **replaced the unconditional per-event full-library scan** on unmatched download IDs (the easiest DoS — `webhooks.ts` else-branch) with a **coalesced** scan: a stable Bull jobId (`lidarr-external-import-scan`, same scheme as the YouTube download scan) collapses a webhook flood into one queued scan at a time, so external imports still get picked up — there is no periodic scan to fall back on. The secret check stays fail-closed when a secret is configured; when none is set the request is accepted but logs a loud warning (a startup-style nudge). Rate-limiter keying improves with `TRUST_PROXY_HOPS` (F35).
>
> **Deferred (the breaking half), with a note.** Making secret auth the **hard default** requires auto-generating `lidarrWebhookSecret` on startup and wiring it into Lidarr's connection config (which `services/lidarr.ts` has zero handling for today) — otherwise it instantly 401s every real Lidarr webhook on existing installs (e.g. the homelab `music.tonymead.org`, which the audit's safety note names explicitly) and silently breaks download tracking. This is the same "breaks an existing integration" risk class as F37 and was deferred by the same operator decision. Per-event zod schemas (shape safety; the handlers are already null-safe and the 1 MB body limit bounds size) are a low-value follow-up. Operators are advised to set a webhook secret (`docs/UPGRADING.md`), which **is** enforced fail-closed once present.

> **Audit note.** Per audit: the Lidarr webhook secret IS settable via the systemSettings API (it just isn't auto-generated).

**Files:** `backend/src/routes/webhooks.ts:96`, `backend/src/routes/webhooks.ts:159`, `backend/src/index.ts:259`

**Problem.** Secret verification is gated on `if (settings.lidarrWebhookSecret)` (webhooks.ts:96) — with no secret configured (the default) POST /api/webhooks/lidarr accepts any body, and index.ts:259 deliberately mounts webhooks with NO rate limiter. handleGrab/handleDownload/handleImportFailure read `payload: any` (lines 159/194/256) and feed attacker-controlled artist/album/downloadId strings straight into simpleDownloadManager matching and `scanQueue.add('scan', ...)` (line 233/246). An attacker who can reach the host can spam full-library scans and inject arbitrary metadata into the download/match pipeline with no auth and no throttle. The timingSafeCompare when a secret IS set is correct.

**Recommendation.** Confirmed as written. Verified: webhooks.ts:96 gates secret verification on truthy settings.lidarrWebhookSecret, so with no secret (the default — it is never auto-generated; only persisted if a user manually supplies it, and there is no frontend UI that sets it — grep found zero app references outside systemSettings storage) the POST accepts any body. index.ts:259 mounts /api/webhooks with NO limiter, and there is no global/app-wide rate limiter (only per-route apiLimiter), so the endpoint is genuinely unthrottled. handleGrab/handleDownload/handleImportFailure (159/194/256) read payload:any and feed attacker-controlled downloadId/artistName/albumTitle into simpleDownloadManager.onDownloadComplete (DB findFirst/findMany + fuzzy matching, line 642-647) and into scanQueue.add — including the easiest attack: a Download event whose downloadId matches no job hits the else branch (webhooks.ts:246) and enqueues a FULL library scan unconditionally. timingSafeCompare (utils/timingSafe.ts) is implemented correctly with length-safe crypto.timingSafeEqual. Recommended changes, refined: (a) Fail closed only with a migration that auto-generates lidarrWebhookSecret (crypto.randomBytes) for any install where lidarrEnabled is already true, surface it in the system-settings UI, AND wire it into the Lidarr connection config in services/lidarr.ts (currently absent) so real webhooks keep working. (b) Add a dedicated webhook rate limiter on the POST, but use a custom keyGenerator / tighten trust-proxy because app.set('trust proxy', true) otherwise lets attackers spoof the IP key. Keep GET /lidarr/verify unauthenticated. (c) Replace payload:any with per-eventType zod schemas validated before any queue/DB work, but keep them lenient on unknown/extra fields (Lidarr payloads vary by event/version) — validate the fields you actually read rather than rejecting unknown keys. Note the 1mb express.json limit already bounds body size, so the zod win is type/shape safety, not oversize protection. No raw-SQL/contract concerns introduced.

**Safety / pitfalls.** Fail-closed change is correct but has two real pitfalls. (1) Backward-compat / lockout: existing deployments (e.g. the homelab music.tonymead.org instance per MEMORY) have lidarrEnabled=true with NO lidarrWebhookSecret today; flipping to "require secret when Lidarr enabled" will instantly 401 every real Lidarr webhook and silently break download tracking + post-import scans until the operator regenerates the secret AND updates Lidarr's webhook config. Must auto-generate on migration/startup for already-enabled installs and ensure the secret is actually pushed into Lidarr's connection settings (the lidarr.ts service has no webhook-secret wiring at all today — grep shows zero secret handling there), otherwise you trade an open endpoint for a broken integration. (2) Rate-limiter keying: app.set("trust proxy", true) trusts the entire X-Forwarded-For chain; an IP-keyed express-rate-limit will key off attacker-spoofable XFF unless trust proxy is tightened or a custom keyGenerator is used — so the throttle can be trivially bypassed in this exact config. Also note express.json limit is already 1mb globally, so "oversized body" is partly mitigated already; zod is still warranted for shape/type safety. The /lidarr/verify GET endpoint must stay unauthenticated (Lidarr's "Test"/reachability probe) — only gate the POST. zod parsing must remain lenient about extra/unknown Lidarr fields (use .passthrough or .partial) since Lidarr payloads vary by event and version; a strict schema that rejects unknown keys would drop legitimate webhooks.

---

### F33 — SSRF guard blocks only literal private hosts — DNS rebinding to an internal IP bypasses it

**✅ complete** (`feat/f33-ssrf-guard`) · dimension: security · severity: high · effort: M · risk: medium · epic: #12

> **Fix shipped.** Added async `resolveSafeOutboundUrl` / `resolveSafeOutboundRedirectTarget` (`outboundUrlSafety.ts`) that DNS-resolve the host and range-check every resolved IP, reusing the shared private/loopback/link-local predicates — which now cover **all of `127.0.0.0/8` and `0.0.0.0/8`** rather than only the canonical literals (a DNS answer of `127.0.0.53` previously passed the range check). Applied at the actual fetch entry points: `rssParser.parseFeed` **and each feed redirect hop** (followed manually with per-hop re-validation), `imageProxy.fetchExternalImage` + its redirect hops, and `podcastCache.downloadCover` (redirects hard-disabled there). The systemSettings admin connection test deliberately keeps the **string check only** — those endpoints are admin-only and legitimately target Docker/LAN hosts (`http://lidarr:8686`) in the documented compose deployment. The string-only `normalizeSafeOutboundUrl` stays as the sync pre-check at non-fetch validation sites (no broad async ripple). Tests mock `dns/promises` in every touched suite (no live-DNS dependence). **Residual (noted in code):** check-time resolution leaves a DNS-rebinding window; pinning the resolved IP into the request agent is a larger follow-up. The YouTube route stays out of scope (backend never fetches the user URL, per the audit).

**Files:** `backend/src/services/outboundUrlSafety.ts:41`, `backend/src/services/outboundUrlSafety.ts:58`, `backend/src/services/imageProxy.ts:41`, `backend/src/services/youtubeDownload.ts:185`

**Problem.** normalizeSafeOutboundUrl (the shared guard used by the cover-art/share-link image proxies) rejects private/loopback/link-local destinations purely by string-matching the URL's hostname (isBlockedHostname/isBlockedIpv4Hostname, outboundUrlSafety.ts:12-52). It never resolves DNS, so an attacker-controlled hostname like rebind.evil.com that resolves to 169.254.169.254 or 10.x passes the check, then fetch()/Node connects to the private IP (cloud metadata, internal services). Redirect handling re-applies the same string-only guard (imageProxy.ts:56), so it inherits the gap. Separately, the YouTube passthrough enforces no host allowlist at all: youtube.ts:49 validates url only as z.string().min(1) and youtubeDownload.ts forwards it to the sidecar/yt-dlp, delegating all egress containment to yt-dlp.

**Recommendation.** CONFIRMED for the image/redirect proxy, with line-number corrections and a stronger problem statement. The shared guard normalizeSafeOutboundUrl (outboundUrlSafety.ts:58) calls isBlockedHostname (line 41) which is purely string/regex-based on parsedUrl.hostname; no dns import exists anywhere in the outbound path (verified: zero dns/undici usage). fetchExternalImage (imageProxy.ts:83) then calls global fetch on the raw hostname (line 41), so any attacker hostname that resolves to a private/metadata IP connects. The redirect loop re-uses the same string-only guard (imageProxy.ts:56), inheriting the gap. The exposure is concrete and attacker-controllable via two routes: GET /library/cover-art-colors?url= (library.ts:4765, rate-limited but the url is straight from the query) and GET /share-links/access/:token/cover?url= (shareLinks.ts:748, gated only by a share token any user can mint). Beyond DNS rebinding, I empirically confirmed the guard also fails on alternate literal-IP encodings of 127.0.0.1 — decimal 2130706433, hex 0x7f000001, octal 0177.0.0.1, and short-form 127.1 all pass isBlockedIpv4Hostname yet Node/undici will connect to loopback — so the fix should normalize/parse the resolved numeric address, not just the hostname string. Recommended fix: in fetchExternalImage's connect path, resolve via dns.lookup({all:true}) (or use a custom http/https Agent lookup hook), reject if ANY returned address is loopback/private/link-local/ULA/IPv4-mapped/unspecified (reuse the existing predicates but applied to the RESOLVED IP, and also block 0.0.0.0/8, 100.64/10 CGNAT, and broadcast), and pin the socket to that vetted IP inside the same lookup callback to close the TOCTOU window while preserving TLS servername for cert validation. Keep the existing protocol check and redirect re-validation (each redirect hop must re-resolve+re-pin). Add tests mirroring outboundUrlSafety.test.ts that stub the lookup to a private IP. REJECTED/SCOPED-DOWN for the YouTube portion: the claim 'youtubeDownload.ts forwards it to the sidecar/yt-dlp' is accurate but the SSRF model is different — the backend's axios baseURL is the trusted internal sidecar (config.ytmusicStreamer.url) and the user-supplied url is only passed as a query PARAM to /yt/info and /yt/playlist-info; startDownload/stream take a videoId, not a url. The backend never makes an HTTP request to the attacker's url, so this is not a backend SSRF and does not belong in F33's high-severity image-proxy fix. Egress containment for that url legitimately lives in yt-dlp inside the sidecar. A hostname allowlist at youtube.ts:30/74 (z.string().url() + youtube.com/youtu.be allowlist) is a reasonable low-priority defense-in-depth hardening, but it is not the same vulnerability class and should not be bundled as 'high'.

**Safety / pitfalls.** DNS-lookup-then-connect introduces a real TOCTOU window; the proposed pin (custom http(s).Agent lookup hook returning the vetted IP) must also re-run the private-range check on EVERY resolved address inside the lookup callback, otherwise an attacker can rebind between the dns.lookup check and the agent's own resolution. Node 18.19 is in use, so connecting by pinned-IP-with-Host-header must preserve TLS SNI/servername (verify cert against the original hostname, not the IP) to avoid breaking https cover hosts. Performance: an extra dns.lookup per fetch on the hot cover-art path (/library/cover-art-colors, /library cover proxy) adds latency, but Redis caching already fronts these so the impact is bounded. Per AGENTS.md, backend egress config belongs in config.ts, not hardcoded; keep the IP-version family list configurable there. No raw-SQL/Prisma contract is touched. Do NOT change the YouTube route to a fetch-style SSRF guard — see refinedRecommendation; the backend never fetches the user url there, and an over-strict z.string().url()+allowlist could regress today's behavior (the sidecar already 400/422s bad urls and the route surfaces those).

---

### F34 — CORS allows every origin unconditionally despite an allowlist knob, while credentials:true

**✅ complete (PR #21)** · dimension: security · severity: medium · effort: S · risk: low · epic: #12

**Files:** `backend/src/index.ts:131`, `backend/src/index.ts:146`, `backend/src/index.ts:159`, `backend/src/config.ts:133`

**Problem.** The cors() origin callback returns callback(null, true) on every branch — even when an origin is explicitly NOT in config.allowedOrigins it logs '[CORS] ... allowing anyway (self-hosted)' (index.ts:154-160). Combined with credentials:true (index.ts:166), any website can make credentialed cross-origin requests. Today auth is header bearer/API-key so the blast radius is limited, but express-session is fully wired with cookies (index.ts:202-221) — if cookie/session web auth is ever activated this is an immediate CSRF/credential-theft vector, and the ALLOWED_ORIGINS env var (config.ts:133) is misleadingly non-functional.

**Recommendation.** Confirmed real. In backend/src/index.ts the cors() origin callback returns callback(null, true) on EVERY branch, including the explicit not-in-allowlist case (lines 153-160: logs "[CORS] ... allowing anyway (self-hosted)" then still allows). With credentials:true (line 166), cors v2.8.5 reflects the request Origin into Access-Control-Allow-Origin and sets Access-Control-Allow-Credentials:true — the browser-exploitable shape. ALLOWED_ORIGINS is parsed (parseEnvCsv, backend/src/utils/envParsers.ts:58) and stored (config.ts:133-135) but never actually restricts anything; it only changes a debug log line, so the knob is genuinely misleading.

Fix: in the else at index.ts:153-160 (origin present, allowlist configured, origin not included) change callback(null, false) instead of allow-and-log. Keep lines 137-139 (no-origin), 140-145 (dev / allowedOrigins===true), and 161-163 (no allowlist configured -> permissive default) exactly as-is. This is the smallest-safe-change and matches the proposal. Update CHANGELOG.md and document that HTTPS/multi-origin deployments must set ALLOWED_ORIGINS (note Tauri desktop origin if applicable).

One correction to the finding's framing on blast radius: session auth is NOT merely hypothetical-future. backend/src/middleware/auth.ts checks req.session?.userId FIRST and comments it as the "primary method for web" (lines 92, 218-219), and routes offline.ts/listeningState.ts use req.session.userId! as their sole user source. HOWEVER, req.session.userId is never assigned anywhere in the backend — login (auth.ts:258 "No server-side session to destroy") issues JWTs only. So the express-session cookie currently carries no userId, meaning a cross-origin CSRF request rides no ambient credential TODAY. The finding's conditional ("if cookie/session web auth is ever activated") is the accurate framing; the risk is latent. The fact that the auth middleware already prefers session and routes already depend on it makes activation plausibly a one-handler change away, which strengthens (not weakens) the case for fixing the CORS allowlist now. Recommend fixing.

**Safety / pitfalls.** Low. Flipping the not-in-allowlist branch (backend/src/index.ts:153-160) to callback(null, false) is safe with cors v2.8.5 ("cors": "^2.8.5" in backend/package.json): a falsy origin result omits CORS headers and lets the request through to the handler without throwing — the browser, not the server, blocks the cross-origin read, so no 5xx. The existing guard Array.isArray(config.allowedOrigins) && config.allowedOrigins.length > 0 (index.ts:147-148) ensures the restrictive branch only triggers when an admin actually set ALLOWED_ORIGINS; the default empty array (config.ts:135) falls through to the permissive else (index.ts:161-163), so self-hosters who never configured origins are unaffected. The only real risk is operational: an admin who sets ALLOWED_ORIGINS but forgets a legitimate origin (e.g. a Tauri/desktop or alternate-host frontend) would suddenly have credentialed requests blocked — must be documented. No AGENTS.md contract concerns (no raw SQL, env still read only in config.ts, no frontend boundary touched).

---

### F35 — Helmet ships without HSTS/CSP, session cookie 'secure' is opt-in via string env, and trust proxy trusts all hops

**✅ complete** (`feat/f35-cookie-secure`) · dimension: security · severity: medium→low · effort: S · risk: medium · epic: #12

> **Audit note.** ✓ Headline wrong: helmet ^7 ships default CSP AND HSTS; the code only customizes `crossOriginResourcePolicy`, so both headers ARE sent. Real residual = cookie `secure` gated on a string env + trust-proxy hop count. Severity medium→low.
>
> **Fix shipped** (the two valid items only; HSTS/CSP correctly rejected — helmet already ships them). Cookie `secure` moved off raw `process.env` into `config.secureCookies`, defaulting **true in production** (explicit `SECURE_COOKIES` honored either way) — closes the silent non-secure-cookie-in-prod gap. `trust proxy` is now `config.trustProxy`, driven by `TRUST_PROXY_HOPS` (numeric → spoof-resistant IP resolution); default stays trust-all to preserve multi-hop deploys. config.test.ts covers both; operator note in `docs/UPGRADING.md`.

**Files:** `backend/src/index.ts:126`, `backend/src/index.ts:200`, `backend/src/index.ts:216`, `backend/src/middleware/rateLimiter.ts:8`

**Problem.** helmet() is configured only with crossOriginResourcePolicy (index.ts:126-130): no HSTS for the documented HTTPS-reverse-proxy deployments and no CSP. Session cookie secure is `process.env.SECURE_COOKIES === 'true'` defaulting false with sameSite:'lax' (index.ts:216), so a prod deploy that forgets the flag silently ships non-secure cookies. app.set('trust proxy', true) (index.ts:200) trusts ALL proxies, which is exactly why rateLimiter.ts:8 had to set validate.trustProxy:false — meaning a client can spoof X-Forwarded-For to evade per-IP rate limits (auth/download/image limiters).

**Recommendation.** Reject the HSTS/CSP portion as stated — they are already enabled by helmet's defaults; the finding's "no HSTS / no CSP" claim is factually wrong (verified in helmet 7.2.0 source). If hardening is desired, make the existing defaults explicit and add `hsts: { maxAge: 31536000, includeSubDomains: true, preload: true }`, and only TIGHTEN CSP after testing against the swagger UI and frontend. Keep the two genuinely valid items: (1) Move cookie.secure off `process.env` into config.ts to honor the env-boundary contract, defaulting secure=true in production with an explicit SECURE_COOKIES escape hatch so HTTP local deploys still work. (2) Replace `app.set('trust proxy', true)` with a config-driven `TRUST_PROXY_HOPS` (in config.ts) so a spoofed X-Forwarded-For can't mint fresh per-IP rate-limit buckets; only then drop `validate.trustProxy:false` in rateLimiter.ts:8, and verify express-rate-limit doesn't throw ERR_ERL_PERMISSIVE_TRUST_PROXY. Severity is at most low/medium given the app's explicit single-user self-hosted design and lenient limits (auth 40/15min with skipSuccessfulRequests).

**Safety / pitfalls.** The proposed "enable HSTS only in production" and "add a CSP" steps are based on a false premise and would REGRESS security/break the UI. Helmet 7.2.0 (node_modules/helmet/index.cjs:322-403) applies BOTH CSP and HSTS by default when only crossOriginResourcePolicy is passed (options.contentSecurityPolicy and the HSTS option are undefined -> case undefined -> defaults pushed). The app already sends `Strict-Transport-Security: max-age=15552000; includeSubDomains` on EVERY response and a full default CSP (default-src 'self', upgrade-insecure-requests, etc.). Gating HSTS on production would remove a header that is currently always sent. A hand-rolled CSP for swagger UI risks breaking swagger-ui (needs 'unsafe-inline' styles) and the same-origin Next.js/Tauri frontend. Cookie change: forcing secure from nodeEnv would break the documented local-network HTTP deploys (index.ts:214-215) because secure cookies are never sent over HTTP -> silent login/session breakage; must keep an explicit override and route via config.ts (current `process.env.SECURE_COOKIES` at index.ts:216 already violates the AGENTS.md 'env only via config.ts' contract). Trust-proxy change: hardcoding 1 breaks documented Docker/Portainer multi-hop chains; and dropping validate.trustProxy:false WITHOUT a correct hop count makes express-rate-limit throw ERR_ERL_PERMISSIVE_TRUST_PROXY and crash the limiters.

---

### F36 — jwt.verify omits the algorithms pin and /refresh re-reads the secret inline as `as any`

**✅ complete** (`feat/f36-jwt-pin`) · dimension: security · severity: medium→low · effort: S · risk: low · epic: #11

> **Audit note.** ✓ Severity medium→low (body agrees): the RS256→HS256 forgery is unreachable in jsonwebtoken v9 with a string secret. Defense-in-depth, not a live hole.
>
> **Fix shipped** (#11 Phase 3). A shared `verifyAuthToken(token)` helper (middleware/auth.ts) pins `algorithms: ["HS256"]` and resolves the secret from one validated source. Every auth-class `jwt.verify` now uses it: the 4 middleware sites, the `/refresh` route, and `onboarding.ts` (which had the same inline-`require` + inline-secret + `as any` pattern) — removing all the inline `process.env` reads and casts. The Listen Together socket pins `algorithms` inline. Tests assert `jwt.verify` is called with the algorithm pin. Existing HS256 tokens are unaffected. The segmented-streaming session-token verifies (`sessionService.ts`, a separate `SESSION_TOKEN_SECRET`) are folded into F37's media-token work.

**Files:** `backend/src/middleware/auth.ts:136`, `backend/src/middleware/auth.ts:165`, `backend/src/routes/auth/localCredentials.ts:168`, `backend/src/services/listenTogetherSocket.ts:603`, `backend/src/routes/onboarding.ts:631`

**Problem.** Every jwt.verify across the codebase (auth.ts:136/165/267/295, routes/auth/localCredentials.ts:168, listenTogetherSocket.ts:603, onboarding.ts:631) omits the algorithms option, so tokens are accepted for any algorithm the library will try — a defense-in-depth gap of the alg-confusion class even though signing is HS256. /refresh additionally re-derives the secret inline as `process.env.JWT_SECRET || process.env.SESSION_SECRET!` and decodes `as any` (localCredentials.ts:168-171) instead of reusing JWT_SECRET_VALIDATED and JWTPayload from middleware/auth.ts; onboarding.ts:631 does the same require('jsonwebtoken') + inline-secret pattern. JWT_SECRET silently aliasing SESSION_SECRET (auth.ts:7) also conflates two distinct secrets.

**Recommendation.** Code claims are accurate: NO jwt.verify call pins `algorithms` — auth.ts:136,165,267,295; routes/auth/localCredentials.ts:168; listenTogetherSocket.ts:603; onboarding.ts:631; sessionService.ts:387,790. /refresh (localCredentials.ts:168-171) does re-derive `process.env.JWT_SECRET || process.env.SESSION_SECRET!` inline and decodes `as any`; onboarding.ts:628-631 does a local `require('jsonwebtoken')` + the same inline secret with `as {userId:string}`. Signing is HS256 (jsonwebtoken sign.js defaults alg to HS256; no RS/ES keys anywhere). HOWEVER the severity framing is overstated: this is jsonwebtoken v9.0.3, whose verify.js (lines 132-134) defaults `algorithms` to HS_ALGS=['HS256','HS384','HS512'] ONLY when the secret is a string/Buffer (the case here — a public key is never passed to verify). The classic RS256→HS256 alg-confusion FORGERY is therefore NOT reachable in this codebase; the residual gap is merely that HS384/HS512 are also accepted, which is not a forgery vector. So this is low/defense-in-depth, not a medium exploitable issue. Worthwhile cleanups, in priority order: (a) Centralize the secret in config.ts (contract violation: backend env must go through config.ts, yet config.ts defines only SESSION_SECRET, and JWT_SECRET is read raw from process.env in 4+ files) and export `verifyAccessToken(token)`/`verifyRefreshToken(token)` from middleware/auth.ts that pass `{ algorithms:['HS256'] }` and return a typed JWTPayload. (b) Replace the inline `as any`/inline-secret in routes/auth/localCredentials.ts /refresh and the `require()`+inline-secret in onboarding.ts with those helpers — these are the strongest wins (kills the `as any`, the duplicate fallback logic, and the stray runtime require). (c) Add `{ algorithms:['HS256'] }` to listenTogetherSocket.ts:603 and to sessionService.ts:387/790 in place, WITHOUT collapsing sessionService into the shared helper (it has its own secret + ignoreExpiration + subject + payload guard). Keep the SESSION_SECRET fallback as a logged deprecation, not a hard cutover, to avoid invalidating live access/refresh tokens.

**Safety / pitfalls.** Two real pitfalls. (1) Secret-domain divergence: segmented-streaming/sessionService.ts is NOT a JWT_SECRET consumer — it mints and verifies its OWN token type (SESSION_TOKEN_SECRET, lines 30-31, sign at :782, verify at :387/:790) with extra options (ignoreExpiration, subject, custom payload type guard isSegmentedSessionTokenPayload). Routing it through a generic verifyAccessToken helper would strip ignoreExpiration/subject handling and the payload-shape validation, breaking segment-token decoding. Leave sessionService alone (only add an explicit { algorithms:['HS256'] } pin if desired) — do NOT migrate it to the shared JWTPayload helper. (2) Introducing a distinct non-aliasing JWT_SECRET in config.ts is operationally breaking: per the repo's own memory, docker-entrypoint.sh generates SESSION_SECRET and deployments rely on JWT_SECRET||SESSION_SECRET aliasing; existing issued tokens (24h access, 30d refresh) are signed with the SESSION_SECRET-derived value, so forcing a separate JWT_SECRET with no fallback would invalidate all live sessions and refresh tokens. Keep the fallback (as the proposal says) — make the alias a logged deprecation only, never a hard cutover.

---

### F37 — Long-lived full-privilege JWT embedded in stream/cover-art URLs (?token=) leaks via logs, history, Referer

**⬜ open · DEFERRED-WITH-NOTE (Wave 1)** · dimension: security · severity: high · effort: M · risk: medium · ⚠️ breaking · epic: #12

> **Why deferred (Wave 1).** This is a cross-cutting redesign, not a contained fix: it replaces the full-privilege access token in `?token=` media URLs with short-lived, audience-scoped media tokens across the streaming AND cover-art paths (`requireAuthOrToken` query-token branch + `getStreamUrl`/`getCoverArtUrl` in `frontend/lib/api.ts`). The audit (appendix, F37 flag) explicitly warns it is **riskier than "lowest-risk"**: the cover-art→token migration risks breaking canvas/`<img>` cross-origin loads and the Tauri native client, and it touches both backend auth and the frontend API boundary. Deferred by operator decision to a focused follow-up PR with its own verification (real-app run on web + Tauri), rather than a tail-end change.
>
> **Approach when picked up.** The repo already has the right pattern to build on: the segmented-streaming `sessionService` mints short-lived tokens bound to `sessionId`+`trackId` delivered via the `x-streaming-session-token` header (the same `SESSION_TOKEN_SECRET` jwt.verify sites at `sessionService.ts:387/790` that F36 left unpinned — pin `algorithms:["HS256"]` there as part of this work). Mint a short-lived (minutes), audience-scoped (`media`/`cover`) token per resource instead of reusing the 24h account JWT; prefer a header or a per-resource signed token over the query param where the client allows it; verify canvas/Tauri/cross-origin cover loads still work before cutover.

**Files:** `backend/src/middleware/auth.ts:264`, `backend/src/routes/library.ts:4900`, `frontend/lib/api.ts:1241`, `frontend/lib/api.ts:1390`

**Problem.** requireAuthOrToken (auth.ts:264) accepts the user's 24h access token as a query param, and the frontend appends that same long-lived JWT to <audio>/<img> URLs: getStreamUrl (api.ts:1241), getCoverArtUrl (api.ts:1390/1399) and others. Query-string tokens are logged by reverse proxies/CDNs, persist in browser history, and leak via Referer — and cover-art URLs can be sent to external image hosts. The token is the full access token (not scoped, not short-lived), so a single leaked media URL is reusable account-wide until expiry. The repo already has the better pattern: the segmented-streaming sessionService mints short-lived per-session tokens bound to sessionId+trackId (sessionService.ts) delivered via the x-streaming-session-token header.

**Recommendation.** Confirmed real: requireAuthOrToken (auth.ts:264) accepts the full 24h access token (generateToken, auth.ts:46-63, payload = userId/username/role/tokenVersion — full account scope) as ?token=, and api.ts appends it to <audio> (getStreamUrl 1235), <img> cover-art (getCoverArtUrl 1382, includeToken defaults true, including the external/native cover-art proxy branch at 1413-1416 and the audiobook/podcast branches at 1390/1399), and getBrowseImageUrl (1437). library.ts:1146 applies requireAuthOrToken router-wide, so /tracks/:id/stream (4900) and /cover-art/:id (4092) both honor it. A leaked media URL is reusable account-wide until 24h expiry (only invalidated by tokenVersion bump on password change). Real leak vectors: reverse-proxy/CDN access logs, browser history, and — most concerning — the external cover-art proxy can carry the token alongside ?url=<external host>, and color-extraction/canvas flows. Correct the finding on two points before acting: (a) the existing segmented session pattern does NOT use a separate secret (sessionService.ts:30 reuses JWT_SECRET); its merits are 5-min TTL (SESSION_TTL_SECONDS) + binding to sessionId/trackId/userId (issueSessionToken, 772). (b) That pattern still transports its token via a query param (st=), so 'never in a URL' is aspirational for <audio>/<img>. Tightened recommendation, in priority order: (1) Cover art is the highest-leverage, lowest-risk fix — flip getCoverArtUrl to includeToken=false by default and serve same-origin covers via the existing express-session cookie (the app already supports session auth in authenticateRequest), eliminating the long-lived token from the highest-volume, cross-origin-prone URLs; keep token only where canvas/CORS demonstrably requires it and there gate it behind a scoped token. (2) For audio streaming, prefer migrating clients onto the segmented session-token path (already scoped + short-lived) rather than inventing a parallel scheme; for the legacy /stream endpoint, introduce a short-lived (minutes), trackId-scoped signed token minted per playback, verified server-side, and (per defense-in-depth) signed with a dedicated secret wired through backend/src/config.ts (NOT read directly from process.env, per the env contract) and ideally rotate the segmented token onto that same dedicated secret too. (3) Keep requireAuthOrToken's query-token branch available for API-key/Subsonic compatibility but stop emitting the primary access token into URLs from the frontend. Add a regression test asserting getStreamUrl/getCoverArtUrl never contain the raw access JWT.

**Safety / pitfalls.** Migrating getCoverArtUrl/getStreamUrl is higher-risk than it looks. (1) getCoverArtUrl is called with includeToken=true by default across many components AND is documented as needed for canvas color extraction (cross-origin canvas/CORS) — covers feed <img> tags, native: paths, external-URL proxying, and the cover-art-colors path. Switching to cookie-auth breaks the existing comment's stated reason ("cookies may not be sent cross-origin in development", api.ts:1237) and could break Tauri desktop and cross-origin dev where SameSite/credentials differ. (2) Scoped per-image tokens for cover art are awkward because a single page renders dozens of covers; a naive per-request mint would add latency/round-trips. (3) The finding asserts the segmented session token uses a SEPARATE secret — it does NOT: SESSION_TOKEN_SECRET (sessionService.ts:30) = JWT_SECRET || SESSION_SECRET || config.sessionSecret, the same primary secret. So 'reuse the existing better pattern' and 'use a separate secret' are partially contradictory; the separate-secret part is a NEW requirement, not an existing pattern. (4) Even the segmented 'better pattern' still ships its token in a query param (st=, SEGMENTED_SESSION_TOKEN_QUERY_PARAM, streaming route reads req.query at line 40), so the proposal's framing of 'never put a token in a URL' is not what the codebase's own better pattern does — for <audio>/<img> a URL-borne credential is largely unavoidable; the real fix is scope+short-TTL, not URL avoidance. (5) requireAuthOrToken's query-token branch is also used by Subsonic/API-key clients and possibly external apps; removing it outright would break those. AGENTS.md contract (Prisma-only, config.ts for env) is not violated by this change, but any new secret must be wired through backend/src/config.ts, not read directly from process.env.

---

### F38 — ML model artifacts and Python deps are pulled at build time with no checksum/hash pinning (supply chain)

**✅ complete (PR #120)** · dimension: security · severity: medium · effort: M · risk: low · epic: #10

> **Audit note.** Per audit: internal contradiction with F50 on the Essentia model count. CORRECTED 2026-07-11 — the CLAP-checkpoint clause above was backwards: the measured artifact is 2,352,471,003 bytes (~2.35 GB; HF `x-linked-size`/`Content-Length`, measured 2026-07-11), not ~600MB. The repo's own "~600MB" comment was the wrong figure, not the ~2GB estimate the original audit note dismissed. Now fixed — see the shipped change below.
>
> **Fix shipped.** Implements Recommendation items (a) and (b) in all three Dockerfiles named in the Safety/pitfalls note below. (a) Every build-time model `curl` — `services/audio-analyzer/Dockerfile` (10 Essentia `.pb` heads), `services/audio-analyzer-clap/Dockerfile` (the CLAP `.pt` checkpoint), and the root AIO `Dockerfile` (which duplicates both) — is now immediately followed, in the same `RUN` layer, by `echo "<sha256>  <path>" | sha256sum -c -`; a digest mismatch fails the build at that layer instead of baking in a tampered or silently re-published artifact. Digests measured 2026-07-11: 10 Essentia `.pb` files (≈3.9 MB total, re-hashed locally as a cross-check) and the CLAP checkpoint (2,352,471,003 bytes / ~2.35 GB — HF API `lfs.oid`, cross-checked by the build's own `sha256sum -c` against the actual downloaded bytes). (b) The CLAP downloads (per-service + AIO) also swap the mutable `resolve/main` ref for the immutable pinned commit `resolve/b3708341862f581175dba5c356a4ebf74a9b6651/`, so the source ref itself is pinned, not just the bytes. (c) Registry mirroring is **declined this cycle**: single-operator homelab calibration — sha256 verification already delivers the integrity property (tamper/MITM/re-publish detection); a project-owned registry mirror would add ongoing storage/sync operational burden with no additional security benefit at this deployment's scale. (d) Python dependency lockfile/hash-pinning is tracked separately as **F50** (companion packet, same cycle). (e) pip-audit + Trivy CI scanning already shipped earlier in **#63 / F45** — the Problem paragraph's "no pip-audit/CVE scan in CI" line below is now stale and has been corrected in place. Also corrected two stale size comments in the root Dockerfile: the Essentia models "(~200MB total)" → "(~4MB total)" (measured ≈3.9 MB) and the CLAP checkpoint "(~600MB)" → "(~2.35 GB / 2,352,471,003 bytes)".

**Files:** `services/audio-analyzer/Dockerfile:78`, `services/audio-analyzer-clap/Dockerfile:39`, `services/ytmusic-streamer/requirements.txt`, `services/audio-analyzer-clap/requirements.txt`

**Problem.** The Essentia Dockerfile curls ~10 .pb models from essentia.upf.edu and the CLAP Dockerfile curls a ~2GB .pt checkpoint from Hugging Face with no sha256 verification — a compromised/MITM'd or silently re-published upstream is baked in and executed as model weights with no detection. In parallel, every services/\*/requirements.txt uses unbounded `>=` floor pins (torch>=2.0.0, transformers>=4.30.0, fastapi>=0.115.0, yt-dlp>=2025.1.0) with no lockfile or hashes, so two builds of the same commit can ship different, untested transitive trees (notably the torch/transformers ABI). There is also no pip-audit/CVE scan in CI at the time of this review — since shipped, see F45/PR #63 (this line is stale; corrected 2026-07-11).

**Recommendation.** Confirmed and slightly under-scoped. Exact state: services/audio-analyzer/Dockerfile:53-76 curls 10 .pb models from essentia.upf.edu via plain `curl -L` (line 78/79 is only `ls -lh`, not verification); services/audio-analyzer-clap/Dockerfile:34-42 curls the ~2GB .pt from huggingface.co/lukewys/laion_clap using the MUTABLE `resolve/main` ref with no hash; the identical downloads are also in root Dockerfile:60-118 (AIO image, uncited). No sha256sum/--require-hashes/lockfile anywhere under services/; all requirements.txt (clap, ytmusic-streamer, audio-analyzer, tidal-downloader) use unbounded `>=` floors; no pip-audit/Trivy/grype/safety/SBOM in any of the 4 .github/workflows. Tightened fix: (a) Pin every model by sha256 and `sha256sum -c -` immediately after each curl, in ALL THREE Dockerfiles (per-service + root AIO); fail the build on mismatch. (b) For the HF checkpoint additionally swap `resolve/main` -> `resolve/<immutable-commit-sha>/` so the source ref itself is pinned. (c) Best: mirror both model sets into the project GHCR/registry and pull from there to remove the upstream runtime dependency. (d) Adopt uv (or pip-tools) per service with a committed hash lock installed via `uv pip sync --require-hashes`, validated on linux/amd64; keep yt-dlp/ytmusicapi on a deliberately loose, commented constraint (homelab YT fix depends on a recent yt-dlp). (e) Add pip-audit + Trivy image scan in CI, but introduce non-blocking first (ubuntu:20.04 base in audio-analyzer is EOL and torch will surface CVEs) then ratchet to gating on HIGH/CRITICAL.

**Safety / pitfalls.** The remediation is build-tooling only and touches no runtime code or the AGENTS.md app contracts (frontend api.ts, backend config.ts, Prisma/no-raw-SQL) — those are orthogonal. Real operational pitfalls to flag: (1) The same unverified model curls are DUPLICATED in the root AIO /home/tony/projects/soundspan/Dockerfile:60-118 (not cited in the finding); any sha256 fix must be applied in BOTH the per-service Dockerfiles AND the root Dockerfile or the AIO image stays vulnerable. (2) `uv pip sync --require-hashes` will break builds for essentia-tensorflow/laion-clap, which historically ship platform-specific wheels with shifting transitive trees — generating a working hash-locked file for torch+essentia on linux/amd64 is non-trivial and must be validated against the actual build platform (image-builds.yml pins platforms: linux/amd64, and CLAP/AIO run on self-hosted). (3) yt-dlp/ytmusicapi MUST stay loose (documented in MEMORY: the android-client fix depended on a recent sidecar yt-dlp); a blanket lock would regress YT downloads — the finding already carves this out, keep it. (4) Gating release on Trivy HIGH/CRITICAL will likely fail immediately given ubuntu:20.04 base (EOL April 2025) in audio-analyzer and torch CVEs — introduce as non-blocking/allowlisted first, then ratchet, or the release pipeline hard-stops.

---

### F39 — No shared MediaSource/DownloadProvider abstraction: each acquisition source is a bespoke singleton wired into the orchestrator with hardcoded if/else routing

**⬜ open** · dimension: extensibility · severity: high · effort: L · risk: medium · epic: #18

**Files:** `backend/src/services/acquisitionService.ts:289`, `backend/src/services/acquisitionService.ts:329`, `backend/src/services/acquisitionService.ts:188`, `backend/src/services/simpleDownloadManager.ts:12`, `backend/src/services/soulseek.ts:54`, `backend/src/services/lidarr.ts:96`, `backend/src/services/youtubeDownload.ts:170`

**Problem.** The acquisition pipeline has no provider interface. acquisitionService.ts hardcodes the source universe as the literal union `"soulseek" | "lidarr"` (lines 61, 83-85, 188-189) and dispatches via a copy-pasted if/else ladder: acquireAlbumInternal (289-391) is a ~100-line block that branches `if (behavior.primarySource === "soulseek") { acquireAlbumViaSoulseek; ...fallback lidarr } else if ("lidarr") { acquireAlbumViaLidarr; ...fallback soulseek }` — the primary/fallback logic is literally duplicated and mirrored per source. The two acquire methods (acquireAlbumViaSoulseek:491, acquireAlbumViaLidarr:682) re-implement attempt counting, status-text, and job updates independently. Each provider class exposes a divergent surface and error model (soulseek: `{success,error}` tuples + searchAndDownloadBatch; lidarr: thrown AcquisitionError + searchAlbum; youtubeDownload: thrown axios errors + startDownload). simpleDownloadManager.ts:12 imports lidarrService directly, so the orchestrator is statically bound to one provider. Adding a 3rd acquisition source (e.g. a new indexer, or wiring the already-clean youtubeDownload into batch acquisition) requires editing the union type, the behavior matrix, the dispatch ladder, AND writing another bespoke acquireAlbumViaX method — the opposite of an open/closed boundary. This is the single biggest extensibility blocker in the backend.

**Recommendation.** The extensibility problem is real and accurately described: hardcoded "soulseek"|"lidarr" union (acquisitionService.ts:61,83-85), a mirrored copy-paste primary/fallback if/else ladder (329-382 with a dead else at 383), two bespoke acquireAlbumViaX methods each re-implementing attempt-counting and job updates, divergent provider surfaces/error models (soulseek {success,error}+searchAndDownloadBatch; lidarr thrown AcquisitionError+addAlbum/searchAlbum; youtube thrown axios+startDownload), and simpleDownloadManager.ts:12 statically importing lidarrService. youtubeDownload is genuinely NOT in the acquisition pipeline (only routes/youtube.ts imports it), so adding it would require editing union+matrix+ladder+a new method — confirming the open/closed violation. The core proposal (MediaSource interface + Map registry + single prioritized buildProviderChain loop, promote AcquisitionError/AcquisitionErrorType from lidarr.ts into a shared module, adapt the two existing providers behind it then delete the dispatch) is sound and is the right direction. CORRECTION: do NOT key the registry off CANONICAL_MEDIA_SOURCE_VALUES from packages/media-metadata-contract — that enum is ["local","tidal","youtube","youtube-direct"] (playback/stream sources) and contains neither "soulseek" nor "lidarr"; keying off it would not typecheck. Introduce a distinct AcquisitionSource id type (e.g. "soulseek"|"lidarr"|"youtube") for the acquisition axis. Also budget for rewriting acquisitionService.test.ts (it spies on the soon-to-be-removed private methods) and for the AGENTS.md "smallest-safe-change" culture: land the interface+adapters behind unchanged behavior first as one PR, the dispatch deletion + test rewrite as a second. Treat youtube wiring as a follow-up, not part of the refactor, since it has no MBID/album acquisition path today.

**Safety / pitfalls.** Three concerns. (1) Test coupling: acquisitionService.test.ts:404 uses jest.spyOn(svc, "acquireAlbumViaSoulseek"/"acquireAlbumViaLidarr") by name and asserts dispatch routing. The "no behavior change, covered by existing tests" claim holds only for the introduce-adapters step; the final delete-the-dispatch step renames/removes those private methods and WILL break these spies, requiring test rewrites. (2) Divergent success semantics must be preserved: lidarr returns success:true on grab/start (webhook completes later, acquisitionService.ts:718-728) while soulseek returns success only after a synchronous 50%-track threshold (617-655). A unified acquireAlbum()->AcquisitionResult and the prioritized loop's `if(result.success) break` must keep these distinct meanings; an adapter that normalizes "success" to "completed" would change fallback timing. (3) Attempt-count duplication: simpleDownloadManager.startDownload itself writes lidarrAttempts/statusText (lines 184-209), overlapping acquisitionService.updateJobStatusText (705); a lidarr adapter must not double-increment. None of these are blockers, but they invalidate the "behavior-preserving, fully test-covered" framing of the second half.

---

### F40 — Frontend api.ts is a 3693-LOC monolithic ApiClient (286 methods) hand-mirroring a backend OpenAPI spec that already exists — drift-prone and untyped via file-wide ApiData=any

**⬜ open** · dimension: extensibility · severity: high · effort: L · risk: medium · epic: #16

> **Audit note.** ✓ Re-homed to #16 (frontend decomposition) — the `api.ts` structural split was previously unowned (#15 type-safety disclaimed the structure, #18 disclaimed it as a follow-up). Overlaps F4. swagger schema count is larger than the title's 4.

**Files:** `frontend/lib/api.ts:376`, `frontend/lib/api.ts:405`, `backend/src/config/swagger.ts:104`, `backend/src/config/swagger.ts:130`

**Problem.** lib/api.ts is one 3693-line class with 286 call helpers (`this.get/post/...` count), each manually re-typing request/response shapes. A single `type ApiData = any` (line 376) with an eslint-disable backs ~91 occurrences (`tracks: ApiData[]`, `request<{ artists: ApiData[] }>`), so every consumer of getAlbums/getRecommendations/etc. receives `any` and loses compile-time safety. Crucially the contract is NOT missing on the backend: backend/src/config/swagger.ts already builds a complete `swaggerSpec` (OpenAPI 3.0) by scanning ./src/routes/_.ts, with reusable component schemas (User/Artist/Album/Track) defined at swagger.ts:43-104. The frontend re-derives all of this by hand, so server and client shapes silently diverge with no compile-time link. The whole class is also a single `api` singleton with no domain boundaries, so every consumer pulls the entire surface (auth, library, podcasts, downloads, vibe, streaming) and the file is unownable per-feature, even though the codebase otherwise organizes by features/_.

**Recommendation.** Confirmed structurally: api.ts is one 3693-line ApiClient (export const api = new ApiClient() at L3693), ~273-286 methods, type ApiData=any at L376 with eslint-disable used 91x; features/_ seam (10 domains) exists and every consumer imports the whole singleton. The monolith + any-typed-surface + no-per-feature-ownership problem is REAL and present now. The split-along-features/_ and keep-the-transport-core half of the proposal is sound and the lowest-risk win — do that first (api-core.ts holding request/get/post + auth/refresh/timeout + the SHARED inFlightGetRequests map; per-feature modules compose onto the one singleton for back-compat). Correct/temper the generate-types-from-spec half: (1) The premise that the backend already has an authoritative contract is only partially true — swagger.ts's `apis` array references ./src/config/swaggerSchemas.ts which DOES NOT EXIST (only 4 inline schemas User/Artist/Album/Track at swagger.ts:43-104 are real), and across 457 route handlers those component schemas are $ref'd in responses only ~6 times (the other 15 refs are Error). (2) The @swagger jsdoc (397 hand-written blocks) is ITSELF a hand-maintained drift surface, not generated — and most response bodies are inline `type: object` or bare `type: object` with no properties, so openapi-typescript would emit Record<string,unknown>/unknown for the bulk of getAlbums/getRecommendations/etc., i.e. ApiData=any renamed, not real safety. So generated types will NOT eliminate drift unless the backend schemas are first tightened (e.g. derive request/response DTOs from Prisma/zod and $ref them). Recommend reordering: split-by-feature now (mechanical, contract-preserving); treat type-generation as a forcing function to backfill real $ref component schemas in routes before it pays off; also handle the /api path-prefix mismatch (spec documents /api/library, client calls /library + appends /api).

**Safety / pitfalls.** The api.ts singleton is the contractually-mandated frontend API boundary (AGENTS.md: "Use frontend/lib/api.ts as the frontend API boundary. No direct fetch from components"). The proposed file split MUST preserve the `import { api } from "@/lib/api"` surface and the singleton's cross-cutting behavior, or it breaks the contract and ~all feature consumers. Concrete behaviors that must survive a split: bearer-token load/refresh-on-401, request timeout + 1 retry (MAX_TIMEOUT_RETRIES), in-flight GET dedup (inFlightGetRequests Map at line 409 — splitting domain methods into separate modules that each hold their own core instance would silently disable dedup unless they share ONE core instance), and dynamic baseUrl resolution incl. the `/api` prefix appended in toAbsoluteApiUrl/cover-url paths (1388/1397). Second, replacing `ApiData=any` with generated types is NOT behavior-neutral the way the proposal implies: frontend tsconfig has strict:false, and many response schemas in routes are bare `type: object` with no properties; openapi-typescript will emit `Record<string,unknown>`/`unknown` for those, which under strict:false vs the current `any` can surface new property-access errors in 91 call sites' consumers and force narrowing churn. Sequence it per-domain behind compile checks, not file-wide.

---

### F41 — AudioEngine interface declares ~13 optional methods, forcing runtime typeof-capability checks instead of a typed capability split

**⬜ open** · dimension: extensibility · severity: medium · effort: M · risk: low · epic: #18

> **Audit note.** Per audit: ~11 optional methods (not ~13); the Tauri adapter does not implement `reload`. The capability-split argument holds.

**Files:** `frontend/lib/audio-engine/types.ts:141`, `frontend/lib/audio-engine/index.ts:386`

**Problem.** The AudioEngine contract (types.ts:119-158) makes the core transport (load/play/pause/stop/seek/volume/time/events) required, but bolts on 11 optional streaming-only capabilities (reload?, refreshManifest?, quarantineRepresentation?, clearRepresentationQuarantine?, getActualCurrentTime?, hasTrackEnded?, notifyTrackEnded?, isCurrentlySeeking?, getSeekTarget?, plus destroy?/preload?). Because they are optional on the base interface, the HybridRuntimeAudioEngine and orchestrator must guard each with `typeof engine.method === 'function'` and synthesize fallbacks at runtime (index.ts:386+). This blurs the contract — callers cannot trust capabilities at the type level, the same fallback logic is duplicated rather than enforced, and adding a 4th engine adapter gives no compiler guidance on which advanced hooks it must implement to participate in DASH failover. The DASH-only concepts (representation quarantine, manifest refresh) leak into the base interface that Howler/Tauri engines have no business implementing.

**Recommendation.** Accept the core extensibility critique but correct two factual errors before acting. CORRECTION A: getActualCurrentTime/hasTrackEnded/notifyTrackEnded/isCurrentlySeeking/getSeekTarget/reload/preload are NOT segmented-only — Howler AND Tauri implement all of them concretely (howlerEngineAdapter.ts:354-375, tauriNativeEngineAdapter.ts:331-349). Do not relegate these to a no-op mixin; promote them to REQUIRED on the base AudioEngine interface (or keep optional only destroy/preload). Only the three true DASH concepts — refreshManifest, quarantineRepresentation, clearRepresentationQuarantine — should move into a new `interface SegmentedAudioEngine extends AudioEngine`. CORRECTION B: a second-layer type already exists — RuntimeAudioEngine (index.ts:70-86) redeclares every method as required and is what createRuntimeAudioEngine() returns and what AudioPlaybackOrchestrator.tsx consumes (line 390). Therefore the typeof guards in the ORCHESTRATOR (lines 1210, 3464, 3823, 4618, 4743, 5078) are already redundant against their declared type and are the cheapest, safest cleanup — they can be deleted outright once the methods are required, no new abstraction needed. The remaining 9 typeof guards live INSIDE HybridRuntimeAudioEngine (index.ts:386-460) and are load-bearing; the smallest-safe improvement there is a structural `isSegmented(engine): engine is SegmentedAudioEngine` guard applied to `this.getActiveEngine()` per call (collapsing 3+ typeof checks into one named guard for the DASH trio), NOT a one-time instanceof narrow. Net: a real readability/extensibility win, but downgrade from 'medium impact across the contract' to a localized two-part cleanup, and keep the structural-guard + lazy-import constraint front of mind.

**Safety / pitfalls.** Three concrete risks. (1) The fallback typeof guards inside HybridRuntimeAudioEngine (frontend/lib/audio-engine/index.ts:386-460) are LIVE, not dead: Howler (howlerEngineAdapter.ts) and Tauri (tauriNativeEngineAdapter.ts) implement NONE of reload/refreshManifest/quarantineRepresentation/clearRepresentationQuarantine (grep count = 0), and the hybrid swaps its active inner engine per-source at runtime (getActiveEngine()), so replacing them with a one-time `isSegmented(engine)` guard would break failover/manifest-refresh when Howler is active — the guard must re-evaluate against the CURRENT active engine each call, which the existing per-call typeof already does. (2) An `instanceof VideoJsSegmentedEngine` guard would defeat the lazy-import design: videoJsSegmentedEngine.ts is dynamically imported only on DASH sources (index.ts:63-68) precisely to keep the ~500KB video.js dependency out of the initial bundle; an `instanceof` reference at the consumer would statically pull it in. Use a structural/capability type guard, not instanceof. (3) frontend tsconfig has strict:false, so a SegmentedAudioEngine split buys far less compile-time safety than the proposal implies — undefined method access won't be flagged the way the finding assumes.

---

### F42 — ~50-entry recommender registry is hand-maintained with one near-duplicate bespoke method per mix — adding a recommender requires editing the orchestrator

**⬜ open** · dimension: extensibility · severity: medium · effort: L · risk: medium · epic: #18

> **Audit note.** Per audit: registry is ~38 entries (not ~50). `generateMoodTagMix` (`programmaticPlaylists.ts:2568`) is ✓ dead — only its own test calls it; not in the `mixGenerators` registry (delete candidate).

**Files:** `backend/src/services/programmaticPlaylists.ts:457`, `backend/src/services/programmaticPlaylists.ts:474`, `backend/src/services/hybridSimilarity.ts:104`

**Problem.** generateAllMixes (programmaticPlaylists.ts:457) hard-codes a ~50-item `{ fn, weight, name }` array (the array runs from line 474 well past 650). Each entry points at a bespoke 60-90 line method (generateEraMix, generateGenreMix, generatePartyMix, generateChillMix, ...) that repeats the same shape: query-by-feature, genre fallback, min-track gate, shuffle, slice, build coverUrls, return ProgrammaticMix. The differences between most generators are purely declarative (a where-clause, genre seeds, a label/weight). Adding a recommender means BOTH writing another bespoke method AND editing the central array — the inverse of open/closed. Meanwhile hybridSimilarity.ts:104 is a genuinely modern recommender (clean feature-flag fan-out, candidate over-fetch + re-rank) that lives entirely outside this registry, so the two recommender styles can't be composed or weighted together.

**Recommendation.** Confirmed in code, with corrections. generateAllMixes (programmaticPlaylists.ts:457) hard-codes a {fn,weight,name} array at lines 474-688 — ~38 registered entries (the "~50" is an overcount; the file defines 44 generate\* methods total). The bespoke generators are genuinely near-duplicate: the "curated vibe" set (SadGirlSundays 2929, MainCharacterEnergy 2993, VillainEra 3048, 3AMThoughts 3104, ...) differ ONLY in a Prisma where-clause + label/description/color, then share an identical tail (randomSample -> coverUrls = filter(coverUrl).slice(0,4).map -> return ProgrammaticMix); .slice(0,4) appears 43×, getMixColor 44×. Strong corroboration the teammate missed: generateMoodTagMix (line 2600) is ALREADY a parameterized generic builder (moodTag, mixName, mixDescription -> ProgrammaticMix) — but it is dead code, registered nowhere, proving both that the spec-array approach is feasible AND that a prior generic attempt was abandoned. hybridSimilarity.findSimilarTracks (hybridSimilarity.ts:104) is confirmed a clean feature-flag fan-out (hybrid/CLAP-only/features-only, candidate over-fetch via CANDIDATE_MULTIPLIER + re-rank) living entirely outside the registry: programmaticPlaylists.ts never imports it; the only caller is routes/vibe.ts:519. So the two recommender styles genuinely can't be composed/weighted together. Tightened recommendation: (a) FIRST extract the shared tail as a buildMix(tracks, {id,type,name,description,color,limit,selector}) helper and a buildMixFromSpec(spec) that runs the Prisma query — TDD-guarded by the existing tests. (b) Migrate ONLY the purely-declarative curated-vibe + mood-tag mixes to a spec array; keep genuinely-unique generators (Era, Genre, TopTracks, Rediscover, Day/Sunday/Monday/Friday, multi-strategy Happy/RoadTrip) as custom Recommenders. (c) Preserve every per-mix knob (selector strategy, minTracks gate, output size, day-gate, fallback chain) as explicit spec fields. (d) Keep the deleted methods as thin delegating wrappers so behavior/spies tests stay green; update tests in the same change. (e) Define the Recommender interface and adapt hybridSimilarity as one Recommender so similarity- and feature-based mixes can weight together — but treat its $queryRaw/no-raw-SQL contract violation as a separate finding, not solved here. Net: medium-value extensibility cleanup, real, and safe IF the heterogeneity knobs and the tested public-method contract are preserved.

**Safety / pitfalls.** Three concrete pitfalls a "one generic builder + pure-data spec" rewrite must respect or it will silently change behavior / break the suite: (1) The individual public generator methods are a TESTED CONTRACT — backend/src/services/**tests**/programmaticPlaylistsBehavior.test.ts enumerates them by name and programmaticPlaylistsCuratedMixes.test.ts does jest.spyOn(service, "generateSundayMix"/"generateMondayMix"/"generateFridayMix"). Deleting these named methods in favor of data breaks those tests; the safe path keeps thin named wrappers that delegate to the shared builder so the public surface and spies survive. (2) Real per-mix heterogeneity that a naive generic builder would flatten: three distinct selection strategies (inline PRNG sort ×9, randomSample ×21, diversifyTracksUniqueFirst+backfillFromLibraryForDiversity ×5), three min-track gates (length<15 ×36, <8 ×11, <5 ×3), differing output sizes (TRACK_LIMIT=20 vs DAILY_TRACK_LIMIT=10 vs WEEKLY=20), day-of-week gating (getDay() !== 0 / !== 1), and multi-strategy fallback chains (Happy/RoadTrip: enhanced ML query -> standard heuristics -> genre-pattern fallback). These must become explicit knobs on the spec, not be assumed-uniform — the tests pin exact trackCounts (e.g. 20, 10, 17) and null-on-too-small behavior. (3) hybridSimilarity.findSimilarTracks uses prisma.$queryRaw (pgvector <=>), which already violates AGENTS.md "no raw SQL"; wrapping it as a Recommender is orthogonal and neither fixes nor worsens that, but don't let the wrapper claim to address it.

---

### F43 — Per-external-source coupling: each media source is a parallel stack (route file + service singleton + frontend URL builder) with no unifying stream-provider seam, despite a CanonicalMediaSource contract already existing

**⬜ open** · dimension: extensibility · severity: medium · effort: L · risk: medium · epic: #18

**Files:** `backend/src/routes/youtube.ts:1`, `backend/src/routes/youtubeMusic.ts:1`, `backend/src/routes/tidalStreaming.ts:1`, `backend/src/services/tidalStreaming.ts:129`, `packages/media-metadata-contract/src/index.ts:3`

**Problem.** Each playback source is wired end-to-end as an independent vertical with no shared streaming-provider interface: separate route files mounted at distinct prefixes (/api/youtube, /api/ytmusic, /api/tidal-streaming in index.ts:309-311), separate service singletons (tidalStreaming.ts:129, youtubeMusic.ts, youtubeDownload.ts) each implementing their own getStreamProxy/range-forwarding, and separate frontend URL builders. The shared contract package already defines exactly the seam that's missing — `CANONICAL_MEDIA_SOURCE_VALUES` and `CanonicalMediaSource` (media-metadata-contract/src/index.ts:3-10) plus normalize/resolve helpers — but it is used for metadata normalization, NOT to drive a provider registry for streaming/search. So adding a new playback source means creating a whole new route file, a new service singleton, a new frontend builder, and threading another sourceType literal through, rather than registering one provider. The contract proves the team identified the right abstraction (canonical source enum) but stopped at metadata and didn't extend it to behavior.

**Recommendation.** Confirmed and stronger than stated. The parallel-stack coupling is real and pervasive: three route files mounted at distinct prefixes (index.ts:309-311), three service singletons each with a near-identical getStreamProxy(id, quality, rangeHeader) + getStreamInfo (tidalStreaming.ts:420, youtubeMusic.ts:550, youtubeDownload.ts:219), three near-identical frontend URL builders (api.ts:3225 getYtMusicStreamUrl, 3305 getYouTubeStreamUrl, 3472 getTidalStreamUrl), and — the strongest evidence the teammate omitted — the per-source if/else switch on `streamSource` is duplicated at least 3 times inside AudioPlaybackOrchestrator.tsx (lines 4343-4349, 5273-5282, 6383-6395), each repeating the same tidal/youtube/youtube-direct/local branch to pick a builder. The contract already half-completes the seam: youtubeMusic.searchCanonical (youtubeMusic.ts:477) and the tidal service both emit the shared CanonicalMediaSearchResult, and toAudioEngineSourceType (contract index.ts:185) is already used in the orchestrator (line 210) — so search and source-normalization are unified but stream-URL/proxy behavior is not. Tighten the proposal as follows: (a) Put ONLY the pure StreamProvider type in the contract package; keep getStreamProxy returning a backend stream type, not in the shared package. (b) On the frontend, the highest-value, lowest-risk first step is replacing the triplicated streamSource switch in AudioPlaybackOrchestrator with a single api.getStreamUrl(canonicalSource, providerIdentity) that internally dispatches to the existing builders — this collapses ~9 duplicated branches and is independently shippable before any backend router work. (c) On the backend, extract the shared response-header-forwarding/pipe logic into one helper first (smallest safe), then introduce the registry + generic router with legacy prefixes kept as shims. (d) Model the provider asymmetries explicitly (id type, optional userId, public/authenticated variant, per-provider guard middleware) rather than flattening them. Severity medium is fair — this is extensibility/maintainability, not a correctness or security defect.

**Safety / pitfalls.** Three concrete pitfalls the proposal must respect, none of which refute the finding but which constrain the adapter shape: (1) Contract-package purity. packages/media-metadata-contract has ZERO runtime dependencies and is imported by BOTH frontend and backend. The StreamProvider _interface_ can live there only as a pure type (no axios/express/prisma imports). The registry + adapters MUST stay backend-side (they import axios/prisma), or you pull backend-only runtime deps into a package the Next.js frontend bundles. The finding already scopes adapters to the backend, so this is satisfiable but easy to get wrong. (2) Provider asymmetries that a naive single interface erases: tidal track id is `number`, youtube/ytmusic ids are `string`; tidal/ytmusic getStreamProxy take `userId` (per-user OAuth) while youtubeDownload.getStreamProxy takes none; auth guards differ per source (requireTidalStreamingEnabled + ensureUserOAuth vs requireYtMusicEnabled vs plain requireAuthOrToken on youtube); and ytmusic uniquely has BOTH authenticated `/stream` and `/stream-public` endpoints selected via a `usePublic` flag (api.ts:3225-3235, route youtubeMusic.ts:1338). A unified getStreamUrl(source,id) and StreamProvider must model id-type, userId-optionality, the public/authenticated variant, and per-provider guards or it will silently break ytmusic public playback and tidal/ytmusic per-user auth. (3) A generic `/api/stream/:source/*` router must preserve the exact response-header forwarding currently done per-route (Accept-Ranges, Content-Range, Content-Type passthrough, status code from upstream, range header forwarding, and the headersSent guard) — these are subtly duplicated-but-consistent today (youtube.ts:174-197, tidalStreaming.ts:923-953); a shared pipe helper is actually a net win here. As long as the legacy prefixes are kept as shims (as proposed) and the frontend continues to call through frontend/lib/api.ts (not direct fetch), the AGENTS.md contract is not violated.

---

### F44 — library.ts (305KB) mixes HTTP, business logic, and 121 direct Prisma calls in one god-route, blocking reuse and a controller/service/repository layering

**✅ complete (#124)** · dimension: extensibility · severity: medium · effort: L · risk: medium · epic: #14 · duplicate of F2

> **Audit note.** ✓ Duplicate of F2 (same god-file); kept for the controller/service/repository angle. Don't double-count in dimension tallies.
>
> **Fix shipped (#124).** Completed through the audit-corrected first-stage scope documented in F2: named exported handlers, shared utility/service extraction, typed sort maps, and ordered per-resource sub-routers behind the unchanged mount facade. The larger typed LibraryService/Prisma-access extraction remains a separate follow-up and was deliberately not folded into this readability-only change.

**Files:** `backend/src/routes/library.ts`, `backend/src/routes/library/*.ts`, `backend/src/utils/library*.ts`, `backend/src/services/libraryMaintenance.ts`

**Problem.** library.ts is a 305KB / 8324-LOC route god-file that fuses three layers: HTTP handling, business logic (counts-readiness fallbacks, genre aggregation, randomization), and data access (121 direct prisma.\* calls, untyped `let where: any = {}` query objects built inline, 6 raw $queryRaw blocks). There is no LibraryService, so the same artist/album/track query logic cannot be reused by subsonic.ts or artists.ts (which re-implement similar queries), and none of it is testable without spinning up HTTP. The routes already group cleanly by resource prefix (/artists, /albums, /tracks, /genres) but live in one file. This is an extensibility problem, not just readability: any new consumer of library data (a new route, a new client, the recommender pipeline) must either go through HTTP or copy the inline Prisma logic, and there is no repository seam to swap query strategies.

**Recommendation.** The structural problem is real and well-cited: library.ts (8324 LOC, 121 prisma calls, 6 $queryRaw, 3 `let where: any`) genuinely fuses HTTP + business logic (countsReady fallback at 1806, genre aggregation, shuffle randomization) + inline data access, with no service/repository seam, and subsonic.ts independently re-queries prisma.artist/album.findMany (e.g. line 908) — so the duplication and lack-of-reuse premise holds. Extracting a query-owning service that returns typed DTOs, then splitting the route file by the already-clean prefix boundaries, is the correct direction. BUT correct two inaccuracies before acting: (a) The duplication partner is subsonic.ts (92 prisma calls, its own where-clauses), NOT artists.ts — artists.ts is external-metadata discovery with a single prisma.userSettings call and re-implements nothing, so do not justify the work by "artists.ts re-implements similar queries." (b) The "untestable without HTTP" claim is false: six library\*.test.ts files already unit-test handlers by extracting them from router.stack with mocked prisma — so the win is replacing fragile stack-introspection + mock boilerplate with clean service-level unit tests, not enabling testing that is currently impossible. Keep the proposed smallest-safe sequence (lift data-access into a behavior-preserving service consumed by existing handlers → add service unit tests → then physically split), but add: preserve the $transaction/30s-timeout boundary and the countsReady branch selection verbatim, and migrate the existing router.stack-based tests when the file is split. Given the medium severity and that handlers are already tested, treat this as a high-value-but-non-urgent extensibility cleanup, not a correctness fix.

**Safety / pitfalls.** The mechanical risk is the AGENTS.md "All DB access through Prisma. No raw SQL" contract — but that is not violated by the refactor itself (the 6 $queryRaw blocks are pre-existing and would just move into a service; none are *Unsafe, so no injection risk and no new contract debt is introduced). Real behavioral risks: (1) The /artists and /tracks/shuffle paths use prisma.$transaction with an explicit 30s timeout (lines 1867-1901) and the countsReady readiness-fallback (line 1806) that silently switches between denormalized-count and JOIN-based WHERE clauses — extracting this into a service must preserve both the transaction boundary AND the fallback branch selection exactly, or fresh-DB/pre-backfill installs will start returning 0 results. (2) Six existing library test files reach handlers via `(router as any).stack.find(...).route.stack[i].handle`; physically splitting library.ts into albums.ts/tracks.ts/genres.ts will break every one of those stack-index lookups, so the split must update or replace those tests in lockstep. (3) The /albums "owned" filter rewrites `where` into an AND/OR shape conditionally (lines 3168-3177) — a naive DTO extraction can drop the AND-wrapping and change results.

---

### F45 — CI has no security/vulnerability scanning and no dependency automation (Dependabot/Renovate)

**🟡 partial (PR #63)** · dimension: dependencies-build · severity: high · effort: M · risk: low · epic: #10

> **Audit note.** ✓ The original body's 'FIVE workflows' is wrong — there are THREE (helm-chart-release, image-builds, quality-visibility) and SEVEN Dockerfiles. (The former AWM-health PR-gate workflow was removed in the ACM→AWM rename, dropping the count from four to three.) The CI-gaps thesis (no CVE/SAST/dep-scan) holds.
>
> **Fix shipped.** `.github/dependabot.yml` opens weekly, grouped minor/patch PRs across the 4 npm manifests (`/`, `/backend`, `/frontend`, `/packages/media-metadata-contract`), the 4 pip services, all 7 Dockerfile directories, and `github-actions`. `.github/workflows/security-scanning.yml` adds a Trivy filesystem scan (CRITICAL/HIGH, `ignore-unfixed`, findings triaged via `.trivyignore`), a gitleaks secret scan via the OSS binary directly (not `gitleaks-action`, which needs an org license), and a `pip-audit` sweep of the 6 sidecar requirements files; `.github/workflows/image-security-scanning.yml` adds the matching Trivy scans of the 8 GHCR images in its own schedule/workflow_dispatch-only workflow (so PRs don't carry a permanently skipped image-scan check; skips gracefully if a GHCR image doesn't exist yet). `.github/workflows/codeql.yml` adds CodeQL (`javascript-typescript` + `python`, default-setup style). `.github/workflows/dependency-review.yml` adds `dependency-review-action` on PRs. Day-one Trivy fs baseline (this worktree, commit 796dacd): 49 HIGH / 0 CRITICAL across the two npm lockfiles, all with a fixed version available, none allowlisted (left as the intended visible signal — see `.trivyignore`'s policy header). Day-one gitleaks baseline (full-history scan of the main checkout): 7 hits, all confirmed test fixtures / changelog prose, allowlisted in `.gitleaks.toml` with rationale. **Ratchet:** pip-audit now blocks every unexpected advisory; the legacy TensorFlow exceptions are explicit in `docs/SECURITY.md`. Gitleaks is now blocking (`--exit-code=1`, `continue-on-error` removed), while Trivy and dependency-review remain `continue-on-error: true` pending the owner-paced ratchet documented in the workflows; Dependabot security-updates (separate from the version-updates this ships) is not yet enabled; the trivy-image job hasn't run for real yet, so the anticipated ubuntu:20.04/torch base-image CVE baseline (#59 risk 3) isn't seeded in `.trivyignore` — add it with rationale the first time that job actually reports something.

**Files:** `.github/workflows/image-builds.yml`, `.github/workflows/quality-visibility.yml`, `.github/workflows/helm-chart-release.yml`

**Problem.** Across all three workflows there is zero CVE/SAST/secret scanning and no dependency-update bot. image-builds.yml builds and pushes 8 images to GHCR (bundling Postgres 16, Redis, ffmpeg, yt-dlp, PyTorch, TensorFlow, Essentia) with no Trivy/Grype scan, no CodeQL, no npm audit / pip-audit, and no gitleaks; quality-visibility.yml runs jest/lint/build with continue-on-error. There is no .github/dependabot.yml and no renovate.json (both confirmed absent), so 2 npm manifests + 4 Python requirements + GitHub Actions pins drift entirely by hand — which is precisely how the stack accumulated Express 4, Bull 4, helmet 7, EOL ubuntu:20.04 and floor-pinned Python with no upgrade pressure. For a self-hosted product shipping public images, known-CVE images can be released undetected.

**Recommendation.** Confirmed and slightly under-scoped by the teammate. Correct/tighten as follows. SCOPE: there are THREE workflows (helm-chart-release, image-builds, quality-visibility), all with zero scanning — don't overlook helm-chart-release.yml (publishes 8 image refs + a gh-pages chart), include it too. Real-repo manifest counts to point Dependabot at: 3 npm (backend, frontend, packages/media-metadata-contract) and 4 pip services (audio-analyzer, audio-analyzer-clap, tidal-downloader, ytmusic-streamer; the first two have only requirements.txt, the latter two also requirements-test.txt) plus 5 Dockerfiles for the docker ecosystem and github-actions for the pinned `uses:`. Ignore everything under soundspan-worktrees/ (duplicate git worktrees, not real targets). CONCRETE base-image risk to anchor the Trivy work: services/audio-analyzer/Dockerfile is `FROM ubuntu:20.04` (EOL Apr 2025) and the AIO Dockerfile bundles postgresql-16 + redis-server + ffmpeg + python audio stack — exactly the unscanned attack surface claimed. IMPLEMENTATION FIXES to the proposal: (a) don't scan "after push" against a non-existent local image — instead add `load: true` to the build step and run aquasecurity/trivy-action on the local image BEFORE the push (amd64-only build makes load cheap), OR scan the pushed registry digest with GHCR auth; (b) MUST ship a .trivyignore / `ignore-unfixed: true` / severity allowlist from the start because PyTorch/TF/ubuntu:20.04 images carry unfixable CVEs that would otherwise hard-fail every build under the `fail-fast: false` matrix incl. 2 self-hosted runners; (c) CodeQL: language list should be `javascript-typescript` and `python` (not separate js); the Helm/bash code is out of CodeQL scope. (d) dependency-review-action + gitleaks on PRs are good and additive. Also note quality-visibility.yml's jest/lint/build are gated by `continue-on-error: ${{ vars.CI_NON_BLOCKING_TEST_VISIBILITY != 'false' }}`, confirming nothing today blocks a vulnerable change. Add a CHANGELOG.md entry in the implementing PR.

**Safety / pitfalls.** The finding itself is purely diagnostic and safe. The PROPOSED CHANGE has one real implementation pitfall that must be corrected or it will silently no-op / fail: image-builds.yml uses docker/build-push-action with `push: true` and NO `load: true` and no docker-type `outputs:` (verified: the only `outputs:` is a job-level release_tag at line 14). So the built images never exist in the runner's local docker daemon. Trivy "after the push step" therefore cannot scan a local image — it must scan the registry ref (trivy image ghcr.io/...:tag) which requires the Trivy step to be authenticated to GHCR (the existing docker/login-action covers the runner, but Trivy's own pull needs creds too), and there is a push->scan ordering risk (a CVE-laden image is already public before the scan fails). Cleaner: either add `load: true` (amd64-only here, so feasible) and scan locally BEFORE push, or scan the pushed digest and treat it as detect-and-alert rather than gate. Two more operational notes: (1) 2 of 8 matrix builds run on `self-hosted` runners (aio, audio-analyzer-clap) with `fail-fast: false` — a Trivy gate that hard-fails on HIGH/CRITICAL will block releases on images like ubuntu:20.04/PyTorch/TF that carry many unfixable CVEs, so the allowlist/severity policy and `ignore-unfixed` are not optional, they are required to avoid bricking the release pipeline on day one. (2) Adding a security workflow touches no app code and AGENTS.md has no CI/security contract clause it could violate, but CHANGELOG.md exists and the contract says keep it updated, so the implementing PR should add an entry.

---

### F46 — No typecheck gate in CI; frontend stuck on strict:false / target ES2017 while backend is strict:true

**🟡 partial (PR #64)** · dimension: dependencies-build · severity: medium · effort: S · risk: low · epic: #8

> **Fix shipped, partial.** Added the missing fast, standalone, PR-visible typecheck: two new `quality-visibility.yml` jobs, `backend-typecheck` and `frontend-typecheck`, each a whole-program `tsc --noEmit` (backend's builds `packages/media-metadata-contract` first; frontend's does too via its existing `tsc -p ../packages/media-metadata-contract/tsconfig.json` prebuild step), plus a "Run frontend component tests" step in the existing frontend job. Ratcheted exactly the way this finding's own Safety section prescribed — `continue-on-error` gated on `CI_NON_BLOCKING_TEST_VISIBILITY`, non-blocking first. `backend-typecheck` lands GREEN day one: the 4 errors this PR's own `@types/node` 24 bump would otherwise surface (all one class — `.at()` vs the old `lib: ["ES2020"]`; see F53's companion-lib note) are resolved by the same PR's `lib: ["ES2022"]` alignment, so backend `tsc --noEmit` is 0 errors. `frontend-typecheck` surfaces 92 pre-existing errors non-blocking — that's the 1.10.0 error-budget work, none of it introduced here. (Still 🟡: making it a required PR check needs an admin to flip `CI_NON_BLOCKING_TEST_VISIBILITY` to `'false'` plus branch protection — not done here. The frontend ES2017→ES2020 target bump and the frontend strict:false→strict:true incremental ratchet from this finding's own Recommendation are untouched — the backend `lib` bump is a type-declaration alignment with the already-running runtime, not an ES-target or strict-flag change, per this same Audit note + #59 WS2.2.)

> **Audit note.** ✓ `@ts-ignore` count is 1, not ~480 — that figure is `eslint-disable` (a different mechanism). Scope F46 to the CI typecheck gate; cross-link #15 for the type work.

**Files:** `.github/workflows/quality-visibility.yml`, `frontend/tsconfig.json`, `backend/tsconfig.json`, `backend/package.json`

**Problem.** CI runs frontend eslint+next build+targeted coverage and backend jest, but there is no standalone tsc --noEmit anywhere (grep confirms NO typecheck gate), and backend/package.json `build` (tsc) is never run in any workflow. The api-runtime now transpiles TS at boot via tsx (Dockerfile CMD), so backend type errors may never surface in CI at all. frontend/tsconfig.json has strict:false (strictNullChecks/noImplicitAny off) and target ES2017, so the two halves of the monorepo enforce different safety bars; backend/tsconfig.json is strict:true / ES2020. The frontend ships React 19 to modern browsers but downlevels to ES2017 unnecessarily.

**Recommendation.** Real gap, but narrower than claimed. Verified: no standalone `tsc --noEmit` anywhere in .github/ (4 workflows); frontend tsconfig strict:false + target ES2017 (lines 3,7) vs backend strict:true + ES2020 (lines 5,8); default backend api-runtime image runs `npx tsx src/index.ts` (Dockerfile L111) with no compile at boot. Correct two overstatements in the finding: (1) backend type errors DO surface in CI — image-builds.yml builds backend-worker (target worker-runtime) and AIO, both run `npm run build`=tsc over all of src/** (tsconfig include: src/**/\*) — but only on push:[main,feature/**] and release, NOT on pull_request, so it is post-push and coupled to a slow Docker build, not a fast PR gate. (2) Frontend already type-checks: `npm run build`=`next build` (Next 16.1.4, no `typescript.ignoreBuildErrors` in next.config.ts) runs full tsc against the lax config, and backend jest (ts-jest preset, no isolatedModules/transpileOnly) type-checks its test paths. The true defect is the absence of a FAST, STANDALONE, PR-BLOCKING typecheck plus the strict-bar asymmetry — not total invisibility. Proposed change is right-shaped: add a fast typecheck job (backend `tsc --noEmit` whole-program; frontend `tsc --noEmit`), moving the backend whole-program check out of the Docker-build critical path so PRs (which never trigger image-builds) get coverage. Introduce it non-blocking first per the documented rollout, then make it required via branch protection once green. Bump frontend target to ES2020; pursue strictNullChecks -> full strict incrementally with a per-directory error budget, never as an immediate blocking flip. Files: /home/tony/projects/soundspan/.github/workflows/quality-visibility.yml, /home/tony/projects/soundspan/.github/workflows/image-builds.yml, /home/tony/projects/soundspan/backend/Dockerfile, /home/tony/projects/soundspan/frontend/next.config.ts, /home/tony/projects/soundspan/CONTRIBUTING.md.

**Safety / pitfalls.** The "make it a required PR check (not continue-on-error)" part conflicts with the repo's deliberate posture: CONTRIBUTING.md (lines 143-162) documents that quality-visibility is non-blocking BY DEFAULT and prescribes a gradual rollout (visibility -> thresholds -> blocking -> branch-protection required checks). The continue-on-error expression `vars.CI_NON_BLOCKING_TEST_VISIBILITY != 'false'` is true unless the var is explicitly 'false'. Given 888 `any` + ~480 ts-ignore in backend and frontend strict:false, a brand-new BLOCKING whole-program `tsc --noEmit` (especially after flipping strictNullChecks) would likely fail on day one and block every PR. Land the typecheck job non-blocking/visibility-first, fix fallout, then ratchet to required — do not flip strictNullChecks as a blocking gate before fallout is fixed. Bumping frontend target ES2017->ES2020 is low-risk (Tauri 2 + React 19 + Next 16.1.4 modern baseline). No AGENTS.md contract violation in the change itself.

---

### F47 — API runtime image ships uncompiled TypeScript and runs it via tsx in production with full dev dependencies

**⬜ open** · dimension: dependencies-build · severity: high→medium · effort: M · risk: low · epic: #19 · duplicate of F17

> **Audit note.** ✓ Duplicate of F17 (same tsx-in-prod issue); severity high→medium. The worker target already compiles to `dist`.

**Files:** `backend/Dockerfile`, `backend/package.json`

**Problem.** The api-runtime stage copies `src ./src` (not dist) and `CMD ["npx", "tsx", "src/index.ts"]` (Dockerfile:89,111), pulling node_modules from the unpruned `deps` stage (Dockerfile:85) so typescript+tsx+all dev deps ship to production. The build stage already runs `npm run build` (tsc -> dist) and the worker-runtime stage correctly ships compiled dist/worker.js with `npm prune --omit=dev` (Dockerfile:40-57) — so the compiled output and the slim pattern both already exist; only the API path doesn't use them. This pays a larger image, slower cold start, higher memory, and ships a TS toolchain (a larger attack surface) into the production API.

**Recommendation.** Confirmed: api-runtime ships uncompiled TS and runs it via tsx with full unpruned dev deps. Dockerfile:85 copies node_modules from the unpruned `deps` stage, Dockerfile:89 copies `src` (not dist), and Dockerfile:111 runs CMD ["npx","tsx","src/index.ts"] — shipping tsx, typescript, jest, ts-jest, supertest, prisma into the production API. The compiled artifact already exists (build stage `npm run build` -> tsc -> dist, Dockerfile:32-37; package.json `start`=node dist/index.js) and the slim pattern is already proven twice: worker-runtime (Dockerfile:40-70) and the AIO image, which does EXACTLY the proposed fix — `npm install -g prisma@<pinned-version>` then `npm prune --omit=dev` and runs `node dist/index.js` (root Dockerfile:181-183, 270). So F47's core claim and its "as the AIO image already does" prisma approach are accurate. Tightened plan: (1) add a worker-deps-equivalent pruned stage for the API; (2) base api-runtime on node:20-slim, install ffmpeg/tini/openssl, COPY --from=<pruned> node_modules + package*.json + prisma, COPY --from=build /app/dist ./dist, COPY the contract package, plus healthcheck.js and docker-entrypoint.sh; (3) install pinned prisma globally so the entrypoint's `npx prisma migrate deploy` keeps working after prune; (4) CMD ["node","dist/index.js"]. CRITICAL additions the finding omits: in the build stage copy src/config/vibe-vocabulary.json into dist/config (tsc won't), and fix the swagger `apis` glob in src/config/swagger.ts:105 to target ./dist/routes/*.js (or make it env-aware / precompute swaggerSpec) — otherwise API docs and /vibe vocabulary silently degrade. Bonus: doing this also closes the same vibe-vocab gap that already exists in worker-runtime.

**Safety / pitfalls.** A naive dist-only copy (mirroring worker-runtime exactly) would cause two SILENT runtime regressions that the finding does not call out. (1) swagger-jsdoc: backend/src/config/swagger.ts:105 sets apis: ["./src/routes/*.ts", "./src/config/swaggerSchemas.ts"] — a CWD-relative glob against /app/src. With src removed, swagger-jsdoc matches zero files and /api/docs + /api/docs.json (mounted at backend/src/index.ts:387-398) serve a near-empty spec (only the hand-written components/security, no documented paths). No crash, but degraded API docs. The AIO image avoids this only because it keeps src/ present (root Dockerfile:174). (2) vibe-vocabulary.json: backend/src/services/vibeVocabulary.ts:36-38 readFileSync's it from ../config or ../../src/config; tsc does NOT copy .json to dist, so under dist-only both paths miss, loadVocabulary() returns null (called at module load in backend/src/routes/vibe.ts:27), and /vibe search silently loses vocabulary expansion/reranking. The umapWorker spawn (umapProjection.ts:70-76) is safe — it already prefers .js and falls back. Migrations are safe if prisma is installed globally (entrypoint uses `npx prisma migrate deploy`, docker-entrypoint.sh:46), which the AIO image already does. So the change is safe ONLY if the two runtime assets are handled: copy src/config/vibe-vocabulary.json into dist/config during build, and re-point the swagger apis glob at ./dist/routes/\*.js (or precompute the spec). The AGENTS.md contract is not violated by this change.

---

### F48 — Bull 4 is maintenance-only with a version-mismatched @types/bull; migrate to BullMQ

**⬜ open** · dimension: dependencies-build · severity: medium · effort: L · risk: medium · ⚠️ breaking · epic: #19

**Files:** `backend/package.json`, `backend/src/workers/queues.ts`, `backend/src/workers/index.ts`, `backend/src/index.ts`

**Problem.** backend pins bull@^4.16.5 plus @types/bull@^3.15.9 — a community type package pinned to a different major than the runtime (3 vs 4), so the queue layer's types are already approximate. Bull 4 is in maintenance mode; BullMQ is the TypeScript-native successor (no @types needed) with Worker/Queue separation, per-job idempotency via deterministic jobId (which directly enables the scan-dedup and discover-per-week idempotency wins called out by other reviewers), native attempts/backoff, rate limiting, and worker concurrency. The Bull surface is contained — only 6 files import it (workers/queues.ts, workers/index.ts, and 4 processors) and @bull-board already ships a BullMQAdapter, so index.ts:558's BullAdapter swaps cleanly.

**Recommendation.** The core claim is real: package.json pins bull@^4.16.5 (line 50) against @types/bull@^3.15.9 (line 39) — a confirmed major mismatch (runtime 4 vs types 3) — and Bull 4 is maintenance-only. ioredis@5.9.2 and @bull-board/api@6 (which ships node_modules/@bull-board/api/bullMQAdapter.js) are already installed, so the dependency substrate is ready, and the BACKEND_PROCESS_ROLE split with a real src/worker.ts entrypoint makes staging viable. Proceed, but tighten scope: (a) DELETE the analysisQueue ('audio-analysis', queues.ts:87) during migration — it is a dead Bull queue, never .process()'d or .add()'d anywhere; audio analysis runs via unifiedEnrichment.ts + p-limit, not this queue. The finding wrongly implies it must be ported. (b) Treat the repeatable/scheduler jobs as the highest-risk surface: BullMQ replaced repeat:{every} with the upsertJobScheduler/Job Scheduler API, so registerSchedulerJobs and discoverCron need real rework, not a mechanical swap. (c) Refactor schedulerQueue.process('\*') and the legacy unnamed discover handler into name-switching Workers. (d) The actual idempotency wins (deterministic jobId for scan/discover) live in ~10 enqueue call sites across routes/services (library.ts:1274, downloadQueue.ts:516, discoverWeekly.ts:854, webhooks.ts, youtube.ts, discover.ts:217's legacy add({userId})), not the 6 Bull-importing files; today scan uses no jobId and discover only dedups on one of two paths (discover.ts:181 yes, :217 no) — so the gain is genuine but the change is broader than the cited file list. (e) Add a Worker-specific createIORedisClient override (maxRetriesPerRequest:null), plan Redis key-migration/drain of leftover Bull repeat jobs at cutover, and update CHANGELOG.md. 'Swaps cleanly in one PR' understates the scheduler/repeat-job and Redis-state work; size it as a focused but non-trivial PR gated by the existing worker contract test suite.

**Safety / pitfalls.** Bull-to-BullMQ is NOT a behavior-preserving swap. (1) Bull and BullMQ use different Redis key schemas, so in-flight delayed jobs and—critically—persisted repeatable job definitions (the discover cron and the many repeat:{every} scheduler jobs in registerSchedulerJobs, workers/index.ts:651-834) do not carry over across deploy; they orphan in Redis and must be re-registered, and new idempotent jobIds won't dedupe against leftover Bull keys. (2) The proposal calls the wildcard processor and legacy unnamed handlers a clean swap, but BullMQ has no .process('_') or per-name registration — schedulerQueue.process('_') (workers/index.ts:908) and the legacy discoverQueue.process(processDiscoverWeekly) (line 845) must be refactored into a single name-switching Worker. (3) BullMQ Workers require maxRetriesPerRequest:null, but createIORedisClient (utils/ioredis.ts:43) hardcodes 3 — a Worker connection override is mandatory or Workers throw at startup (the proposal does flag this correctly). (4) CHANGELOG.md (repo root) must be updated per contract. Within Prisma/no-raw-SQL/logger boundaries this is otherwise safe.

---

### F49 — Express 4 (Express 5 is GA) with half-migrated middleware majors

**✅ complete** (`deps/express-5`) · dimension: dependencies-build · severity: medium · effort: L · risk: medium · ⚠️ breaking · epic: #19

> **Fix shipped.** Express 4.22 → 5.2.1 (+`@types/express` ^5), migrated per this finding's own recommendation as a compatibility-only change — no try/catch removal, no error-path refactor. The audit's undercount was itself an undercount: THREE path-to-regexp v8 breakages existed, not two — `library.ts` `"/cover-art/:id?"` → `"/cover-art{/:id}"`, `subsonic.ts` `router.all("*")` → `"/{*splat}"`, and a third nobody had flagged: `streaming.ts`'s inline regex-constrained param `":segmentName([A-Za-z0-9_.-]+\\.(?:m4s|webm))"` (removed syntax in v8), now an unconstrained `:segmentName` plus a pre-auth guard middleware that `next("route")`s on a pattern mismatch, preserving Express 4's 404-without-auth-check semantics. Two Express-4 behavior defaults are explicitly restored in `index.ts`: `app.set("query parser", "extended")` (v5 flipped the default to `simple`) and a post-`express.json` shim that resets `req.body` to `{}` when no parser matched (v5 leaves it `undefined`; ~100 handlers destructure it unconditionally, and without the shim malformed requests would 500 instead of 400). The `@types/express` v5 `ParamsDictionary` widening to `string | string[]` (splat params) surfaced ~70 type errors at routes whose middleware breaks literal-path param inference; fixed with per-route param typings (`Request<{ id: string }>` / `router.get<{ id: string }>`) across 19 route files — no runtime change, named params are always strings. Middleware survey: `@bull-board/express` resolves to 6.16.4 (depends on express ^5.2.0), `express-session` 1.19.0, `express-rate-limit` 8.5.2 (peer `>= 4.11`), `swagger-ui-express` 5.0.1 (peer allows 5.x); cors/compression/multer/connect-redis are express-agnostic — nothing hard-blocked. helmet stays on 7 per this finding's own recommendation (v8 + CSP/HSTS is its own PR). Verified: `tsc --noEmit` 0 errors, full backend Jest suite green, plus a runtime smoke that registered all 339 route path literals on an Express 5 router (syntax proof) and exercised the three migrated patterns end-to-end against the real subsonic/library/streaming routers.

> **Audit note.** ✓ Undercount: a SECOND guaranteed Express-5 breakage exists at `subsonic.ts:6667` (`router.all("*")`), not just `library.ts`. The migration surface is larger than 'small'.

**Files:** `backend/package.json`

**Problem.** backend pins express@^4.18.2 while related middleware is already on current majors — express-rate-limit@^8.2.1, multer@^2.0.2, bcrypt@^6 — so the app is half-migrated. Express 4 is on an EOL trajectory; Express 5 (GA) brings native async-error propagation (which would let the codebase delete the 130+ hand-rolled per-route try/catch blocks other reviewers documented), an updated path-to-regexp (a recurring ReDoS CVE source), and stricter routing. helmet is still on 7 (v8 current) — another security-relevant major behind.

**Recommendation.** The dependency facts are accurate and worth acting on: express ^4.18.2 (installed 4.22.1) and helmet ^7.1.0 (7.2.0) trail current majors while express-rate-limit ^8.2.1, multer ^2.0.2, and bcrypt ^6.0.0 are already current — so the "half-migrated" framing holds. Keep the proposal as an isolated, narrowly-scoped Express 4->5 PR, but DECOUPLE it from the try/catch removal narrative, which does not apply to this codebase: there is no next(error) forwarding today, so v5 async-error propagation yields no automatic cleanup and instead would regress local error responses. Concretely: (1) Treat the v5 bump as a compatibility migration only — audit is small: no app.del or req.param() usage exists; the only path-to-regexp breakage found is the optional param at library.ts:4092 (router.get("/cover-art/:id?")), which must move to the v8 syntax (e.g. a separate route or {0,} form). (2) Do NOT sequence it "before the route god-file refactor to land try/catch removal once" — that benefit is illusory here; if anything, converting 598 local catch blocks to throw typed AppErrors is a separate, much larger initiative that should be evaluated on its own merits and tested per-route, not bundled with a dependency bump. (3) The helmet 7->8 bump and adding CSP/HSTS is independently valid and higher-value security work: the current helmet() config (src/index.ts:127) sets only crossOriginResourcePolicy with no CSP and no HSTS. Do helmet as its own PR. Gate all of this on the existing middleware/route test suites (supertest is present) as the regression net, paying specific attention to status-code assertions.

**Safety / pitfalls.** The proposal's headline benefit ("delete the 130+ hand-rolled try/catch blocks" by leaning on Express 5 native rejection forwarding) is unsafe as described and would change behavior. I found ZERO next(error)/next(err) calls anywhere in backend/src (non-test). The ~598 route catch blocks do NOT forward to the tail errorHandler — they handle errors locally with res.status(...).json(...), frequently returning context-specific status codes and messages (e.g. auth.ts:332 res.status(401) "Invalid refresh token"; auth.ts:321 401 "Token invalidated"; library.ts catch blocks 500). The generic errorHandler (src/middleware/errorHandler.ts, registered at src/index.ts:402) only maps AppError categories to 400/503/500 and otherwise returns a bare 500. Removing those try/catch blocks and relying on v5 auto-forwarding would silently collapse those 401/403/404/custom responses into generic 500s and drop their messages — a real API regression — unless every block is first refactored to throw typed AppErrors. That refactor is itself larger than the Express bump and is the opposite of the smallest-safe-change / no-scope-expansion rule in AGENTS.md. Also note: path-to-regexp v8 (in Express 5) breaks the one optional-param route found, router.get("/cover-art/:id?") at library.ts:4092, which must be rewritten.

---

### F50 — Python sidecar dependencies are floor-pinned with no lockfile or hashes — non-reproducible ML images

**✅ complete (PR #121)** · dimension: dependencies-build · severity: medium · effort: M · risk: low · epic: #10

> **Audit note.** Per audit: 10 Essentia `.pb` files; CLAP checkpoint ~600MB per the repo comment (not ~2GB). CORRECTED 2026-07-11 — the checkpoint-size clause was backwards: measured 2,352,471,003 bytes ≈ 2.35 GB (HF `x-linked-size`); the repo's ~600MB comment was the wrong figure. See Fix shipped.

> **Fix shipped.** Every Python image (4 sidecars + AIO) now installs from a hash-pinned lock generated with `uv pip compile --generate-hashes` targeted at each image's interpreter, installed via plain `pip install --require-hashes` — a rebuild of one commit resolves the identical transitive tree. Per-image locks: `ytmusic-streamer/requirements.lock` (py3.14), `tidal-downloader/requirements.lock` (py3.14), `audio-analyzer-clap/requirements.lock` (py3.12), `audio-analyzer/requirements.lock` (py3.8) — the last now that its requirements.txt is the real manifest (TF 2.13.x + essentia-tensorflow + redis/psycopg2/numpy<2.0), converting both former inline installs. The AIO uses ONE merged `requirements-aio.lock` (analyzer+clap resolved jointly at py3.11 — the joint resolution IS satisfiable, so no sequential-lock fallback was needed) replacing all three of its pip installs, plus a new build-time import smoke (`import numpy, tensorflow`; `find_spec('essentia')`). `.github/workflows/security-scanning.yml` pip-audit repoints its 4 runtime lanes at the locks and adds an AIO-lock lane. **Exemption (F38/F50 carve-out):** `yt-dlp` and `ytmusicapi` stay loose floors (installed after the lock from `ytmusic-streamer/requirements-exempt.txt`) so a YouTube breakage stays a same-day floor bump; their `requests` transitive is pre-pinned in the lock so only those two float. **Corrections to this finding, verified on the built images (2026-07-11):** (i) the Recommendation's "curls 11 Essentia .pb files" is wrong — it is **10** (the Audit note is right); (ii) the ~600MB figure (Audit note + AIO `Dockerfile:105` comment) is wrong — the measured checkpoint is **2,352,471,003 bytes ≈ 2.35 GB** (Hugging Face `x-linked-size`), so the Problem's "~2GB" was the closer number; (iii) pitfall (2)'s premise that "torch is typically resolved from the PyTorch index" does **not** hold here — the CLAP image installs torch from plain PyPI today, and that is exactly what got pinned+hashed (no index change). Tests: ytmusic 125 / tidal 63 / analyzer env-helper 5 pytest green under py3.14 docker; tidal built twice `--no-cache` → identical `pip freeze`; a flipped hash fails the install layer. AIO freeze==lock verified against the built image (exact match modulo pip's setuptools/wheel freeze exclusions and the apt-managed supervisor). Model-checksum sha256 pinning is the separate F38 packet's scope (same issue #119).

**Files:** `services/ytmusic-streamer/requirements.txt`, `services/audio-analyzer-clap/requirements.txt`, `services/tidal-downloader/requirements.txt`, `services/audio-analyzer/requirements.txt`

**Problem.** Every requirements.txt uses unbounded `>=` floors (torch>=2.0.0, transformers>=4.30.0, laion-clap>=1.1.4, fastapi>=0.115.0, yt-dlp>=2025.1.0; only tidalapi has a range). There is no pyproject.toml, no uv.lock/poetry.lock/requirements.lock, and no hash pinning anywhere under services/ (confirmed absent). Two builds of the same commit a week apart can resolve different transitive trees — especially torch/transformers whose ABIs break frequently — so the multi-GB ML images are unreproducible and unverifiable. Separately, the ML model weights are curl'd at build time with NO checksum: the CLAP ~2GB .pt from huggingface (audio-analyzer-clap/Dockerfile:39) and ~10 Essentia .pb models from essentia.upf.edu (audio-analyzer/Dockerfile:53-76) are baked in with no sha256 verification, so a re-published or MITM'd artifact runs as model weights undetected.

**Recommendation.** Confirmed and adopt, with corrections. Factual corrections to the finding: it's not "only tidalapi" with a range — there are two upper-bounded constraints (tidalapi>=0.8.0,<1.0.0 and numpy>=1.19.0,<1.24.0 in audio-analyzer — the latter changed by this fix: the manifest now pins numpy<2.0.0, matching what the image actually installed); every other dep across all four requirements.txt plus both requirements-test.txt is an open >= floor. No lockfile/pyproject/hash exists anywhere under services/ (find + grep both empty), and CI (image-builds.yml) just docker-buildx-builds these Dockerfiles with no pinning/audit; no dependabot, no pip-audit. The two model-download claims are exact: audio-analyzer-clap/Dockerfile:34-42 curls the ~2GB CLAP .pt from huggingface with no checksum (only a load-test on line 45), and audio-analyzer/Dockerfile:53-76 curls 11 Essentia .pb files (correction 2026-07-11: 10, not 11 — see Fix shipped) from essentia.upf.edu with verification being just `ls -lh` on line 79. Recommendation: adopt uv per service (pyproject.toml + committed uv.lock, installed via uv pip sync --require-hashes) — BUT (a) the lock must also capture the inline tensorflow>=2.13.0,<2.16.0 + essentia-tensorflow currently buried in the analyzer Dockerfile, (b) must declare the PyTorch extra index URL so torch wheels resolve deterministically (a reproducibility hole today even before transitive drift) (correction 2026-07-11: this repo installs torch from plain PyPI; pinned+hashed as-is, no index change — see Fix shipped), and (c) keep yt-dlp/ytmusicapi on a deliberately loose, documented-refresh constraint. For models, sha256-verify-then-fail-build is correct and additive; prefer mirroring essentia.upf.edu/huggingface artifacts into the project registry/object store since both are single upstreams. This also gives one home for pip-audit in a new security workflow. Severity medium is right: supply-chain/reproducibility integrity, not an active runtime bug.

**Safety / pitfalls.** The migration itself is low-risk but has real pitfalls that must be respected or it will break the sidecars: (1) yt-dlp/ytmusicapi MUST stay on a loose constraint — the user's own memory notes record a prior breakage when YouTube changed clients, and hash-pinning them would force lockfile churn on every YouTube break. (2) torch/torchvision/torchaudio in the CLAP image are typically resolved from the PyTorch index, not plain PyPI; a uv lock must declare the extra index URL or it will pull wrong (CUDA vs CPU) wheels — the current setup is already non-deterministic on this axis (correction 2026-07-11: this repo installs torch from plain PyPI; pinned+hashed as-is — see Fix shipped). (3) tensorflow/essentia-tensorflow are pinned inline in audio-analyzer/Dockerfile lines 38-40, NOT in any requirements.txt, so a uv migration that only converts requirements.txt would leave them as unpinned floors. (4) The CLAP model 'verify' (load_ckpt) and Essentia 'verify' (ls -lh) catch corruption but NOT a maliciously re-published valid artifact, so sha256 verification is genuinely additive. No AGENTS.md contract conflict: AGENTS.md imposes no Python packaging contract (only the shared logger and doc-coverage rules).

---

### F51 — audio-analyzer pinned to EOL Ubuntu 20.04 / Python 3.8 / old TensorFlow

**✅ done** · dimension: dependencies-build · severity: medium · effort: L · risk: medium · epic: #19

> **Fix shipped.** `services/audio-analyzer/Dockerfile` now builds `FROM python:3.11-slim` (Debian bookworm) instead of `ubuntu:20.04`/Python 3.8, dropping the redundant `python3*` apt packages and adding `libgomp1` for the TensorFlow CPU wheel's OpenMP runtime. `requirements.lock` was recompiled with `uv pip compile --generate-hashes --python-version 3.11`, resolving the SAME ABI-critical stack as the AIO image (`requirements-aio.lock`): TensorFlow 2.15.1, essentia-tensorflow 2.1b6.dev1389, Keras 2.15.0, tensorflow-io-gcs-filesystem 0.37.1, numpy 1.26.4, psycopg2-binary 2.9.12, redis 8.1.0 — so the standalone and AIO analyzers share one embedding/model behavior. `requirements.txt` pins `essentia-tensorflow==2.1b6.dev1389` (the package publishes only pre-release dev builds, which made uv's unpinned selection cache-sensitive and non-reproducible) and raises the numpy floor to `>=1.26.0,<2.0.0`. The Python 3.8 BrokenProcessPool compatibility shim in `analyzer.py` was removed in favor of a direct `from concurrent.futures.process import BrokenProcessPool` (the class is not re-exported at the `concurrent.futures` top level even on 3.11). Separately, the analyzer's internal duration/interval timers (idle-timeout, resize-debounce, DB-reconcile schedule, batch-rate) were switched from wall-clock `time.time()` to `time.monotonic()` so NTP/clock steps no longer corrupt them; the cross-process `audio:worker:heartbeat` epoch-ms timestamp deliberately stays wall-clock. The blocking `security-scanning.yml` pip-audit lane now audits this lock on Python 3.11 with the TF-2.15 exception set mirroring the AIO lane (`docs/SECURITY.md`), verified green against the regenerated lock. Verification: docker build not run (disk), Dockerfile validated by manual review; the 9-test `services/audio-analyzer/tests` pytest suite passes on a Python 3.11 venv. Full analysis-output regression against a known track is deferred to the first real image build (see below).

**Files:** `services/audio-analyzer/Dockerfile`, `services/audio-analyzer/requirements.txt`

**Problem.** The Essentia analyzer base image is `FROM ubuntu:20.04` (standard support ended Apr 2025) running Python 3.8 (EOL Oct 2024), constrained to tensorflow>=2.13,<2.16 and numpy<1.24 to satisfy essentia-tensorflow, and the code carries a Python 3.8 BrokenProcessPool compatibility shim. The other three sidecars already run python:3.14-slim (ytmusic, tidal) and python:3.12-slim (CLAP), so this one image blocks OS/interpreter security patches and pins the heaviest ML image to an unsupported stack.

**Recommendation.** Confirmed and worth doing — the image is genuinely on EOL Ubuntu 20.04 / Python 3.8 (both past EOL as of 2026-06), pins TF 2.13-2.15 and numpy<2.0, carries a live Python 3.8 BrokenProcessPool shim (analyzer.py L76-87, used at L1307/L1669/L1813), and is the only sidecar not on a maintained base (others: py3.14/py3.14/py3.12). Correct the citation: the operative numpy pin is `numpy<2.0.0` in Dockerfile L82; requirements.txt's `numpy<1.24.0` is dead (never installed). Sequence the fix as: (1) FIRST verify essentia-tensorflow availability — it is the gating constraint; check current PyPI wheels for python:3.11/3.12-slim or ubuntu:22.04/24.04 before assuming any base bump works. Prefer slim+ffmpeg apt over full Ubuntu to also shrink the heaviest image. (2) If essentia-tensorflow has a wheel for a newer interpreter, bump base + TF + numpy together, pin all transitive deps in a lockfile (the build currently pins nothing transitively), and delete the BrokenProcessPool shim only after the interpreter is >=3.9. (3) If it genuinely cannot move, isolate Essentia extraction behind a subprocess/RPC on a minimal legacy runtime rather than rewriting onto CLAP — do NOT treat 'migrate to CLAP/torch' as a drop-in, because it changes analysis outputs (different embedding space, loss of the 9 MusiCNN classification heads) and would require re-analyzing the catalog and migrating stored vectors. Keep the existing spawn start-method and entropy-init handling intact through any refactor.

**Safety / pitfalls.** The migration itself is the risk, not staying put. (1) essentia-tensorflow's pinned constraint chain is the real blocker: it links against a TF 2.13-2.15 ABI and is the reason for both ubuntu:20.04/py3.8 and numpy<2.0 (line 82 actually pins numpy<2.0, NOT <1.24 — requirements.txt's <1.24 is never used by the Dockerfile since there is no `pip install -r`). Naively bumping to python:3.11/3.12 will likely fail to resolve essentia-tensorflow wheels and break the build. (2) The 'migrate mood/embedding to the CLAP/torch path' suggestion is NOT a like-for-like swap and would be a behavior change: the Essentia path produces MusiCNN embeddings + 9 specific classification heads (mood_happy/sad/relaxed/aggressive/party/acoustic/electronic, danceability, voice_instrumental) plus Danceability extractor, whose outputs almost certainly feed downstream consumers (vibe/discovery/playlist features). CLAP produces a different 512-dim audioset embedding space — dropping the MusiCNN heads changes analysis output and stored vectors, not just the runtime. (3) The build uses spawn-mode ProcessPoolExecutor and an entropy-init workaround (`random_device could not be read`) noted in the Dockerfile/code comments — a subprocess/RPC isolation refactor must preserve spawn semantics and the per-worker model loading. (4) This is a live, CI-built, Helm/compose-deployed image, so a broken base-image change ships to prod (music.tonymead.org). No AGENTS.md contract violation in the proposal (it's infra, not raw-SQL/api-boundary), but the lockfile/reproducibility pairing it mentions is essential since the current build has zero pinned transitive deps.

---

### F52 — AIO image is a single-stage, root-supervisord monolith with build-toolchain bloat and runtime CVE-removal anti-pattern

**⬜ open** · dimension: dependencies-build · severity: medium · effort: L · risk: medium · epic: #19

**Files:** `Dockerfile`, `docker-compose.aio.yml`

**Problem.** The root Dockerfile is single-stage (one FROM node:20-slim, confirmed) baking Postgres 16, Redis, TensorFlow+Essentia (~200MB models), CLAP+torch (~2GB+ checkpoint), Node and Next.js into one image run by supervisord as root (gosu for initdb/pg). Because it is single-stage, build-essential+python3-dev are apt-get purge'd in a later layer (Dockerfile:223) so the bloat persists in earlier layers, and 'hardening' rm's curl/wget at runtime (Dockerfile:224-225) rather than never installing them. The result is a multi-GB image that violates one-process-per-container and cannot honor the chart's runAsNonRoot. The AIO compose also defaults to the floating `:latest` tag (docker-compose.aio.yml:16).

**Recommendation.** The technical core is real and accurately cited: Dockerfile is single-stage (one FROM node:20-slim at line 5), bakes PG16+redis+supervisor (16-26), TensorFlow/Essentia+~200MB models (49-83), CLAP/torch+~600MB checkpoint (98-118), and Node+Next.js into one root-supervisord (user=root, line 245) multi-GB image; build-essential/python3-dev are purged at 223 after the model-download layers (49-118) so they persist in earlier frozen layers; curl/wget/nc are rm'd at runtime (224-227) rather than never-installed; docker-compose.aio.yml:16 defaults to :latest (and .env.example:128 explicitly recommends VERSION=latest). Keep the recommendations to (a) convert AIO to multi-stage so build-essential/python3-dev/build-only curl never reach the final layer (consistent with backend/Dockerfile's 5-stage and frontend/Dockerfile's 3-stage patterns), (b) split the ~3GB CLAP/torch checkpoint into an optional image variant, and (c) pin postgres/redis to patch tags. BUT correct three overclaims: (1) Drop the 'cannot honor the chart's runAsNonRoot' framing as a violation — charts/soundspan/templates/aio/deployment.yaml:32-34 deliberately omits global.podSecurityContext (only fsGroup:1000, with an explanatory comment) and enforces runAsNonRoot only on individual-mode services, so the chart already accommodates this by design. (2) The proposed 'rely on the security workflow's Trivy scan' is unsubstantiated — NO trivy/grype/snyk/scout/scan workflow exists anywhere in .github/; reword to 'ADD a Trivy/Grype image scan to image-builds.yml as the compensating control before removing the runtime rm.' (3) The 'document the AIO-vs-split-for-production split' work is largely already done — docs/KUBERNETES.md:103 already notes AIO runs as root via supervisord while individual images are non-root, and docs/DEPLOYMENT.md:55-57,95-99,322 already frame AIO as the convenience/single-host artifact and steer scaling to the split docker-compose.yml; scope any doc change to a small clarifying note rather than new documentation. Net: real medium-value dependencies/build cleanup, but treat AIO multi-stage as the substantive item and re-baseline the security/runAsNonRoot/doc claims against what already exists.

**Safety / pitfalls.** A multi-stage rewrite is harder than the finding implies and can break the build: the AIO final image must still contain PostgreSQL 16 + redis + supervisor + both Python ML runtimes (TensorFlow/Essentia and torch/CLAP) and their ~3GB of model checkpoints, so this is not a clean builder-vs-slim-runtime split like backend/Dockerfile (which only copies a node dist/). curl is genuinely required DURING build (Dockerfile:9-11 for the PG apt key, :60-116 for model/CLAP downloads); python3-dev/build-essential are needed to compile some pip wheels. Removing the runtime rm of curl/wget WITHOUT adding a compensating image scan reduces defense-in-depth, and the finding wrongly assumes such a scan already exists. No AGENTS.md contract surface (Prisma/raw-SQL/api-boundary/loggers) is touched, so contract risk is none; the risk is purely build-correctness and a net-negative security posture if mitigations are dropped before replacements land.

---

### F53 — Node runtime version unpinned and drifts across CI (frontend on Node 24, everything else Node 20)

**✅ complete (PR #64)** · dimension: dependencies-build · severity: medium · effort: S · risk: low · epic: #8

> **Fix shipped.** Every Node-based Dockerfile (backend x3 stages, frontend x3 stages, root AIO x1) now builds `FROM node:24-bookworm-slim` (was node:20-slim; bookworm pinned explicitly — see commit message — because the AIO image installs PG16 via `$(lsb_release -cs)-pgdg` and TF/Essentia/CLAP via pip against the image's system Python 3.11, so pinning the Debian family keeps both stable across future `node:24` tag updates). Every CI `node-version` pin (quality-visibility's backend job, both image-builds.yml jobs, helm-chart-release.yml) now reads `24`, closing the exact divergence this finding named (frontend CI-validated on 24, shipped on 20). `@types/node` bumped to `^24` in backend + frontend. The root, backend, frontend, and shared-contract `engines.node` declarations now require Node `>=24.0.0`.
>
> **Companion lib alignment.** `@types/node` 24 drops the legacy compat declarations for post-ES2020 built-ins that v20 carried, so under the old `lib: ["ES2020"]` the types bump alone would surface 4 new `TS2550` errors at backend's `Array/String.prototype.at()` call sites (2 test files + 1 service file; verified clean-room that base `796dacd` with its own lockfile typechecks at 0 errors). The same PR raises `backend/tsconfig.json` `lib` to `ES2022` — a type-declaration alignment with the actual runtime (Node ≥ 20 implements `.at()`; the api-runtime image already executes this code via `tsx`), preventing that would-be regression. Emitted code and `target` unchanged.
>
> **Verified (proof of ✅):** all three `bake core` images build clean on Node 24 — `backend` (api-runtime), `backend-worker`, `frontend` — and `node --version` inside each → `v24.18.0`; the full AIO image (all 45 steps: apt + `pgdg` PG16 repo, both Python analyzer dependency installs (TF/Essentia and torch/transformers/laion-clap), Essentia model downloads, the ~2.35 GB CLAP checkpoint (2,352,471,003 bytes; unauthenticated — that HF repo is ungated; "~600MB" corrected 2026-07-11, see F38), backend `npm run build`, frontend `next build`) built to completion locally; backend `tsc --noEmit` under node:24 → 0 errors.

**Files:** `.github/workflows/quality-visibility.yml`, `backend/package.json`, `frontend/package.json`

**Problem.** No `engines.node` in any package.json and no .nvmrc/.node-version at root (both confirmed absent). quality-visibility.yml runs backend tests on node-version '20' but the frontend lint/build/test job on node-version '24', while every Dockerfile (backend, frontend, AIO) builds on node:20-slim. So frontend CI validates on Node 24 but the shipped frontend image runs Node 20 — CI can pass on behavior that diverges from production and vice versa.

**Recommendation.** Confirmed in canonical tree (ignore soundspan-worktrees/, which are local worktrees of other branches). Facts: no engines.node in any package.json (backend/frontend/packages); no .nvmrc/.node-version/.tool-versions anywhere; quality-visibility.yml pins backend Node "20" (line 31) but frontend lint/build/test "24" (line 87); every production Dockerfile uses node:20-slim (root AIO Dockerfile:5 builds frontend via `next build` and runs it under supervisor on Node 20; frontend/Dockerfile 2/20/48; backend/Dockerfile 10/44/73); the other two Node jobs (image-builds.yml 23/183, helm-chart-release.yml 34) both pin 20, making frontend's 24 the lone outlier; no .github/dependabot config exists. So the frontend is CI-validated on Node 24 but built+shipped on Node 20 — divergence is real. Tightened recommendation: (1) Add a root .nvmrc/.node-version = 20 and add `"engines": {"node": ">=20 <21"}` to backend, frontend, and packages/media-metadata-contract package.json. (2) Change quality-visibility.yml frontend job to node-version-file (or node-version "20") so CI matches the shipped image; do the same for the other setup-node steps for single-source-of-truth. (3) Add .github/dependabot.yml covering github-actions, docker, and the npm directories so the pinned version stays coherent. (4) Treat any move off Node 20 (e.g., to 22 LTS) as a separate PR that bumps Dockerfiles + CI + @types/node together and re-validates the AIO image's PG16/TensorFlow/CLAP pip stack. Note the frontend quality job currently runs continue-on-error by default (vars.CI_NON_BLOCKING_TEST_VISIBILITY != 'false', line 77), so the divergence is non-blocking today unless that org var is set to 'false' — real but lower-severity until then.

**Safety / pitfalls.** The core fix (add .nvmrc/.node-version + engines.node, align the frontend CI job to Node 20 via node-version-file) is safe and touches only build/CI config — no AGENTS.md contract surface (api.ts boundary, config.ts, Prisma, loggers) is affected. However the proposal's bundled alternative of "bump images+CI to 22 LTS in one PR" is NOT safe to lump in: the root AIO Dockerfile (node:20-slim, Debian bookworm) installs PG16/pgvector, redis, and pins TensorFlow >=2.13,<2.16 + essentia-tensorflow + CLAP/torch via pip --break-system-packages against the image's bundled Python. Changing the node base-image major can shift the Debian release, glibc, and system Python, breaking those pins and the apt PG16 repo setup. Also @types/node is pinned ^20 in both backend and frontend devDeps, so validating frontend on Node 24 is already type-incoherent. Recommend pinning to 20 now; treat any major bump as a separate, tested PR.

---

### F54 — POST /device-link/verify is an unauthenticated TOCTOU that mints full-privilege API keys

**✅ complete** (`feat/f54-device-link-toctou`) · dimension: security · severity: high · effort: S · risk: low · epic: #12

> **Fix shipped.** The claim is now an atomic conditional `updateMany({ where: { id, usedAt: null } })` inside a `prisma.$transaction`; the API key is minted only if `count === 1`, otherwise the transaction rolls back and the handler returns `400 'Code already used'` with no key created. The route moved from `apiLimiter` to `authLimiter`. Tests: `deviceLinkRuntime.test.ts` (concurrent-claim case mints no key) + `apiEntrypointRuntime.test.ts` (authLimiter mount).

> **Provenance.** Found by the independent audit (§6.1) and re-verified against deviceLink.ts. NOT in the original 53.

**Files:** `backend/src/routes/deviceLink.ts:118`, `backend/src/routes/deviceLink.ts:136`, `backend/src/routes/deviceLink.ts:146`, `backend/src/routes/deviceLink.ts:155`, `backend/src/index.ts (apiLimiter mount)`

**Problem.** The unauthenticated POST /device-link/verify reads linkCode.usedAt (deviceLink.ts:136), then prisma.apiKey.create(...) (146), then SEPARATELY prisma.deviceLinkCode.update({usedAt}) (155) — with no transaction and no conditional updateMany. Two concurrent verifies of the same code both pass the `if (linkCode.usedAt)` guard and both mint a full-privilege ApiKey (auth.ts resolves these to the user). This is the EXACT TOCTOU class fixed for invite codes in F26 — the in-repo pattern (updateMany ... where usedAt:null) exists and is simply not applied here. Compounding: the route is mounted under the general apiLimiter, not authLimiter, and the code is 6 chars over a 32-char alphabet (~1.1B) with a 5-min TTL — a human-typable, unauthenticated, full-privilege credential mint on the wrong rate-limit tier.

**Recommendation.** Mirror the F26 fix: claim the code atomically before minting — `const claimed = await prisma.deviceLinkCode.updateMany({ where: { id: linkCode.id, usedAt: null }, data: { usedAt: new Date(), deviceName, /* apiKeyId set after create or in a tx */ } })` and proceed only if `claimed.count === 1`, else return 400 'Code already used'. Prefer wrapping the claim + apiKey.create in a single prisma.$transaction so a failed key creation rolls back the claim. Separately, move the route behind authLimiter (the stricter tier) rather than the general apiLimiter. Small, surgical, high-value — a strong Wave-1 candidate (smaller and scarier than several items that shipped).

**Safety / pitfalls.** The apiKeyId currently set on the code points at the created key; with the claim-first ordering, set apiKeyId in a follow-up update inside the same transaction (or create the key first inside the tx, then claim with the id) so the link record still references its key. Keep the existing 404 (invalid) / 400 (expired) branches. Verify generateApiKey() entropy is adequate now that the value is the credential.

---

## Architecture: strategy going forward

> Distilled from the independent audit's architecture pass (§9 — the audit was never split into a standalone file; its findings and this architecture pass are inline throughout this document), re-verified in spots. The framing question: where is the line between a whole revamp, churny PRs, and _actionable_ cleanup — given a future model might one day digest the whole tree at once, but real engineers build on a kernel of good stuff?

**Shape.** soundspan is a **modular monolith + Python sidecars**: a Next.js PWA (custom server proxy, one `ApiClient.request<T>()` chokepoint, AudioEngine adapter strategy) → an Express API + Bull workers in one codebase (role-split by `BACKEND_PROCESS_ROLE`, one Prisma gateway) → Postgres 16 + pgvector, Redis (queues + locks + embed streams), and four Python sidecars. The two analyzers are **`BRPOP` workers that write results straight to Postgres** (DB-as-contract), not HTTP services — a deliberate, good seam.

### Deferred modernization after 2.0.0

These items are deliberately deferred past the 2.0.0 release because each is
invasive and offers little immediate consumer value compared with the release
risk:

| Area                  | Owned decision                                                                                                                                                                                                                                                                                                             |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Backend module format | Defer the CommonJS-to-ESM migration. Revisit it as a coordinated runtime, build, test, and dependency change rather than a pre-release conversion.                                                                                                                                                                         |
| Python sidecar layout | Defer a shared `src/`-layout re-architecture. Revisit it with an explicit packaging and container-entrypoint plan across all sidecars.                                                                                                                                                                                     |
| Frontend lint debt    | Keep the current 0-error/182-warning result as a tracked ratchet: no warning growth, with warning classes burned down in focused follow-ups.                                                                                                                                                                               |
| Branch governance     | Keep `enforce_admins`, required approvals, required conversation resolution, and rulesets off (owner decision, 2026-08-13). The project has one maintainer and an auto-merge flow that these settings would block. 21 status checks are required. Revisit per issue #441 when a second contributor joins or fork PRs open. |

### The kernel — preserve and lean into (do not rewrite)

The most important architectural fact the issue-by-issue review never states: **the codebase already has the two things that make aggressive cleanup — by a human or a future big model — safe rather than reckless.**

1. **A contract-test spine** — 31 `*Contract.test.ts` + a real data-path linter (`scripts/verifyNoIgnoredDataPaths.ts`) encode the boundaries that matter (queue registration, scheduler claim-locks, Prisma resilience, the no-raw-SQL-data-path rule).
2. **The pure-`*Policy.ts` pattern** — ~14 side-effect-free decision modules (backend services + `frontend/lib/audio-engine/*Policy.ts`) backed by ~125 unit tests, already imported by the god-components. This is the proven template for taming the god-files incrementally.

| Kernel piece                                                   | Where                                                    |
| -------------------------------------------------------------- | -------------------------------------------------------- |
| `media-metadata-contract` (the one true cross-component seam)  | `packages/media-metadata-contract`                       |
| Single Prisma gateway + role-aware pool                        | `utils/db.ts`, `worker.ts`                               |
| Contract-test spine + data-path linter                         | 31 `*Contract.test.ts`, `verifyNoIgnoredDataPaths.ts`    |
| Pure `*Policy.ts` + ~125 unit tests (the template)             | `frontend/lib/audio-engine/*Policy.ts`, backend services |
| AudioEngine adapter strategy (Howler / video.js; the Tauri adapter was removed per issue #607) | `frontend/lib/audio-engine/`                             |
| pgvector substrate + `hybridSimilarity` (graceful degradation) | `schema.prisma`, `services/hybridSimilarity.ts`          |
| Feature-folder modularization (the target structure)           | `frontend/features/*`                                    |
| Centralized Bull queue registry                                | `workers/queues.ts`                                      |
| Per-component multi-stage Dockerfiles + `docker-bake`          | `backend/Dockerfile`, `docker-bake.json`                 |

**Strategic answer:** don't revamp, and don't ship churny PRs that split god-files now. (a) Lean into the kernel; (b) clear cheap cruft so the next reader/LLM isn't misled by phantom paths; and (c) **unify shared infra (retry / lock / error) _before_ any god-file split** — splitting `library.ts` today would triple the copies of the retry proxy; unifying it first makes the split mechanical.

### Tiered cleanup

**NOW — a bundled "janitor PR" + a few small ones (XS–M, low risk, behavior-preserving).** All verified dead/orphan in this pass:

- Delete dead **`analysisQueue`** (`workers/queues.ts:106`, defined never produced/consumed — confirm no sidecar producer), dead **`generateMoodTagMix`** (`programmaticPlaylists.ts:2568`, only its own test calls it), dead **`safeError`/`wrapNodeError`** (`utils/errors.ts`, 0 prod callers), and orphan UI **`MediaCard.tsx`** + **`PagedGridCarousel.tsx`** (0 imports).
- Fold the 3 unseeded inline Fisher-Yates copies into `utils/shuffle.ts` (keep the seeded LCG layer — that's the F6 determinism).
- Extract one `utils/withDbRetry` + one proxy factory (collapses the 8 retry copies — see F7).
- api-runtime: run compiled `node dist` not `tsx` (F17/F47); add `engines.node` + `.nvmrc` (F53); `.gitignore` `frontend/tsconfig.tsbuildinfo`; centralize the TIDAL sidecar URL in `config.ts`; unify the sidecar `from common.*` import root.
- **Toast unification** (own PR): two systems mounted at once in `app/layout.tsx` (sonner + custom `toast-context`, ~26 files) → migrate to sonner, delete the custom context (F9).

**STAGE — planned, contract-guarded (M–L, medium risk):**

- Unify the ~7 Redis claim-lock blocks into one `withRedisClaim(key, ttl, op)` (leader-election-critical — guard with the existing claim-lock contract tests).
- Consolidate error handling onto ONE path (today four idioms + ~298 inline `res.status().json()`) — **F1/F14**.
- Type the hot API paths + flip frontend `strict` per `features/*` folder against the existing contract — **F4/F40**.
- Adopt npm **workspaces** (collapses the ~8 `tsc` pre-hooks + the `file:`-copy trap) — **F8/#8**, highest-leverage build-friction fix.
- Add inbound auth to the two HTTP sidecars (mirror `requireInternalSecret`) — **F31**.
- A thin `SidecarClient` to collapse the per-source streaming triplets — **F39/F43**, driven by a concrete new-source need, not preemptively.
- Decompose `library.ts` along resource lines (the proven `subsonic.ts` model) — **but only after** the retry/lock/error infra is unified — **F2/F44**.

**LEAVE for the big-bang (or whenever the file is next touched):** the AIO single-stage image (its one-container simplicity is a real self-host feature), full god-component teardown (`AudioPlaybackOrchestrator` 7080 LOC — keep extracting policies behind the 125-test harness, do **not** rewrite — **F5**), routes-own-Prisma → repository layering (**F44**, only pays off folded into the error/retry unification), and the vibe-vocabulary/legacy-column changes (behavior changes needing eval + destructive migration, not refactors).

### Two course-corrections

- **The "no raw SQL" worry is overstated.** `$queryRaw` is deliberately scoped to search/similarity/embedding reads + a few maintenance writes, guarded by a linter + contract tests. Treat it as a _blessed escape hatch_, not cruft.
- **The codebase is genuinely disciplined where it counts** — only ~5 TODOs, zero FIXME/HACK/@deprecated, no orphaned services, no dishonest skipped tests. The cruft is concentrated in _mechanical duplication_ and a handful of _god-files_ — a much better problem to have. Don't budget for comment-rot or test-rot.

**Net line:** ship the janitor + toast PRs and the cheap Docker/config wins now; stage the infra unification (retry/lock/error) and workspaces deliberately, behind the contract tests; leave the god-file splits and the AIO monolith for after the infra is unified — or for the model that one day reads all 200k lines at once. The kernel will still be there to build on.
