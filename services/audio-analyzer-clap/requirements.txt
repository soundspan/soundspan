# CLAP and ML
laion-clap>=1.1.7
torch>=2.0.0
torchaudio>=2.11.0
torchvision>=0.15.0
transformers>=5.15.0
# librosa is the preprocessing loader for the active embedding space; a major
# version change can shift resample/mel output and drift stored vectors, so it
# stays on the family both images actually run until a deliberate space bump.
# The 1.0 floor (dependabot #470) also cannot resolve for the python3.11 AIO
# interpreter alongside the other sidecar pins.
librosa>=0.11.0,<1.0

# Database and queue
redis>=8.1.0
psycopg2-binary>=2.9.12
pgvector>=0.5.0

# Utilities
python-dotenv>=1.2.2
numpy>=1.26.4
requests>=2.34.2
fastapi>=0.141.1
uvicorn>=0.52.3
